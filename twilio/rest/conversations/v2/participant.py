r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Maestro (Conversations)
    Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination



class ParticipantInstance(InstanceResource):

    class CreateParticipantInConversationRequest(object):
        """
            :ivar name: 
            :ivar type: 
            :ivar profile_id: 
            :ivar addresses: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.type: Optional["ParticipantInstance.str"] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profileId")
            self.addresses: Optional[List[ParticipantList.CreateParticipantInConversationRequestAddresses]] = payload.get("addresses")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "type": self.type,
                    "profileId": self.profile_id,
                    "addresses": [addresses.to_dict() for addresses in self.addresses] if self.addresses is not None else None,
            }

    class CreateParticipantInConversationRequestAddresses(object):
        """
            :ivar channel: 
            :ivar address: 
            :ivar channel_id: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.channel: Optional["ParticipantInstance.str"] = payload.get("channel")
            self.address: Optional[str] = payload.get("address")
            self.channel_id: Optional[str] = payload.get("channelId")

        def to_dict(self):
            return {
                
                    "channel": self.channel,
                    "address": self.address,
                    "channelId": self.channel_id,
            }



    """
    :ivar id: Participant ID.
    :ivar conversation_id: Conversation ID.
    :ivar account_id: Account ID.
    :ivar name: Participant display name.
    :ivar type: Type of Participant in the Conversation.
    :ivar profile_id: Profile ID. Note: This field is only resolved for `CUSTOMER` participant types, not for `HUMAN_AGENT` or `AI_AGENT` participants.
    :ivar addresses: Communication addresses for this Participant. Address format varies by channel: - SMS/VOICE: E.164 phone number (such as \"+18005550100\") - EMAIL: Email address (such as \"user@example.com\") - WHATSAPP: Phone number with whatsapp prefix (such as \"whatsapp:+18005550100\") - RCS: Sender ID or phone number with rcs prefix (such as \"rcs:brand_acme_agent\" or \"rcs:+18005550100\") 
    :ivar created_at: Timestamp when this Participant was created.
    :ivar updated_at: Timestamp when this Participant was last updated.
    """

    def __init__(self, version: Version, payload:Dict[str, Any], conversation_sid: Optional[str] = None, sid: Optional[str] = None):
        super().__init__(version)

        
        self.id: Optional[str] = payload.get("id")
        self.conversation_id: Optional[str] = payload.get("conversationId")
        self.account_id: Optional[str] = payload.get("accountId")
        self.name: Optional[str] = payload.get("name")
        self.type: Optional["ParticipantInstance.str"] = payload.get("type")
        self.profile_id: Optional[str] = payload.get("profileId")
        self.addresses: Optional[List[str]] = payload.get("addresses")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("createdAt"))
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(payload.get("updatedAt"))

        
        self._solution = { 
            "conversation_sid": conversation_sid or self.conversation_sid,
            "sid": sid or self.sid,
        }
        self._context: Optional[ParticipantContext] = None

    @property
    def _proxy(self) -> "ParticipantContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ParticipantContext for this ParticipantInstance
        """
        if self._context is None:
            self._context = ParticipantContext(self._version, conversation_sid=self._solution['conversation_sid'], sid=self._solution['sid'],)
        return self._context
    
    
    def fetch(self) -> "ParticipantInstance":
        """
        Fetch the ParticipantInstance
        

        :returns: The fetched ParticipantInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "ParticipantInstance":
        """
        Asynchronous coroutine to fetch the ParticipantInstance
        

        :returns: The fetched ParticipantInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ParticipantInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ParticipantInstance with HTTP info
        

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()
    
    
    def update(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> "ParticipantInstance":
        """
        Update the ParticipantInstance
        
        :param create_participant_in_conversation_request: 

        :returns: The updated ParticipantInstance
        """
        return self._proxy.update(create_participant_in_conversation_request=create_participant_in_conversation_request, )

    async def update_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> "ParticipantInstance":
        """
        Asynchronous coroutine to update the ParticipantInstance
        
        :param create_participant_in_conversation_request: 

        :returns: The updated ParticipantInstance
        """
        return await self._proxy.update_async(create_participant_in_conversation_request=create_participant_in_conversation_request, )

    def update_with_http_info(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Update the ParticipantInstance with HTTP info
        
        :param create_participant_in_conversation_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(create_participant_in_conversation_request=create_participant_in_conversation_request, )

    async def update_with_http_info_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the ParticipantInstance with HTTP info
        
        :param create_participant_in_conversation_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(create_participant_in_conversation_request=create_participant_in_conversation_request, )
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Conversations.V2.ParticipantInstance {}>'.format(context)

class ParticipantContext(InstanceContext):

    class CreateParticipantInConversationRequest(object):
        """
            :ivar name: 
            :ivar type: 
            :ivar profile_id: 
            :ivar addresses: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.type: Optional["ParticipantInstance.str"] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profileId")
            self.addresses: Optional[List[ParticipantList.CreateParticipantInConversationRequestAddresses]] = payload.get("addresses")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "type": self.type,
                    "profileId": self.profile_id,
                    "addresses": [addresses.to_dict() for addresses in self.addresses] if self.addresses is not None else None,
            }

    class CreateParticipantInConversationRequestAddresses(object):
        """
            :ivar channel: 
            :ivar address: 
            :ivar channel_id: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.channel: Optional["ParticipantInstance.str"] = payload.get("channel")
            self.address: Optional[str] = payload.get("address")
            self.channel_id: Optional[str] = payload.get("channelId")

        def to_dict(self):
            return {
                
                    "channel": self.channel,
                    "address": self.address,
                    "channelId": self.channel_id,
            }


    def __init__(self, version: Version, conversation_sid: str, sid: str):
        """
        Initialize the ParticipantContext

        :param version: Version that contains the resource
        :param conversation_sid: 
        :param sid: 
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'conversation_sid': conversation_sid,
            'sid': sid,
        }
        self._uri = '/Conversations/{conversation_sid}/Participants/{sid}'.format(**self._solution)
        
    
    
    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return self._version.fetch_with_response_info(method='GET', uri=self._uri, headers=headers)

    def fetch(self) -> ParticipantInstance:
        """
        Fetch the ParticipantInstance
        

        :returns: The fetched ParticipantInstance
        """
        payload, _, _ = self._fetch()
        return ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid'],
            
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ParticipantInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        

        headers = values.of({})
        
        
        
        headers["Accept"] = "application/json"
        
        return await self._version.fetch_with_response_info_async(method='GET', uri=self._uri, headers=headers)

    async def fetch_async(self) -> ParticipantInstance:
        """
        Asynchronous coroutine to fetch the ParticipantInstance
        

        :returns: The fetched ParticipantInstance
        """
        payload, _, _ = await self._fetch_async()
        return ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid'],
            
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ParticipantInstance and return response metadata
        

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid'],
            
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def _update(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_participant_in_conversation_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return self._version.update_with_response_info(method='PUT', uri=self._uri, data=data, headers=headers)

    def update(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ParticipantInstance:
        """
        Update the ParticipantInstance
        
        :param create_participant_in_conversation_request: 

        :returns: The updated ParticipantInstance
        """
        payload, _, _ = self._update(create_participant_in_conversation_request=create_participant_in_conversation_request)
        return ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid']
        )

    def update_with_http_info(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Update the ParticipantInstance and return response metadata
        
        :param create_participant_in_conversation_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(create_participant_in_conversation_request=create_participant_in_conversation_request)
        instance = ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_participant_in_conversation_request.to_dict()
        
        headers = values.of({})
        
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        

        return await self._version.update_with_response_info_async(method='PUT', uri=self._uri, data=data, headers=headers)

    async def update_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ParticipantInstance:
        """
        Asynchronous coroutine to update the ParticipantInstance
        
        :param create_participant_in_conversation_request: 

        :returns: The updated ParticipantInstance
        """
        payload, _, _ = await self._update_async(create_participant_in_conversation_request=create_participant_in_conversation_request)
        return ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid']
        )

    async def update_with_http_info_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Asynchronous coroutine to update the ParticipantInstance and return response metadata
        
        :param create_participant_in_conversation_request: 

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(create_participant_in_conversation_request=create_participant_in_conversation_request)
        instance = ParticipantInstance(
            self._version,
            payload,
            conversation_sid=self._solution['conversation_sid'],
            sid=self._solution['sid']
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Conversations.V2.ParticipantContext {}>'.format(context)









class ParticipantPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> ParticipantInstance:
        """
        Build an instance of ParticipantInstance

        :param payload: Payload response from the API
        """
        return ParticipantInstance(self._version, payload, conversation_sid=self._solution["conversation_sid"])

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Conversations.V2.ParticipantPage>"





class ParticipantList(ListResource):
    
    class CreateParticipantInConversationRequest(object):
        """
            :ivar name: 
            :ivar type: 
            :ivar profile_id: 
            :ivar addresses: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.name: Optional[str] = payload.get("name")
            self.type: Optional["ParticipantInstance.str"] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profileId")
            self.addresses: Optional[List[ParticipantList.CreateParticipantInConversationRequestAddresses]] = payload.get("addresses")

        def to_dict(self):
            return {
                
                    "name": self.name,
                    "type": self.type,
                    "profileId": self.profile_id,
                    "addresses": [addresses.to_dict() for addresses in self.addresses] if self.addresses is not None else None,
            }

    class CreateParticipantInConversationRequestAddresses(object):
        """
            :ivar channel: 
            :ivar address: 
            :ivar channel_id: 
        """

        def __init__(self, payload: Dict[str, Any]):

            
            self.channel: Optional["ParticipantInstance.str"] = payload.get("channel")
            self.address: Optional[str] = payload.get("address")
            self.channel_id: Optional[str] = payload.get("channelId")

        def to_dict(self):
            return {
                
                    "channel": self.channel,
                    "address": self.address,
                    "channelId": self.channel_id,
            }


    def __init__(self, version: Version, conversation_sid: str):
        """
        Initialize the ParticipantList

        :param version: Version that contains the resource
        :param conversation_sid: 
        
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 'conversation_sid': conversation_sid,  }
        self._uri = '/Conversations/{conversation_sid}/Participants'.format(**self._solution)
        
        
    
    
    
    def _create(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_participant_in_conversation_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return self._version.create_with_response_info(method='POST', uri=self._uri, data=data, headers=headers)

    def create(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ParticipantInstance:
        """
        Create the ParticipantInstance

        :param create_participant_in_conversation_request: 
        
        :returns: The created ParticipantInstance
        """
        payload, _, _ = self._create(create_participant_in_conversation_request=create_participant_in_conversation_request)
        return ParticipantInstance(self._version, payload, conversation_sid=self._solution['conversation_sid'])

    def create_with_http_info(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Create the ParticipantInstance and return response metadata

        :param create_participant_in_conversation_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(create_participant_in_conversation_request=create_participant_in_conversation_request)
        instance = ParticipantInstance(self._version, payload, conversation_sid=self._solution['conversation_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_participant_in_conversation_request.to_dict()
        
        headers = values.of({
                'Content-Type': 'application/x-www-form-urlencoded'
            })
        
        headers["Content-Type"] = "application/json"
        
        
        headers["Accept"] = "application/json"
        
        
        return await self._version.create_with_response_info_async(method='POST', uri=self._uri, data=data, headers=headers)

    async def create_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ParticipantInstance:
        """
        Asynchronously create the ParticipantInstance

        :param create_participant_in_conversation_request: 
        
        :returns: The created ParticipantInstance
        """
        payload, _, _ = await self._create_async(create_participant_in_conversation_request=create_participant_in_conversation_request)
        return ParticipantInstance(self._version, payload, conversation_sid=self._solution['conversation_sid'])

    async def create_with_http_info_async(self, create_participant_in_conversation_request: Union[CreateParticipantInConversationRequest, object]=values.unset) -> ApiResponse:
        """
        Asynchronously create the ParticipantInstance and return response metadata

        :param create_participant_in_conversation_request: 
        
        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(create_participant_in_conversation_request=create_participant_in_conversation_request)
        instance = ParticipantInstance(self._version, payload, conversation_sid=self._solution['conversation_sid'])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)
    
    
    def stream(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[ParticipantInstance]:
        """
        Streams ParticipantInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream(page, limits['limit'])

    async def stream_async(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[ParticipantInstance]:
        """
        Asynchronously streams ParticipantInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.
        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token,
            page_size=limits['page_size']
        )

        return self._version.stream_async(page, limits['limit'])

    def stream_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams ParticipantInstance and returns headers from first page

        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams ParticipantInstance and returns headers from first page

        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token,
            page_size=limits['page_size']
        )

        generator = self._version.stream_async(page_response.data, limits['limit'])
        return (generator, page_response.status_code, page_response.headers)

    def list(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ParticipantInstance]:
        """
        Lists ParticipantInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(self.stream(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        ))

    async def list_async(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[ParticipantInstance]:
        """
        Asynchronously lists ParticipantInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.
        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [record async for record in await self.stream_async(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )]

    def list_with_http_info(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ParticipantInstance and returns headers from first page

        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ParticipantInstance and returns headers from first page

        
        :param str page_token: Page token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> ParticipantPage:
        """
        Retrieve a single page of ParticipantInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of items to return
        :param page_token: Page token for pagination
        :returns: Page of ParticipantInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = self._version.page(method='GET', uri=self._uri, params=data, headers=headers)
        return ParticipantPage(self._version, response, uri=self._uri, params=data, self._solution)

    async def page_async(self, 
      page_size: Union[int, object] = values.unset,
      page_token: Union[str, object] = values.unset,
    ) -> ParticipantPage:
        """
        Asynchronously retrieve a single page of ParticipantInstance records from the API.
        Request is executed immediately
        
        :param page_size: Maximum number of items to return
        :param page_token: Page token for pagination
        :returns: Page of ParticipantInstance
        """
        data = values.of({ 
            'pageSize': page_size,
            'pageToken': page_token,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response = await self._version.page_async(method='GET', uri=self._uri, params=data, headers=headers)
        return ParticipantPage(self._version, response, uri=self._uri, params=data, self._solution)

    def page_with_http_info(self, 
        
        
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata

        
        :param page_token: Page token for pagination
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ParticipantPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = self._version.page_with_response_info(method='GET', uri=self._uri, params=data, headers=headers)
        page = ParticipantPage(self._version, response, uri=self._uri, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(self, 
        
        page_token: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata

        
        :param page_token: Page token for pagination
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ParticipantPage, status code, and headers
        """
        data = values.of({ 
            'pageToken': page_token,
            'PageToken': page_token,
            'Page': page_number,
            'PageSize': page_size,
        })

        headers = values.of({
        'Content-Type': 'application/x-www-form-urlencoded'
        })
        
        
        headers["Accept"] = "application/json"
        

        response, status_code, response_headers = await self._version.page_with_response_info_async(method='GET', uri=self._uri, params=data, headers=headers)
        page = ParticipantPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ParticipantPage:
        """
        Retrieve a specific page of ParticipantInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ParticipantInstance
        """
        response = self._version.domain.twilio.request(
            'GET',
            target_url
        )
        return ParticipantPage(self._version, response, self._solution)

    async def get_page_async(self, target_url: str) -> ParticipantPage:
        """
        Asynchronously retrieve a specific page of ParticipantInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ParticipantInstance
        """
        response = await self._version.domain.twilio.request_async(
            'GET',
            target_url
        )
        return ParticipantPage(self._version, response, self._solution)



    def get(self, conversation_sid: str, sid: str) -> ParticipantContext:
        """
        Constructs a ParticipantContext
        
        :param conversation_sid: 
        :param sid: 
        """
        return ParticipantContext(self._version, conversation_sid=conversation_sid, sid=sid)

    def __call__(self, conversation_sid: str, sid: str) -> ParticipantContext:
        """
        Constructs a ParticipantContext
        
        :param conversation_sid: 
        :param sid: 
        """
        return ParticipantContext(self._version, conversation_sid=conversation_sid, sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Conversations.V2.ParticipantList>'

