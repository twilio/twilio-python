r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Maestro (Conversations)
    Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class CommunicationInstance(InstanceResource):
    class ContentTranscriptionTranscription(object):
        """
        :ivar channel: Audio channel identifier (0 for inbound, 1 for outbound).
        :ivar confidence: Overall confidence score for the transcription (0.0-1.0).
        :ivar engine: Transcription engine used.
        :ivar words: Word-level transcription data with timing information.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")
            self.words: Optional[
                List[CommunicationList.ContentTranscriptionTranscriptionWords]
            ] = payload.get("words")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
                "words": [words.to_dict() for words in self.words]
                if self.words is not None
                else None,
            }

    class ContentTranscriptionTranscriptionWords(object):
        """
        :ivar text: The transcribed word.
        :ivar start_time: Start timestamp of this word.
        :ivar end_time: End timestamp of this word.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")
            self.start_time: Optional[datetime] = payload.get("start_time")
            self.end_time: Optional[datetime] = payload.get("end_time")

        def to_dict(self):
            return {
                "text": self.text,
                "start_time": self.start_time,
                "end_time": self.end_time,
            }

    class CreateCommunicationRequest(object):
        """
        :ivar conversation_id: Optional. Routes to an existing conversation. If provided, the communication will be sent within this conversation.
        :ivar configuration_id: Optional. Used for routing and conversation resolution. If provided without conversationId, the system will resolve or create a conversation based on participant addresses.
        :ivar author:
        :ivar recipients: The recipients of this communication.
        :ivar content:
        :ivar channel_id: Optional external reference identifier (e.g., MessageSid, CallSid).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.configuration_id: Optional[str] = payload.get("configuration_id")
            self.author: Optional[
                CommunicationList.CreateCommunicationRequestAuthor
            ] = payload.get("author")
            self.recipients: Optional[
                List[CommunicationList.CreateCommunicationRequestAuthor]
            ] = payload.get("recipients")
            self.content: Optional[
                CommunicationList.CreateCommunicationRequestContent
            ] = payload.get("content")
            self.channel_id: Optional[str] = payload.get("channel_id")

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "configuration_id": self.configuration_id,
                "author": self.author.to_dict() if self.author is not None else None,
                "recipients": [recipients.to_dict() for recipients in self.recipients]
                if self.recipients is not None
                else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channel_id": self.channel_id,
            }

    class CreateCommunicationRequestAuthor(object):
        """
        :ivar address: The address value formatted according to channel type: - SMS: E.164 phone number (such as \"+18005550100\") - WHATSAPP: Phone number with whatsapp prefix (such as \"whatsapp:+18005550100\") - RCS: Sender ID or phone number with rcs prefix (such as \"rcs:brand_acme_agent\" or \"rcs:+18005550100\") - CHAT: Customer-defined string identifier
        :ivar channel: Channel type for sending communications. Note: VOICE is receive-only and not supported for send operations.
        :ivar participant_id: Optional Participant ID. If omitted, the system will resolve or create the participant based on address and channel.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional["CommunicationInstance.str"] = payload.get("channel")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "channel": self.channel,
                "participant_id": self.participant_id,
            }

    class CreateCommunicationRequestContent(object):
        """
        :ivar type: Content type discriminator.
        :ivar text: Transcribed text.
        :ivar transcription:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["CommunicationInstance.str"] = payload.get("type")
            self.text: Optional[str] = payload.get("text")
            self.transcription: Optional[
                CommunicationList.ContentTranscriptionTranscription
            ] = payload.get("transcription")

        def to_dict(self):
            return {
                "type": self.type,
                "text": self.text,
                "transcription": self.transcription.to_dict()
                if self.transcription is not None
                else None,
            }

    """
    :ivar message: Status message indicating the request was accepted.
    :ivar conversation_id: The ID of the conversation (may be newly created).
    :ivar communication_id: The ID of the created communication, if available. May be absent when endpoint performs setup-only without persisting a communication record.
    :ivar channel_id: Channel-specific message ID if available (e.g., MessageSid from messaging adapter).
    :ivar id: Communication ID.
    :ivar account_id: Account ID.
    :ivar author: 
    :ivar content: 
    :ivar recipients: Communication recipients.
    :ivar created_at: Timestamp when this Communication was created.
    :ivar updated_at: Timestamp when this Communication was last updated.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        conversation_sid: Optional[str] = None,
        sid: Optional[str] = None,
    ):
        super().__init__(version)

        self.message: Optional[str] = payload.get("message")
        self.conversation_id: Optional[str] = payload.get("conversationId")
        self.communication_id: Optional[str] = payload.get("communicationId")
        self.channel_id: Optional[str] = payload.get("channelId")
        self.id: Optional[str] = payload.get("id")
        self.account_id: Optional[str] = payload.get("accountId")
        self.author: Optional[str] = payload.get("author")
        self.content: Optional[str] = payload.get("content")
        self.recipients: Optional[List[str]] = payload.get("recipients")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )

        self._solution = {
            "conversation_sid": conversation_sid or self.conversation_sid,
            "sid": sid or self.sid,
        }
        self._context: Optional[CommunicationContext] = None

    @property
    def _proxy(self) -> "CommunicationContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: CommunicationContext for this CommunicationInstance
        """
        if self._context is None:
            self._context = CommunicationContext(
                self._version,
                conversation_sid=self._solution["conversation_sid"],
                sid=self._solution["sid"],
            )
        return self._context

    def fetch(self) -> "CommunicationInstance":
        """
        Fetch the CommunicationInstance


        :returns: The fetched CommunicationInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "CommunicationInstance":
        """
        Asynchronous coroutine to fetch the CommunicationInstance


        :returns: The fetched CommunicationInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the CommunicationInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the CommunicationInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Conversations.V2.CommunicationInstance {}>".format(context)


class CommunicationContext(InstanceContext):
    class ContentTranscriptionTranscription(object):
        """
        :ivar channel: Audio channel identifier (0 for inbound, 1 for outbound).
        :ivar confidence: Overall confidence score for the transcription (0.0-1.0).
        :ivar engine: Transcription engine used.
        :ivar words: Word-level transcription data with timing information.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")
            self.words: Optional[
                List[CommunicationList.ContentTranscriptionTranscriptionWords]
            ] = payload.get("words")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
                "words": [words.to_dict() for words in self.words]
                if self.words is not None
                else None,
            }

    class ContentTranscriptionTranscriptionWords(object):
        """
        :ivar text: The transcribed word.
        :ivar start_time: Start timestamp of this word.
        :ivar end_time: End timestamp of this word.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")
            self.start_time: Optional[datetime] = payload.get("start_time")
            self.end_time: Optional[datetime] = payload.get("end_time")

        def to_dict(self):
            return {
                "text": self.text,
                "start_time": self.start_time,
                "end_time": self.end_time,
            }

    class CreateCommunicationRequest(object):
        """
        :ivar conversation_id: Optional. Routes to an existing conversation. If provided, the communication will be sent within this conversation.
        :ivar configuration_id: Optional. Used for routing and conversation resolution. If provided without conversationId, the system will resolve or create a conversation based on participant addresses.
        :ivar author:
        :ivar recipients: The recipients of this communication.
        :ivar content:
        :ivar channel_id: Optional external reference identifier (e.g., MessageSid, CallSid).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.configuration_id: Optional[str] = payload.get("configuration_id")
            self.author: Optional[
                CommunicationList.CreateCommunicationRequestAuthor
            ] = payload.get("author")
            self.recipients: Optional[
                List[CommunicationList.CreateCommunicationRequestAuthor]
            ] = payload.get("recipients")
            self.content: Optional[
                CommunicationList.CreateCommunicationRequestContent
            ] = payload.get("content")
            self.channel_id: Optional[str] = payload.get("channel_id")

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "configuration_id": self.configuration_id,
                "author": self.author.to_dict() if self.author is not None else None,
                "recipients": [recipients.to_dict() for recipients in self.recipients]
                if self.recipients is not None
                else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channel_id": self.channel_id,
            }

    class CreateCommunicationRequestAuthor(object):
        """
        :ivar address: The address value formatted according to channel type: - SMS: E.164 phone number (such as \"+18005550100\") - WHATSAPP: Phone number with whatsapp prefix (such as \"whatsapp:+18005550100\") - RCS: Sender ID or phone number with rcs prefix (such as \"rcs:brand_acme_agent\" or \"rcs:+18005550100\") - CHAT: Customer-defined string identifier
        :ivar channel: Channel type for sending communications. Note: VOICE is receive-only and not supported for send operations.
        :ivar participant_id: Optional Participant ID. If omitted, the system will resolve or create the participant based on address and channel.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional["CommunicationInstance.str"] = payload.get("channel")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "channel": self.channel,
                "participant_id": self.participant_id,
            }

    class CreateCommunicationRequestContent(object):
        """
        :ivar type: Content type discriminator.
        :ivar text: Transcribed text.
        :ivar transcription:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["CommunicationInstance.str"] = payload.get("type")
            self.text: Optional[str] = payload.get("text")
            self.transcription: Optional[
                CommunicationList.ContentTranscriptionTranscription
            ] = payload.get("transcription")

        def to_dict(self):
            return {
                "type": self.type,
                "text": self.text,
                "transcription": self.transcription.to_dict()
                if self.transcription is not None
                else None,
            }

    def __init__(self, version: Version, conversation_sid: str, sid: str):
        """
        Initialize the CommunicationContext

        :param version: Version that contains the resource
        :param conversation_sid:
        :param sid:
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "conversation_sid": conversation_sid,
            "sid": sid,
        }
        self._uri = "/Conversations/{conversation_sid}/Communications/{sid}".format(
            **self._solution
        )

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> CommunicationInstance:
        """
        Fetch the CommunicationInstance


        :returns: The fetched CommunicationInstance
        """
        payload, _, _ = self._fetch()
        return CommunicationInstance(
            self._version,
            payload,
            conversation_sid=self._solution["conversation_sid"],
            sid=self._solution["sid"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the CommunicationInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = CommunicationInstance(
            self._version,
            payload,
            conversation_sid=self._solution["conversation_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> CommunicationInstance:
        """
        Asynchronous coroutine to fetch the CommunicationInstance


        :returns: The fetched CommunicationInstance
        """
        payload, _, _ = await self._fetch_async()
        return CommunicationInstance(
            self._version,
            payload,
            conversation_sid=self._solution["conversation_sid"],
            sid=self._solution["sid"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the CommunicationInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = CommunicationInstance(
            self._version,
            payload,
            conversation_sid=self._solution["conversation_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Conversations.V2.CommunicationContext {}>".format(context)


class CommunicationList(ListResource):
    class ContentTranscriptionTranscription(object):
        """
        :ivar channel: Audio channel identifier (0 for inbound, 1 for outbound).
        :ivar confidence: Overall confidence score for the transcription (0.0-1.0).
        :ivar engine: Transcription engine used.
        :ivar words: Word-level transcription data with timing information.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")
            self.words: Optional[
                List[CommunicationList.ContentTranscriptionTranscriptionWords]
            ] = payload.get("words")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
                "words": [words.to_dict() for words in self.words]
                if self.words is not None
                else None,
            }

    class ContentTranscriptionTranscriptionWords(object):
        """
        :ivar text: The transcribed word.
        :ivar start_time: Start timestamp of this word.
        :ivar end_time: End timestamp of this word.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")
            self.start_time: Optional[datetime] = payload.get("start_time")
            self.end_time: Optional[datetime] = payload.get("end_time")

        def to_dict(self):
            return {
                "text": self.text,
                "start_time": self.start_time,
                "end_time": self.end_time,
            }

    class CreateCommunicationRequest(object):
        """
        :ivar conversation_id: Optional. Routes to an existing conversation. If provided, the communication will be sent within this conversation.
        :ivar configuration_id: Optional. Used for routing and conversation resolution. If provided without conversationId, the system will resolve or create a conversation based on participant addresses.
        :ivar author:
        :ivar recipients: The recipients of this communication.
        :ivar content:
        :ivar channel_id: Optional external reference identifier (e.g., MessageSid, CallSid).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.configuration_id: Optional[str] = payload.get("configuration_id")
            self.author: Optional[
                CommunicationList.CreateCommunicationRequestAuthor
            ] = payload.get("author")
            self.recipients: Optional[
                List[CommunicationList.CreateCommunicationRequestAuthor]
            ] = payload.get("recipients")
            self.content: Optional[
                CommunicationList.CreateCommunicationRequestContent
            ] = payload.get("content")
            self.channel_id: Optional[str] = payload.get("channel_id")

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "configuration_id": self.configuration_id,
                "author": self.author.to_dict() if self.author is not None else None,
                "recipients": [recipients.to_dict() for recipients in self.recipients]
                if self.recipients is not None
                else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channel_id": self.channel_id,
            }

    class CreateCommunicationRequestAuthor(object):
        """
        :ivar address: The address value formatted according to channel type: - SMS: E.164 phone number (such as \"+18005550100\") - WHATSAPP: Phone number with whatsapp prefix (such as \"whatsapp:+18005550100\") - RCS: Sender ID or phone number with rcs prefix (such as \"rcs:brand_acme_agent\" or \"rcs:+18005550100\") - CHAT: Customer-defined string identifier
        :ivar channel: Channel type for sending communications. Note: VOICE is receive-only and not supported for send operations.
        :ivar participant_id: Optional Participant ID. If omitted, the system will resolve or create the participant based on address and channel.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional["CommunicationInstance.str"] = payload.get("channel")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "channel": self.channel,
                "participant_id": self.participant_id,
            }

    class CreateCommunicationRequestContent(object):
        """
        :ivar type: Content type discriminator.
        :ivar text: Transcribed text.
        :ivar transcription:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["CommunicationInstance.str"] = payload.get("type")
            self.text: Optional[str] = payload.get("text")
            self.transcription: Optional[
                CommunicationList.ContentTranscriptionTranscription
            ] = payload.get("transcription")

        def to_dict(self):
            return {
                "type": self.type,
                "text": self.text,
                "transcription": self.transcription.to_dict()
                if self.transcription is not None
                else None,
            }

    def __init__(self, version: Version):
        """
        Initialize the CommunicationList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/Communications"

    def _create(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_communication_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> CommunicationInstance:
        """
        Create the CommunicationInstance

        :param create_communication_request: The communication to send

        :returns: The created CommunicationInstance
        """
        payload, _, _ = self._create(
            create_communication_request=create_communication_request
        )
        return CommunicationInstance(self._version, payload)

    def create_with_http_info(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Create the CommunicationInstance and return response metadata

        :param create_communication_request: The communication to send

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            create_communication_request=create_communication_request
        )
        instance = CommunicationInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_communication_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> CommunicationInstance:
        """
        Asynchronously create the CommunicationInstance

        :param create_communication_request: The communication to send

        :returns: The created CommunicationInstance
        """
        payload, _, _ = await self._create_async(
            create_communication_request=create_communication_request
        )
        return CommunicationInstance(self._version, payload)

    async def create_with_http_info_async(
        self,
        create_communication_request: Union[
            CreateCommunicationRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the CommunicationInstance and return response metadata

        :param create_communication_request: The communication to send

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            create_communication_request=create_communication_request
        )
        instance = CommunicationInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def get(self, conversation_sid: str, sid: str) -> CommunicationContext:
        """
        Constructs a CommunicationContext

        :param conversation_sid:
        :param sid:
        """
        return CommunicationContext(
            self._version, conversation_sid=conversation_sid, sid=sid
        )

    def __call__(self, conversation_sid: str, sid: str) -> CommunicationContext:
        """
        Constructs a CommunicationContext

        :param conversation_sid:
        :param sid:
        """
        return CommunicationContext(
            self._version, conversation_sid=conversation_sid, sid=sid
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Conversations.V2.CommunicationList>"
