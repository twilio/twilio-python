r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Maestro (Conversations)
    Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page


class CommunicationInConversationInstance(InstanceResource):
    class ContentTranscription1Transcription(object):
        """
        :ivar channel:
        :ivar confidence:
        :ivar engine:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
            }

    class ContentTranscriptionTranscription(object):
        """
        :ivar channel: Audio channel identifier (0 for inbound, 1 for outbound).
        :ivar confidence: Overall confidence score for the transcription (0.0-1.0).
        :ivar engine: Transcription engine used.
        :ivar words: Word-level transcription data with timing information.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")
            self.words: Optional[
                List[CommunicationList.ContentTranscriptionTranscriptionWords]
            ] = payload.get("words")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
                "words": [words.to_dict() for words in self.words]
                if self.words is not None
                else None,
            }

    class ContentTranscriptionTranscriptionWords(object):
        """
        :ivar text: The transcribed word.
        :ivar start_time: Start timestamp of this word.
        :ivar end_time: End timestamp of this word.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")
            self.start_time: Optional[datetime] = payload.get("start_time")
            self.end_time: Optional[datetime] = payload.get("end_time")

        def to_dict(self):
            return {
                "text": self.text,
                "start_time": self.start_time,
                "end_time": self.end_time,
            }

    class CreateCommunicationInConversationRequest(object):
        """
        :ivar author:
        :ivar content:
        :ivar channel_id:
        :ivar recipients:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.author: Optional[
                CommunicationInConversationList.CreateCommunicationInConversationRequestAuthor
            ] = payload.get("author")
            self.content: Optional[
                CommunicationInConversationList.CreateCommunicationInConversationRequestContent
            ] = payload.get("content")
            self.channel_id: Optional[str] = payload.get("channel_id")
            self.recipients: Optional[
                List[
                    CommunicationInConversationList.CreateCommunicationInConversationRequestAuthor
                ]
            ] = payload.get("recipients")

        def to_dict(self):
            return {
                "author": self.author.to_dict() if self.author is not None else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channel_id": self.channel_id,
                "recipients": [recipients.to_dict() for recipients in self.recipients]
                if self.recipients is not None
                else None,
            }

    class CreateCommunicationInConversationRequestAuthor(object):
        """
        :ivar address:
        :ivar channel:
        :ivar participant_id:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional[
                "CommunicationInConversationInstance.str"
            ] = payload.get("channel")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "channel": self.channel,
                "participant_id": self.participant_id,
            }

    class CreateCommunicationInConversationRequestContent(object):
        """
        :ivar type:
        :ivar text:
        :ivar transcription:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional[
                "CommunicationInConversationInstance.str"
            ] = payload.get("type")
            self.text: Optional[str] = payload.get("text")
            self.transcription: Optional[
                CommunicationInConversationList.ContentTranscription1Transcription
            ] = payload.get("transcription")

        def to_dict(self):
            return {
                "type": self.type,
                "text": self.text,
                "transcription": self.transcription.to_dict()
                if self.transcription is not None
                else None,
            }

    """
    :ivar id: Communication ID.
    :ivar conversation_id: Conversation ID.
    :ivar account_id: Account ID.
    :ivar author: 
    :ivar content: 
    :ivar channel_id: Channel-specific reference ID.
    :ivar recipients: Communication recipients.
    :ivar created_at: Timestamp when this Communication was created.
    :ivar updated_at: Timestamp when this Communication was last updated.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        conversation_sid: Optional[str] = None,
    ):
        super().__init__(version)

        self.id: Optional[str] = payload.get("id")
        self.conversation_id: Optional[str] = payload.get("conversationId")
        self.account_id: Optional[str] = payload.get("accountId")
        self.author: Optional[str] = payload.get("author")
        self.content: Optional[str] = payload.get("content")
        self.channel_id: Optional[str] = payload.get("channelId")
        self.recipients: Optional[List[str]] = payload.get("recipients")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )

        self._solution = {
            "conversation_sid": conversation_sid or self.conversation_sid,
        }

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return (
            "<Twilio.Conversations.V2.CommunicationInConversationInstance {}>".format(
                context
            )
        )


class CommunicationInConversationPage(Page):
    def get_instance(
        self, payload: Dict[str, Any]
    ) -> CommunicationInConversationInstance:
        """
        Build an instance of CommunicationInConversationInstance

        :param payload: Payload response from the API
        """
        return CommunicationInConversationInstance(
            self._version, payload, conversation_sid=self._solution["conversation_sid"]
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Conversations.V2.CommunicationInConversationPage>"


class CommunicationInConversationList(ListResource):
    class ContentTranscription1Transcription(object):
        """
        :ivar channel:
        :ivar confidence:
        :ivar engine:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
            }

    class ContentTranscriptionTranscription(object):
        """
        :ivar channel: Audio channel identifier (0 for inbound, 1 for outbound).
        :ivar confidence: Overall confidence score for the transcription (0.0-1.0).
        :ivar engine: Transcription engine used.
        :ivar words: Word-level transcription data with timing information.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.channel: Optional[int] = payload.get("channel")
            self.confidence: Optional[float] = payload.get("confidence")
            self.engine: Optional[str] = payload.get("engine")
            self.words: Optional[
                List[CommunicationList.ContentTranscriptionTranscriptionWords]
            ] = payload.get("words")

        def to_dict(self):
            return {
                "channel": self.channel,
                "confidence": self.confidence,
                "engine": self.engine,
                "words": [words.to_dict() for words in self.words]
                if self.words is not None
                else None,
            }

    class ContentTranscriptionTranscriptionWords(object):
        """
        :ivar text: The transcribed word.
        :ivar start_time: Start timestamp of this word.
        :ivar end_time: End timestamp of this word.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")
            self.start_time: Optional[datetime] = payload.get("start_time")
            self.end_time: Optional[datetime] = payload.get("end_time")

        def to_dict(self):
            return {
                "text": self.text,
                "start_time": self.start_time,
                "end_time": self.end_time,
            }

    class CreateCommunicationInConversationRequest(object):
        """
        :ivar author:
        :ivar content:
        :ivar channel_id:
        :ivar recipients:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.author: Optional[
                CommunicationInConversationList.CreateCommunicationInConversationRequestAuthor
            ] = payload.get("author")
            self.content: Optional[
                CommunicationInConversationList.CreateCommunicationInConversationRequestContent
            ] = payload.get("content")
            self.channel_id: Optional[str] = payload.get("channel_id")
            self.recipients: Optional[
                List[
                    CommunicationInConversationList.CreateCommunicationInConversationRequestAuthor
                ]
            ] = payload.get("recipients")

        def to_dict(self):
            return {
                "author": self.author.to_dict() if self.author is not None else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channel_id": self.channel_id,
                "recipients": [recipients.to_dict() for recipients in self.recipients]
                if self.recipients is not None
                else None,
            }

    class CreateCommunicationInConversationRequestAuthor(object):
        """
        :ivar address:
        :ivar channel:
        :ivar participant_id:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional[
                "CommunicationInConversationInstance.str"
            ] = payload.get("channel")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "channel": self.channel,
                "participant_id": self.participant_id,
            }

    class CreateCommunicationInConversationRequestContent(object):
        """
        :ivar type:
        :ivar text:
        :ivar transcription:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional[
                "CommunicationInConversationInstance.str"
            ] = payload.get("type")
            self.text: Optional[str] = payload.get("text")
            self.transcription: Optional[
                CommunicationInConversationList.ContentTranscription1Transcription
            ] = payload.get("transcription")

        def to_dict(self):
            return {
                "type": self.type,
                "text": self.text,
                "transcription": self.transcription.to_dict()
                if self.transcription is not None
                else None,
            }

    def __init__(self, version: Version, conversation_sid: str):
        """
        Initialize the CommunicationInConversationList

        :param version: Version that contains the resource
        :param conversation_sid:

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "conversation_sid": conversation_sid,
        }
        self._uri = "/Conversations/{conversation_sid}/Communications".format(
            **self._solution
        )

    def _create(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_communication_in_conversation_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> CommunicationInConversationInstance:
        """
        Create the CommunicationInConversationInstance

        :param create_communication_in_conversation_request:

        :returns: The created CommunicationInConversationInstance
        """
        payload, _, _ = self._create(
            create_communication_in_conversation_request=create_communication_in_conversation_request
        )
        return CommunicationInConversationInstance(
            self._version, payload, conversation_sid=self._solution["conversation_sid"]
        )

    def create_with_http_info(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Create the CommunicationInConversationInstance and return response metadata

        :param create_communication_in_conversation_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            create_communication_in_conversation_request=create_communication_in_conversation_request
        )
        instance = CommunicationInConversationInstance(
            self._version, payload, conversation_sid=self._solution["conversation_sid"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_communication_in_conversation_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> CommunicationInConversationInstance:
        """
        Asynchronously create the CommunicationInConversationInstance

        :param create_communication_in_conversation_request:

        :returns: The created CommunicationInConversationInstance
        """
        payload, _, _ = await self._create_async(
            create_communication_in_conversation_request=create_communication_in_conversation_request
        )
        return CommunicationInConversationInstance(
            self._version, payload, conversation_sid=self._solution["conversation_sid"]
        )

    async def create_with_http_info_async(
        self,
        create_communication_in_conversation_request: Union[
            CreateCommunicationInConversationRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the CommunicationInConversationInstance and return response metadata

        :param create_communication_in_conversation_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            create_communication_in_conversation_request=create_communication_in_conversation_request
        )
        instance = CommunicationInConversationInstance(
            self._version, payload, conversation_sid=self._solution["conversation_sid"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[CommunicationInConversationInstance]:
        """
        Streams CommunicationInConversationInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(channel_id=channel_id, page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[CommunicationInConversationInstance]:
        """
        Asynchronously streams CommunicationInConversationInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            channel_id=channel_id, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams CommunicationInConversationInstance and returns headers from first page


        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            channel_id=channel_id, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams CommunicationInConversationInstance and returns headers from first page


        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            channel_id=channel_id, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[CommunicationInConversationInstance]:
        """
        Lists CommunicationInConversationInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                channel_id=channel_id,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[CommunicationInConversationInstance]:
        """
        Asynchronously lists CommunicationInConversationInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                channel_id=channel_id,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists CommunicationInConversationInstance and returns headers from first page


        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            channel_id=channel_id,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        channel_id: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists CommunicationInConversationInstance and returns headers from first page


        :param str channel_id: Resource identifier to filter communications
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            channel_id=channel_id,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        channel_id: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> CommunicationInConversationPage:
        """
        Retrieve a single page of CommunicationInConversationInstance records from the API.
        Request is executed immediately

        :param channel_id: Resource identifier to filter communications
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of CommunicationInConversationInstance
        """
        data = values.of(
            {
                "channelId": channel_id,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return CommunicationInConversationPage(self._version, response, self._solution)

    async def page_async(
        self,
        channel_id: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> CommunicationInConversationPage:
        """
        Asynchronously retrieve a single page of CommunicationInConversationInstance records from the API.
        Request is executed immediately

        :param channel_id: Resource identifier to filter communications
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of CommunicationInConversationInstance
        """
        data = values.of(
            {
                "channelId": channel_id,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return CommunicationInConversationPage(self._version, response, self._solution)

    def page_with_http_info(
        self,
        channel_id: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param channel_id: Resource identifier to filter communications
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with CommunicationInConversationPage, status code, and headers
        """
        data = values.of(
            {
                "channelId": channel_id,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = CommunicationInConversationPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        channel_id: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param channel_id: Resource identifier to filter communications
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with CommunicationInConversationPage, status code, and headers
        """
        data = values.of(
            {
                "channelId": channel_id,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        (
            response,
            status_code,
            response_headers,
        ) = await self._version.page_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = CommunicationInConversationPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> CommunicationInConversationPage:
        """
        Retrieve a specific page of CommunicationInConversationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of CommunicationInConversationInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return CommunicationInConversationPage(self._version, response, self._solution)

    async def get_page_async(self, target_url: str) -> CommunicationInConversationPage:
        """
        Asynchronously retrieve a specific page of CommunicationInConversationInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of CommunicationInConversationInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return CommunicationInConversationPage(self._version, response, self._solution)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Conversations.V2.CommunicationInConversationList>"
