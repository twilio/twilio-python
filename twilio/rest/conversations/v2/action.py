r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Conversation Orchestrator
    Manage configurations, conversations, participants, and communications. Create configurations to define capture rules and channel settings, then use conversations to group related communications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class ActionInstance(InstanceResource):

    class ConversationsV2ActionStatus(object):
        PENDING = "PENDING"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"

    class ConversationsV2Channel(object):
        VOICE = "VOICE"
        SMS = "SMS"
        RCS = "RCS"
        WHATSAPP = "WHATSAPP"
        CHAT = "CHAT"

    """
    :ivar id: Unique identifier for this Action.
    :ivar type: The type of action. Accepted values: SEND_MESSAGE.
    :ivar status: 
    :ivar conversation_id: The conversation this action belongs to.
    :ivar related: Named identifiers from downstream. For SEND_MESSAGE: - messageSid: The downstream message SID (present when PENDING or COMPLETED) - communicationId: The Communication ID (present when COMPLETED) 
    :ivar created_at: Timestamp when the action was created.
    :ivar updated_at: Timestamp when the action was last updated.
    :ivar completed_at: Timestamp when the action reached a terminal status.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        conversation_id: str,
        action_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.id: Optional[str] = payload.get("id")
        self.type: Optional[str] = payload.get("type")
        self.status: Optional["ActionInstance.str"] = payload.get("status")
        self.conversation_id: Optional[str] = payload.get("conversationId")
        self.related: Optional[Dict[str, str]] = payload.get("related")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )
        self.completed_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("completedAt")
        )

        # Only set _solution if path params are provided (not None)
        if conversation_id is not None or action_id is not None:
            self._solution = {
                "conversation_id": conversation_id,
                "action_id": action_id,
            }

        self._context: Optional[ActionContext] = None

    @property
    def _proxy(self) -> "ActionContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ActionContext for this ActionInstance
        """
        if self._context is None:
            self._context = ActionContext(
                self._version,
                conversation_id=self._solution["conversation_id"],
                action_id=self._solution["action_id"],
            )
        return self._context

    def fetch(self) -> "ActionInstance":
        """
        Fetch the ActionInstance


        :returns: The fetched ActionInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "ActionInstance":
        """
        Asynchronous coroutine to fetch the ActionInstance


        :returns: The fetched ActionInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ActionInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ActionInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Conversations.V2.ActionInstance {}>".format(context)


class ActionContext(InstanceContext):

    def __init__(self, version: Version, conversation_id: str, action_id: str):
        """
        Initialize the ActionContext

        :param version: Version that contains the resource
        :param conversation_id:
        :param action_id:
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "conversation_id": conversation_id,
            "action_id": action_id,
        }
        self._uri = "/Conversations/{conversation_id}/Actions/{action_id}".format(
            **self._solution
        )

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> ActionInstance:
        """
        Fetch the ActionInstance


        :returns: The fetched ActionInstance
        """
        payload, _, _ = self._fetch()
        return ActionInstance(
            self._version,
            payload,
            conversation_id=self._solution["conversation_id"],
            action_id=self._solution["action_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ActionInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = ActionInstance(
            self._version,
            payload,
            conversation_id=self._solution["conversation_id"],
            action_id=self._solution["action_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> ActionInstance:
        """
        Asynchronous coroutine to fetch the ActionInstance


        :returns: The fetched ActionInstance
        """
        payload, _, _ = await self._fetch_async()
        return ActionInstance(
            self._version,
            payload,
            conversation_id=self._solution["conversation_id"],
            action_id=self._solution["action_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ActionInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = ActionInstance(
            self._version,
            payload,
            conversation_id=self._solution["conversation_id"],
            action_id=self._solution["action_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Conversations.V2.ActionContext {}>".format(context)


class ActionList(ListResource):

    class ConversationsV2SendMessageContent(object):
        """
        :ivar text: Plain text message body.
        :ivar content_id: Content template ID (HX... format). When provided, the template is rendered with the variables map and sent to the recipient.
        :ivar variables: Variables to substitute into the content template.
        :ivar media_urls: URLs of media attachments to include with the message.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.text: Optional[str] = payload.get("text")
            self.content_id: Optional[str] = payload.get("contentId")
            self.variables: Optional[Dict[str, str]] = payload.get("variables")
            self.media_urls: Optional[List[str]] = payload.get("mediaUrls")

        def to_dict(self):
            return {
                "text": self.text,
                "contentId": self.content_id,
                "variables": self.variables,
                "mediaUrls": self.media_urls,
            }

    class ConversationsV2SendMessageParticipant(object):
        """
        :ivar participant_id: Participant ID to resolve address from.
        :ivar address: Explicit address formatted according to channel type.
        :ivar channel:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.participant_id: Optional[str] = payload.get("participantId")
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional["ActionInstance.ConversationsV2Channel"] = (
                payload.get("channel")
            )

        def to_dict(self):
            return {
                "participantId": self.participant_id,
                "address": self.address,
                "channel": self.channel,
            }

    class ConversationsV2SendMessagePayload(object):
        """
        :ivar _from:
        :ivar to: The recipients of this action.
        :ivar content:
        :ivar channel_settings: Channel-specific parameters forwarded as-is to the downstream sending service. Allows passing backend-specific fields without requiring API changes.
        """

        def __init__(self, payload: Dict[str, Any]):

            self._from: Optional[ActionList.ConversationsV2SendMessageParticipant] = (
                payload.get("from")
            )
            self.to: Optional[
                List[ActionList.ConversationsV2SendMessageParticipant]
            ] = payload.get("to")
            self.content: Optional[ActionList.ConversationsV2SendMessageContent] = (
                payload.get("content")
            )
            self.channel_settings: Optional[Dict[str, object]] = payload.get(
                "channelSettings"
            )

        def to_dict(self):
            return {
                "from": self._from.to_dict() if self._from is not None else None,
                "to": [to.to_dict() for to in self.to] if self.to is not None else None,
                "content": self.content.to_dict() if self.content is not None else None,
                "channelSettings": self.channel_settings,
            }

    class CreateConversationActionRequest(object):
        """
        :ivar type: Action type discriminator. Accepted values: SEND_MESSAGE.
        :ivar payload:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.type: Optional[str] = payload.get("type")
            self.payload: Optional[ActionList.ConversationsV2SendMessagePayload] = (
                payload.get("payload")
            )

        def to_dict(self):
            return {
                "type": self.type,
                "payload": self.payload.to_dict() if self.payload is not None else None,
            }

    def __init__(self, version: Version, conversation_id: str):
        """
        Initialize the ActionList

        :param version: Version that contains the resource
        :param conversation_id:

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "conversation_id": conversation_id,
        }
        self._uri = "/Conversations/{conversation_id}/Actions".format(**self._solution)

    def _create(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_conversation_action_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> ActionInstance:
        """
        Create the ActionInstance

        :param create_conversation_action_request: The action to perform.

        :returns: The created ActionInstance
        """
        payload, _, _ = self._create(
            create_conversation_action_request=create_conversation_action_request
        )
        return ActionInstance(
            self._version, payload, conversation_id=self._solution["conversation_id"]
        )

    def create_with_http_info(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> ApiResponse:
        """
        Create the ActionInstance and return response metadata

        :param create_conversation_action_request: The action to perform.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            create_conversation_action_request=create_conversation_action_request
        )
        instance = ActionInstance(
            self._version, payload, conversation_id=self._solution["conversation_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_conversation_action_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> ActionInstance:
        """
        Asynchronously create the ActionInstance

        :param create_conversation_action_request: The action to perform.

        :returns: The created ActionInstance
        """
        payload, _, _ = await self._create_async(
            create_conversation_action_request=create_conversation_action_request
        )
        return ActionInstance(
            self._version, payload, conversation_id=self._solution["conversation_id"]
        )

    async def create_with_http_info_async(
        self, create_conversation_action_request: CreateConversationActionRequest
    ) -> ApiResponse:
        """
        Asynchronously create the ActionInstance and return response metadata

        :param create_conversation_action_request: The action to perform.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            create_conversation_action_request=create_conversation_action_request
        )
        instance = ActionInstance(
            self._version, payload, conversation_id=self._solution["conversation_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def get(self, action_id: str) -> ActionContext:
        """
        Constructs a ActionContext

        :param action_id:
        """
        return ActionContext(
            self._version,
            conversation_id=self._solution["conversation_id"],
            action_id=action_id,
        )

    def __call__(self, action_id: str) -> ActionContext:
        """
        Constructs a ActionContext

        :param action_id:
        """
        return ActionContext(
            self._version,
            conversation_id=self._solution["conversation_id"],
            action_id=action_id,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Conversations.V2.ActionList>"
