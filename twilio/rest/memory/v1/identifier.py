r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from typing import Any, Dict, List, Optional, Union, Protocol
from twilio.base import serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class ResponseResource(Protocol):
    pass


"""
Nested response model for Identifier
"""


class Identifier:
    def __init__(self, idType: str, value: str):
        self.idType = idType
        self.value = value


"""
Nested response model for IdentifierUpdate
"""


class IdentifierUpdate:
    def __init__(self, idType: str, oldValue: str, newValue: str):
        self.idType = idType
        self.oldValue = oldValue
        self.newValue = newValue


"""
Response model for CreateProfileIdentifier_202_response operations
"""


class CreateProfileIdentifier_202_responseResource:
    def __init__(self, message: str):
        """
        Initialize the CreateProfileIdentifier_202_responseResource
        :param message:

        """
        self.message = message


"""
Response model for UpdateProfileIdentifier_202_response operations
"""


class UpdateProfileIdentifier_202_responseResource:
    def __init__(self, message: str):
        """
        Initialize the UpdateProfileIdentifier_202_responseResource
        :param message:

        """
        self.message = message


"""
Response model for DeleteProfileIdentifier_202_response operations
"""


class DeleteProfileIdentifier_202_responseResource:
    def __init__(self, message: str):
        """
        Initialize the DeleteProfileIdentifier_202_responseResource
        :param message:

        """
        self.message = message


"""
Response model for IdentifierSet operations
"""


class IdentifierSetResource:
    def __init__(self, idType: str, values: List[str]):
        """
        Initialize the IdentifierSetResource
        :param idType: Identifier type defined in Identity Resolution Settings.
        :param values: Server managed collection of stored values for the identifier type.  Identifier values are normalized according to the corresponding identifier settings and ordered chronologically.

        """
        self.idType = idType
        self.values = values


class IdentifierInstance(InstanceResource):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    class IdentifierUpdate(object):
        """
        :ivar id_type: The identifier type to update (e.g., email, phone).
        :ivar old_value: Existing stored value to replace.
        :ivar new_value: New value to store for the identifier. Normalization rules from the corresponding identifier settings apply.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.old_value: Optional[str] = payload.get("old_value")
            self.new_value: Optional[str] = payload.get("new_value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "oldValue": self.old_value,
                "newValue": self.new_value,
            }

    """
    :ivar message: 
    """

    def __init__(
        self,
        version: Version,
        payload: ResponseResource,
        store_id: Optional[str] = None,
        profile_id: Optional[str] = None,
        id_type: Optional[str] = None,
    ):
        super().__init__(version)

        self.message: Optional[str] = payload.get("message")

        self._solution = {
            "store_id": store_id or self.store_id,
            "profile_id": profile_id or self.profile_id,
            "id_type": id_type or self.id_type,
        }
        self._context: Optional[IdentifierContext] = None

    @property
    def _proxy(self) -> "IdentifierContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: IdentifierContext for this IdentifierInstance
        """
        if self._context is None:
            self._context = IdentifierContext(
                self._version,
                store_id=self._solution["store_id"],
                profile_id=self._solution["profile_id"],
                id_type=self._solution["id_type"],
            )
        return self._context

    def create(self, identifier: Identifier) -> "IdentifierInstance":
        """
        Create the IdentifierInstance

        :param identifier:

        :returns: The created IdentifierInstance
        """
        return self._proxy.create(
            identifier,
        )

    async def create_async(self, identifier: Identifier) -> "IdentifierInstance":
        """
        Asynchronous coroutine to create the IdentifierInstance

        :param identifier:

        :returns: The created IdentifierInstance
        """
        return await self._proxy.create_async(
            identifier,
        )

    def create_with_http_info(self, identifier: Identifier) -> ApiResponse:
        """
        Create the IdentifierInstance with HTTP info

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.create_with_http_info(
            identifier,
        )

    async def create_with_http_info_async(self, identifier: Identifier) -> ApiResponse:
        """
        Asynchronous coroutine to create the IdentifierInstance with HTTP info

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.create_with_http_info_async(
            identifier,
        )

    def delete(self, remove_all: Union[bool, object] = values.unset) -> bool:
        """
        Deletes the IdentifierInstance

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete(
            remove_all=remove_all,
        )

    async def delete_async(
        self, remove_all: Union[bool, object] = values.unset
    ) -> bool:
        """
        Asynchronous coroutine that deletes the IdentifierInstance

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async(
            remove_all=remove_all,
        )

    def delete_with_http_info(
        self, remove_all: Union[bool, object] = values.unset
    ) -> ApiResponse:
        """
        Deletes the IdentifierInstance with HTTP info

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info(
            remove_all=remove_all,
        )

    async def delete_with_http_info_async(
        self, remove_all: Union[bool, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the IdentifierInstance with HTTP info

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async(
            remove_all=remove_all,
        )

    def fetch(self) -> "IdentifierInstance":
        """
        Fetch the IdentifierInstance


        :returns: The fetched IdentifierInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "IdentifierInstance":
        """
        Asynchronous coroutine to fetch the IdentifierInstance


        :returns: The fetched IdentifierInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the IdentifierInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the IdentifierInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(self, identifier_update: IdentifierUpdate) -> "IdentifierInstance":
        """
        Update the IdentifierInstance

        :param identifier_update:

        :returns: The updated IdentifierInstance
        """
        return self._proxy.update(
            identifier_update=identifier_update,
        )

    async def update_async(
        self, identifier_update: IdentifierUpdate
    ) -> "IdentifierInstance":
        """
        Asynchronous coroutine to update the IdentifierInstance

        :param identifier_update:

        :returns: The updated IdentifierInstance
        """
        return await self._proxy.update_async(
            identifier_update=identifier_update,
        )

    def update_with_http_info(self, identifier_update: IdentifierUpdate) -> ApiResponse:
        """
        Update the IdentifierInstance with HTTP info

        :param identifier_update:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            identifier_update=identifier_update,
        )

    async def update_with_http_info_async(
        self, identifier_update: IdentifierUpdate
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the IdentifierInstance with HTTP info

        :param identifier_update:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            identifier_update=identifier_update,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.IdentifierInstance {}>".format(context)


class IdentifierContext(InstanceContext):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    class IdentifierUpdate(object):
        """
        :ivar id_type: The identifier type to update (e.g., email, phone).
        :ivar old_value: Existing stored value to replace.
        :ivar new_value: New value to store for the identifier. Normalization rules from the corresponding identifier settings apply.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.old_value: Optional[str] = payload.get("old_value")
            self.new_value: Optional[str] = payload.get("new_value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "oldValue": self.old_value,
                "newValue": self.new_value,
            }

    def __init__(self, version: Version, store_id: str, profile_id: str, id_type: str):
        """
        Initialize the IdentifierContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param id_type: Identifier type configured for the service (for example `email`, `phone`, or `externalId`).
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "profile_id": profile_id,
            "id_type": id_type,
        }
        self._uri = (
            "/Stores/{store_id}/Profiles/{profile_id}/Identifiers/{id_type}".format(
                **self._solution
            )
        )

    def _create(self, identifier: Identifier) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, identifier: Identifier) -> IdentifierInstance:
        """
        Create the IdentifierInstance

        :param identifier:

        :returns: The created IdentifierInstance
        """
        payload, _, _ = self._create(identifier=identifier)
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    def create_with_http_info(self, identifier: Identifier) -> ApiResponse:
        """
        Create the IdentifierInstance and return response metadata

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(identifier=identifier)
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, identifier: Identifier) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(self, identifier: Identifier) -> IdentifierInstance:
        """
        Asynchronous coroutine to create the IdentifierInstance

        :param identifier:

        :returns: The created IdentifierInstance
        """
        payload, _, _ = await self._create_async(identifier=identifier)
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    async def create_with_http_info_async(self, identifier: Identifier) -> ApiResponse:
        """
        Asynchronous coroutine to create the IdentifierInstance and return response metadata

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(identifier=identifier)
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _delete(self, remove_all: Union[bool, object] = values.unset) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        params = values.of(
            {
                "removeAll": serialize.boolean_to_string(remove_all),
            }
        )

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers, params=params
        )

    def delete(self, remove_all: Union[bool, object] = values.unset) -> bool:
        """
        Deletes the IdentifierInstance

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete(remove_all=remove_all)
        return success

    def delete_with_http_info(
        self, remove_all: Union[bool, object] = values.unset
    ) -> ApiResponse:
        """
        Deletes the IdentifierInstance and return response metadata

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete(remove_all=remove_all)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(
        self, remove_all: Union[bool, object] = values.unset
    ) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        params = values.of(
            {
                "removeAll": serialize.boolean_to_string(remove_all),
            }
        )

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers, params=params
        )

    async def delete_async(
        self, remove_all: Union[bool, object] = values.unset
    ) -> bool:
        """
        Asynchronous coroutine that deletes the IdentifierInstance

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async(remove_all=remove_all)
        return success

    async def delete_with_http_info_async(
        self, remove_all: Union[bool, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the IdentifierInstance and return response metadata

        :param remove_all: When true, removes every stored value for the identifier type in a single operation. Defaults to false.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async(remove_all=remove_all)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> IdentifierInstance:
        """
        Fetch the IdentifierInstance


        :returns: The fetched IdentifierInstance
        """
        payload, _, _ = self._fetch()
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the IdentifierInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> IdentifierInstance:
        """
        Asynchronous coroutine to fetch the IdentifierInstance


        :returns: The fetched IdentifierInstance
        """
        payload, _, _ = await self._fetch_async()
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the IdentifierInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(self, identifier_update: IdentifierUpdate) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier_update.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    def update(self, identifier_update: IdentifierUpdate) -> IdentifierInstance:
        """
        Update the IdentifierInstance

        :param identifier_update:

        :returns: The updated IdentifierInstance
        """
        payload, _, _ = self._update(identifier_update=identifier_update)
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    def update_with_http_info(self, identifier_update: IdentifierUpdate) -> ApiResponse:
        """
        Update the IdentifierInstance and return response metadata

        :param identifier_update:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            identifier_update=identifier_update
        )
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(self, identifier_update: IdentifierUpdate) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier_update.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self, identifier_update: IdentifierUpdate
    ) -> IdentifierInstance:
        """
        Asynchronous coroutine to update the IdentifierInstance

        :param identifier_update:

        :returns: The updated IdentifierInstance
        """
        payload, _, _ = await self._update_async(identifier_update=identifier_update)
        return IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )

    async def update_with_http_info_async(
        self, identifier_update: IdentifierUpdate
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the IdentifierInstance and return response metadata

        :param identifier_update:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            identifier_update=identifier_update
        )
        instance = IdentifierInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            id_type=self._solution["id_type"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.IdentifierContext {}>".format(context)


class IdentifierPage(TokenPagination):
    def get_instance(self, payload: Dict[str, Any]) -> IdentifierInstance:
        """
        Build an instance of IdentifierInstance

        :param payload: Payload response from the API
        """
        return IdentifierInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.IdentifierPage>"


class IdentifierList(ListResource):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    class IdentifierUpdate(object):
        """
        :ivar id_type: The identifier type to update (e.g., email, phone).
        :ivar old_value: Existing stored value to replace.
        :ivar new_value: New value to store for the identifier. Normalization rules from the corresponding identifier settings apply.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.old_value: Optional[str] = payload.get("old_value")
            self.new_value: Optional[str] = payload.get("new_value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "oldValue": self.old_value,
                "newValue": self.new_value,
            }

    def __init__(self, version: Version):
        """
        Initialize the IdentifierList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str, profile_id: str, id_type: str) -> IdentifierContext:
        """
        Constructs a IdentifierContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param id_type: Identifier type configured for the service (for example `email`, `phone`, or `externalId`).
        """
        return IdentifierContext(
            self._version, store_id=store_id, profile_id=profile_id, id_type=id_type
        )

    def __call__(
        self, store_id: str, profile_id: str, id_type: str
    ) -> IdentifierContext:
        """
        Constructs a IdentifierContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param id_type: Identifier type configured for the service (for example `email`, `phone`, or `externalId`).
        """
        return IdentifierContext(
            self._version, store_id=store_id, profile_id=profile_id, id_type=id_type
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.IdentifierList>"
