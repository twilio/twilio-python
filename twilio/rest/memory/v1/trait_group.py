r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from typing import Any, Dict, Optional, Union, Protocol
from twilio.base import serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class ResponseResource(Protocol):
    pass


"""
Nested response model for Meta
"""


class Meta:
    def __init__(self, key: str, pageSize: int, nextToken: str, previousToken: str):
        self.key = key
        self.pageSize = pageSize
        self.nextToken = nextToken
        self.previousToken = previousToken


"""
Nested response model for TraitDefinition
"""


class TraitDefinition:
    def __init__(
        self, displayName: str, dataType: str, description: str, idTypePromotion: str
    ):
        self.displayName = displayName
        self.dataType = dataType
        self.description = description
        self.idTypePromotion = idTypePromotion


"""
Nested response model for TraitGroup
"""


class TraitGroup:
    def __init__(
        self,
        displayName: str,
        description: str,
        traits: Dict[str, TraitGroupCoreTraitsValue],
        version: int,
    ):
        self.displayName = displayName
        self.description = description
        self.traits = traits
        self.version = version


"""
Nested response model for TraitGroupCoreTraitsValue
"""


class TraitGroupCoreTraitsValue:
    def __init__(
        self,
        displayName: str,
        dataType: str,
        description: str,
        validationRule: ValidationRule,
        idTypePromotion: str,
    ):
        self.displayName = displayName
        self.dataType = dataType
        self.description = description
        self.validationRule = validationRule
        self.idTypePromotion = idTypePromotion


"""
Nested response model for TraitGroupRequest
"""


class TraitGroupRequest:
    def __init__(
        self,
        displayName: str,
        description: str,
        traits: Dict[str, TraitGroupCoreTraitsValue],
    ):
        self.displayName = displayName
        self.description = description
        self.traits = traits


"""
Nested response model for UpdateTraitGroupRequest
"""


class UpdateTraitGroupRequest:
    def __init__(self, description: str, traits: Dict[str, TraitDefinition]):
        self.description = description
        self.traits = traits


"""
Nested response model for ValidationRule
"""


class ValidationRule:
    def __init__(
        self,
        pattern: str,
        minLength: int,
        maxLength: int,
        ruleType: str,
        min: float,
        max: float,
        minItems: int,
        maxItems: int,
    ):
        self.pattern = pattern
        self.minLength = minLength
        self.maxLength = maxLength
        self.ruleType = ruleType
        self.min = min
        self.max = max
        self.minItems = minItems
        self.maxItems = maxItems


"""
Response model for DeleteTraitGroup_202_response operations
"""


class DeleteTraitGroup_202_responseResource:
    def __init__(self, message: str):
        """
        Initialize the DeleteTraitGroup_202_responseResource
        :param message:

        """
        self.message = message


"""
Response model for CreateTraitGroup_201_response operations
"""


class CreateTraitGroup_201_responseResource:
    def __init__(self, traitGroup: str, meta: str):
        """
        Initialize the CreateTraitGroup_201_responseResource
        :param traitGroup:
        :param meta:

        """
        self.traitGroup = traitGroup
        self.meta = meta


"""
Response model for TraitGroup operations
"""


class TraitGroupResource:
    def __init__(
        self,
        displayName: str,
        description: str,
        traits: Dict[str, TraitGroupCoreTraitsValue],
        version: int,
    ):
        """
        Initialize the TraitGroupResource
        :param displayName: Provides a unique and addressable name to be assigned to this Trait Group
        :param description: description of the Trait Group
        :param traits: Map of traits that are part of this Trait Group, where the key is the trait name and the value is the trait's definition.
        :param version: The current version number of the Trait Group. Incremented on each successful update.

        """
        self.displayName = displayName
        self.description = description
        self.traits = traits
        self.version = version


class TraitGroupInstance(InstanceResource):
    class TraitDefinition(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupCoreTraitsValue(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar validation_rule:
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.validation_rule: Optional[ValidationRule] = payload.get(
                "validation_rule"
            )
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "validationRule": self.validation_rule.to_dict()
                if self.validation_rule is not None
                else None,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupRequest(object):
        """
        :ivar display_name: Unique name of the Trait Group
        :ivar description: description of the Trait Group
        :ivar traits: Map of traits belonging to this Trait Group, keyed by trait name.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitGroupCoreTraitsValue]] = payload.get(
                "traits"
            )

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class UpdateTraitGroupRequest(object):
        """
        :ivar description: Updated description of the Trait Group
        :ivar traits: Map of traits to add, update, or remove in this Trait Group, where the key is the trait name. - To update/add a trait: provide the complete TraitDefinition object - To remove a trait: set dataType to an empty string (\"\")
        """

        def __init__(self, payload: Dict[str, Any]):
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitDefinition]] = payload.get("traits")

        def to_dict(self):
            return {
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class ValidationRule(object):
        """
        :ivar pattern: Regex pattern (max 1000 chars)
        :ivar min_length: Minimum string length
        :ivar max_length: Maximum string length
        :ivar rule_type: Discriminator field indicating this is an array validation rule.
        :ivar min: Minimum value
        :ivar max: Maximum value
        :ivar min_items: Minimum number of items
        :ivar max_items: Maximum number of items
        """

        def __init__(self, payload: Dict[str, Any]):
            self.pattern: Optional[str] = payload.get("pattern")
            self.min_length: Optional[int] = payload.get("min_length")
            self.max_length: Optional[int] = payload.get("max_length")
            self.rule_type: Optional[str] = payload.get("rule_type")
            self.min: Optional[float] = payload.get("min")
            self.max: Optional[float] = payload.get("max")
            self.min_items: Optional[int] = payload.get("min_items")
            self.max_items: Optional[int] = payload.get("max_items")

        def to_dict(self):
            return {
                "pattern": self.pattern,
                "minLength": self.min_length,
                "maxLength": self.max_length,
                "ruleType": self.rule_type,
                "min": self.min,
                "max": self.max,
                "minItems": self.min_items,
                "maxItems": self.max_items,
            }

    """
    :ivar trait_group: 
    :ivar meta: 
    """

    def __init__(
        self,
        version: Version,
        payload: ResponseResource,
        store_id: Optional[str] = None,
        trait_group_name: Optional[str] = None,
    ):
        super().__init__(version)

        self.trait_group: Optional[str] = payload.get("traitGroup")
        self.meta: Optional[str] = payload.get("meta")

        self._solution = {
            "store_id": store_id or self.store_id,
            "trait_group_name": trait_group_name or self.trait_group_name,
        }
        self._context: Optional[TraitGroupContext] = None

    @property
    def _proxy(self) -> "TraitGroupContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: TraitGroupContext for this TraitGroupInstance
        """
        if self._context is None:
            self._context = TraitGroupContext(
                self._version,
                store_id=self._solution["store_id"],
                trait_group_name=self._solution["trait_group_name"],
            )
        return self._context

    def create(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Create the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: The created TraitGroupInstance
        """
        return self._proxy.create(
            prefer=prefer,
            trait_group_request=trait_group_request,
        )

    async def create_async(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Asynchronous coroutine to create the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: The created TraitGroupInstance
        """
        return await self._proxy.create_async(
            prefer=prefer,
            trait_group_request=trait_group_request,
        )

    def create_with_http_info(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Create the TraitGroupInstance with HTTP info

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.create_with_http_info(
            prefer=prefer,
            trait_group_request=trait_group_request,
        )

    async def create_with_http_info_async(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to create the TraitGroupInstance with HTTP info

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.create_with_http_info_async(
            prefer=prefer,
            trait_group_request=trait_group_request,
        )

    def delete(self, prefer: Union[str, object] = values.unset) -> bool:
        """
        Deletes the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete(
            prefer=prefer,
        )

    async def delete_async(self, prefer: Union[str, object] = values.unset) -> bool:
        """
        Asynchronous coroutine that deletes the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async(
            prefer=prefer,
        )

    def delete_with_http_info(
        self, prefer: Union[str, object] = values.unset
    ) -> ApiResponse:
        """
        Deletes the TraitGroupInstance with HTTP info

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info(
            prefer=prefer,
        )

    async def delete_with_http_info_async(
        self, prefer: Union[str, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the TraitGroupInstance with HTTP info

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async(
            prefer=prefer,
        )

    def fetch(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Fetch the TraitGroupInstance

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: The fetched TraitGroupInstance
        """
        return self._proxy.fetch(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )

    async def fetch_async(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Asynchronous coroutine to fetch the TraitGroupInstance

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: The fetched TraitGroupInstance
        """
        return await self._proxy.fetch_async(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )

    def fetch_with_http_info(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Fetch the TraitGroupInstance with HTTP info

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )

    async def fetch_with_http_info_async(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the TraitGroupInstance with HTTP info

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )

    def update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Update the TraitGroupInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: The updated TraitGroupInstance
        """
        return self._proxy.update(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )

    async def update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> "TraitGroupInstance":
        """
        Asynchronous coroutine to update the TraitGroupInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: The updated TraitGroupInstance
        """
        return await self._proxy.update_async(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )

    def update_with_http_info(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Update the TraitGroupInstance with HTTP info

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )

    async def update_with_http_info_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TraitGroupInstance with HTTP info

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.TraitGroupInstance {}>".format(context)


class TraitGroupContext(InstanceContext):
    class TraitDefinition(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupCoreTraitsValue(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar validation_rule:
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.validation_rule: Optional[ValidationRule] = payload.get(
                "validation_rule"
            )
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "validationRule": self.validation_rule.to_dict()
                if self.validation_rule is not None
                else None,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupRequest(object):
        """
        :ivar display_name: Unique name of the Trait Group
        :ivar description: description of the Trait Group
        :ivar traits: Map of traits belonging to this Trait Group, keyed by trait name.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitGroupCoreTraitsValue]] = payload.get(
                "traits"
            )

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class UpdateTraitGroupRequest(object):
        """
        :ivar description: Updated description of the Trait Group
        :ivar traits: Map of traits to add, update, or remove in this Trait Group, where the key is the trait name. - To update/add a trait: provide the complete TraitDefinition object - To remove a trait: set dataType to an empty string (\"\")
        """

        def __init__(self, payload: Dict[str, Any]):
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitDefinition]] = payload.get("traits")

        def to_dict(self):
            return {
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class ValidationRule(object):
        """
        :ivar pattern: Regex pattern (max 1000 chars)
        :ivar min_length: Minimum string length
        :ivar max_length: Maximum string length
        :ivar rule_type: Discriminator field indicating this is an array validation rule.
        :ivar min: Minimum value
        :ivar max: Maximum value
        :ivar min_items: Minimum number of items
        :ivar max_items: Maximum number of items
        """

        def __init__(self, payload: Dict[str, Any]):
            self.pattern: Optional[str] = payload.get("pattern")
            self.min_length: Optional[int] = payload.get("min_length")
            self.max_length: Optional[int] = payload.get("max_length")
            self.rule_type: Optional[str] = payload.get("rule_type")
            self.min: Optional[float] = payload.get("min")
            self.max: Optional[float] = payload.get("max")
            self.min_items: Optional[int] = payload.get("min_items")
            self.max_items: Optional[int] = payload.get("max_items")

        def to_dict(self):
            return {
                "pattern": self.pattern,
                "minLength": self.min_length,
                "maxLength": self.max_length,
                "ruleType": self.rule_type,
                "min": self.min,
                "max": self.max,
                "minItems": self.min_items,
                "maxItems": self.max_items,
            }

    def __init__(self, version: Version, store_id: str, trait_group_name: str):
        """
        Initialize the TraitGroupContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param trait_group_name: A unique Name of the TraitGroup
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "trait_group_name": trait_group_name,
        }
        self._uri = (
            "/ControlPlane/Stores/{store_id}/TraitGroups/{trait_group_name}".format(
                **self._solution
            )
        )

    def _create(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = trait_group_request.to_dict()

        headers = values.of({})

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> TraitGroupInstance:
        """
        Create the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: The created TraitGroupInstance
        """
        payload, _, _ = self._create(
            prefer=prefer, trait_group_request=trait_group_request
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    def create_with_http_info(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Create the TraitGroupInstance and return response metadata

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            prefer=prefer, trait_group_request=trait_group_request
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = trait_group_request.to_dict()

        headers = values.of({})

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> TraitGroupInstance:
        """
        Asynchronous coroutine to create the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: The created TraitGroupInstance
        """
        payload, _, _ = await self._create_async(
            prefer=prefer, trait_group_request=trait_group_request
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    async def create_with_http_info_async(
        self,
        prefer: Union[str, object] = values.unset,
        trait_group_request: Union[TraitGroupRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to create the TraitGroupInstance and return response metadata

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            prefer=prefer, trait_group_request=trait_group_request
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _delete(self, prefer: Union[str, object] = values.unset) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of(
            {
                "Prefer": prefer,
            }
        )

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self, prefer: Union[str, object] = values.unset) -> bool:
        """
        Deletes the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete(prefer=prefer)
        return success

    def delete_with_http_info(
        self, prefer: Union[str, object] = values.unset
    ) -> ApiResponse:
        """
        Deletes the TraitGroupInstance and return response metadata

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete(prefer=prefer)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self, prefer: Union[str, object] = values.unset) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """
        headers = values.of(
            {
                "Prefer": prefer,
            }
        )

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self, prefer: Union[str, object] = values.unset) -> bool:
        """
        Asynchronous coroutine that deletes the TraitGroupInstance

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async(prefer=prefer)
        return success

    async def delete_with_http_info_async(
        self, prefer: Union[str, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the TraitGroupInstance and return response metadata

        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.

        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async(prefer=prefer)
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        params = values.of(
            {
                "includeTraits": serialize.boolean_to_string(include_traits),
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, params=params, headers=headers
        )

    def fetch(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> TraitGroupInstance:
        """
        Fetch the TraitGroupInstance

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: The fetched TraitGroupInstance
        """
        payload, _, _ = self._fetch(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    def fetch_with_http_info(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Fetch the TraitGroupInstance and return response metadata

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        params = values.of(
            {
                "includeTraits": serialize.boolean_to_string(include_traits),
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, params=params, headers=headers
        )

    async def fetch_async(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> TraitGroupInstance:
        """
        Asynchronous coroutine to fetch the TraitGroupInstance

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: The fetched TraitGroupInstance
        """
        payload, _, _ = await self._fetch_async(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    async def fetch_with_http_info_async(
        self,
        include_traits: Union[bool, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the TraitGroupInstance and return response metadata

        :param include_traits: Whether to include trait definitions in the response
        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async(
            include_traits=include_traits,
            page_size=page_size,
            page_token=page_token,
            order_by=order_by,
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_trait_group_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> TraitGroupInstance:
        """
        Update the TraitGroupInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: The updated TraitGroupInstance
        """
        payload, _, _ = self._update(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    def update_with_http_info(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Update the TraitGroupInstance and return response metadata

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_trait_group_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> TraitGroupInstance:
        """
        Asynchronous coroutine to update the TraitGroupInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: The updated TraitGroupInstance
        """
        payload, _, _ = await self._update_async(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )
        return TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )

    async def update_with_http_info_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_trait_group_request: Union[
            UpdateTraitGroupRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TraitGroupInstance and return response metadata

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_trait_group_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            if_match=if_match,
            prefer=prefer,
            update_trait_group_request=update_trait_group_request,
        )
        instance = TraitGroupInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            trait_group_name=self._solution["trait_group_name"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.TraitGroupContext {}>".format(context)


class TraitGroupPage(TokenPagination):
    def get_instance(self, payload: Dict[str, Any]) -> TraitGroupInstance:
        """
        Build an instance of TraitGroupInstance

        :param payload: Payload response from the API
        """
        return TraitGroupInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.TraitGroupPage>"


class TraitGroupList(ListResource):
    class TraitDefinition(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupCoreTraitsValue(object):
        """
        :ivar display_name: Display name of the Trait, which is unique within the Trait Group. Trait names are normalized to lowerCamelCase naming conventions and can include letters, numbers, underscores, hyphens, and periods. They must start with a letter.
        :ivar data_type: Data type of the Trait, such as STRING, NUMBER, BOOLEAN, ARRAY. For DELETE operations in PATCH requests, set this to an empty string (\"\") to mark the trait for deletion.
        :ivar description: Description of the Trait, providing additional context or information.
        :ivar validation_rule:
        :ivar id_type_promotion: The name of the identifier type to promote the trait value to, such as ''email'', ''phone'', ''user_id'', etc. This allows the trait to be mapped to an identifier in Identity Resolution. The identifier type should be configured in the Identity Resolution Settings.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.data_type: Optional[str] = payload.get("data_type")
            self.description: Optional[str] = payload.get("description")
            self.validation_rule: Optional[ValidationRule] = payload.get(
                "validation_rule"
            )
            self.id_type_promotion: Optional[str] = payload.get("id_type_promotion")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "dataType": self.data_type,
                "description": self.description,
                "validationRule": self.validation_rule.to_dict()
                if self.validation_rule is not None
                else None,
                "idTypePromotion": self.id_type_promotion,
            }

    class TraitGroupRequest(object):
        """
        :ivar display_name: Unique name of the Trait Group
        :ivar description: description of the Trait Group
        :ivar traits: Map of traits belonging to this Trait Group, keyed by trait name.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitGroupCoreTraitsValue]] = payload.get(
                "traits"
            )

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class UpdateTraitGroupRequest(object):
        """
        :ivar description: Updated description of the Trait Group
        :ivar traits: Map of traits to add, update, or remove in this Trait Group, where the key is the trait name. - To update/add a trait: provide the complete TraitDefinition object - To remove a trait: set dataType to an empty string (\"\")
        """

        def __init__(self, payload: Dict[str, Any]):
            self.description: Optional[str] = payload.get("description")
            self.traits: Optional[Dict[str, TraitDefinition]] = payload.get("traits")

        def to_dict(self):
            return {
                "description": self.description,
                "traits": [traits.to_dict() for traits in self.traits]
                if self.traits is not None
                else None,
            }

    class ValidationRule(object):
        """
        :ivar pattern: Regex pattern (max 1000 chars)
        :ivar min_length: Minimum string length
        :ivar max_length: Maximum string length
        :ivar rule_type: Discriminator field indicating this is an array validation rule.
        :ivar min: Minimum value
        :ivar max: Maximum value
        :ivar min_items: Minimum number of items
        :ivar max_items: Maximum number of items
        """

        def __init__(self, payload: Dict[str, Any]):
            self.pattern: Optional[str] = payload.get("pattern")
            self.min_length: Optional[int] = payload.get("min_length")
            self.max_length: Optional[int] = payload.get("max_length")
            self.rule_type: Optional[str] = payload.get("rule_type")
            self.min: Optional[float] = payload.get("min")
            self.max: Optional[float] = payload.get("max")
            self.min_items: Optional[int] = payload.get("min_items")
            self.max_items: Optional[int] = payload.get("max_items")

        def to_dict(self):
            return {
                "pattern": self.pattern,
                "minLength": self.min_length,
                "maxLength": self.max_length,
                "ruleType": self.rule_type,
                "min": self.min,
                "max": self.max,
                "minItems": self.min_items,
                "maxItems": self.max_items,
            }

    def __init__(self, version: Version):
        """
        Initialize the TraitGroupList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str, trait_group_name: str) -> TraitGroupContext:
        """
        Constructs a TraitGroupContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param trait_group_name: A unique Name of the TraitGroup
        """
        return TraitGroupContext(
            self._version, store_id=store_id, trait_group_name=trait_group_name
        )

    def __call__(self, store_id: str, trait_group_name: str) -> TraitGroupContext:
        """
        Constructs a TraitGroupContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param trait_group_name: A unique Name of the TraitGroup
        """
        return TraitGroupContext(
            self._version, store_id=store_id, trait_group_name=trait_group_name
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.TraitGroupList>"
