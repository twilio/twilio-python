r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union
from twilio.base import values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class RecallInstance(InstanceResource):
    class CommunicationContent(object):
        """
        :ivar text: Primary text content (optional).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")

        def to_dict(self):
            return {
                "": self.text,
            }

    class MemoryRetrievalRequest(object):
        """
        :ivar conversation_id: A unique identifier for the conversation using Twilio Type ID (TTID) format.
        :ivar query: Hybrid search query for finding relevant memories. Omit to use query expansion to generate query from previous 10 communications in conversation.
        :ivar begin_date: Start date for filtering memories (inclusive).
        :ivar end_date: End date for filtering memories (exclusive).
        :ivar communications_limit: Maximum number of conversational session memories to return. If omitted or set to 0, no session memories will be fetched.
        :ivar observations_limit: Maximum number of observation memories to return. If omitted, defaults to 20. If set to 0, no observation memories will be fetched.
        :ivar summaries_limit: Maximum number of summary memories to return. If omitted, defaults to 5. If set to 0, no summary memories will be fetched.
        :ivar relevance_threshold: Minimum relevance score threshold for observations and summaries to be returned. Only memories with a relevance score greater than or equal to this threshold will be included in the response.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.query: Optional[str] = payload.get("query")
            self.begin_date: Optional[datetime] = payload.get("begin_date")
            self.end_date: Optional[datetime] = payload.get("end_date")
            self.communications_limit: Optional[int] = payload.get(
                "communications_limit"
            )
            self.observations_limit: Optional[int] = payload.get("observations_limit")
            self.summaries_limit: Optional[int] = payload.get("summaries_limit")
            self.relevance_threshold: Optional[float] = payload.get(
                "relevance_threshold"
            )

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "query": self.query,
                "begin_date": self.begin_date,
                "end_date": self.end_date,
                "communications_limit": self.communications_limit,
                "observations_limit": self.observations_limit,
                "summaries_limit": self.summaries_limit,
                "relevance_threshold": self.relevance_threshold,
            }

    class Participant(object):
        """
        :ivar id: Participant identifier.
        :ivar name: Participant display name
        :ivar type:
        :ivar profile_id: The canonical profile ID.
        :ivar address: Address of the Participant (e.g., phone number, email address)
        :ivar channel: The channel on which the message originated
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.type: Optional[ParticipantType] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profile_id")
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional[str] = payload.get("channel")

        def to_dict(self):
            return {
                "": self.id,
                "": self.name,
                "": self.type.to_dict() if self.type is not None else None,
                "": self.profile_id,
                "": self.address,
                "": self.channel,
            }

    class ParticipantType(object):
        HUMAN_AGENT = "HUMAN_AGENT"
        CUSTOMER = "CUSTOMER"
        AI_AGENT = "AI_AGENT"

    """
    :ivar observations: Array of observation memories.
    :ivar summaries: Array of summary memories derived from observations at the end of conversations.
    :ivar communications: Array of recent communication context.
    :ivar meta: 
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        store_id: Optional[str] = None,
        profile_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.observations: Optional[List[Dict[str, object]]] = payload.get(
            "observations"
        )
        self.summaries: Optional[List[Dict[str, object]]] = payload.get("summaries")
        self.communications: Optional[List[str]] = payload.get("communications")
        self.meta: Optional[str] = payload.get("meta")

        self._solution = {
            "store_id": store_id or self.store_id,
            "profile_id": profile_id or self.profile_id,
        }
        self._context: Optional[RecallContext] = None

    @property
    def _proxy(self) -> "RecallContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: RecallContext for this RecallInstance
        """
        if self._context is None:
            self._context = RecallContext(
                self._version,
                store_id=self._solution["store_id"],
                profile_id=self._solution["profile_id"],
            )
        return self._context

    def fetch(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> "RecallInstance":
        """
        Fetch the RecallInstance

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: The fetched RecallInstance
        """
        return self._proxy.fetch(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )

    async def fetch_async(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> "RecallInstance":
        """
        Asynchronous coroutine to fetch the RecallInstance

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: The fetched RecallInstance
        """
        return await self._proxy.fetch_async(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )

    def fetch_with_http_info(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Fetch the RecallInstance with HTTP info

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )

    async def fetch_with_http_info_async(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the RecallInstance with HTTP info

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.RecallInstance {}>".format(context)


class RecallContext(InstanceContext):
    class CommunicationContent(object):
        """
        :ivar text: Primary text content (optional).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")

        def to_dict(self):
            return {
                "": self.text,
            }

    class MemoryRetrievalRequest(object):
        """
        :ivar conversation_id: A unique identifier for the conversation using Twilio Type ID (TTID) format.
        :ivar query: Hybrid search query for finding relevant memories. Omit to use query expansion to generate query from previous 10 communications in conversation.
        :ivar begin_date: Start date for filtering memories (inclusive).
        :ivar end_date: End date for filtering memories (exclusive).
        :ivar communications_limit: Maximum number of conversational session memories to return. If omitted or set to 0, no session memories will be fetched.
        :ivar observations_limit: Maximum number of observation memories to return. If omitted, defaults to 20. If set to 0, no observation memories will be fetched.
        :ivar summaries_limit: Maximum number of summary memories to return. If omitted, defaults to 5. If set to 0, no summary memories will be fetched.
        :ivar relevance_threshold: Minimum relevance score threshold for observations and summaries to be returned. Only memories with a relevance score greater than or equal to this threshold will be included in the response.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.query: Optional[str] = payload.get("query")
            self.begin_date: Optional[datetime] = payload.get("begin_date")
            self.end_date: Optional[datetime] = payload.get("end_date")
            self.communications_limit: Optional[int] = payload.get(
                "communications_limit"
            )
            self.observations_limit: Optional[int] = payload.get("observations_limit")
            self.summaries_limit: Optional[int] = payload.get("summaries_limit")
            self.relevance_threshold: Optional[float] = payload.get(
                "relevance_threshold"
            )

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "query": self.query,
                "begin_date": self.begin_date,
                "end_date": self.end_date,
                "communications_limit": self.communications_limit,
                "observations_limit": self.observations_limit,
                "summaries_limit": self.summaries_limit,
                "relevance_threshold": self.relevance_threshold,
            }

    class Participant(object):
        """
        :ivar id: Participant identifier.
        :ivar name: Participant display name
        :ivar type:
        :ivar profile_id: The canonical profile ID.
        :ivar address: Address of the Participant (e.g., phone number, email address)
        :ivar channel: The channel on which the message originated
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.type: Optional[ParticipantType] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profile_id")
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional[str] = payload.get("channel")

        def to_dict(self):
            return {
                "": self.id,
                "": self.name,
                "": self.type.to_dict() if self.type is not None else None,
                "": self.profile_id,
                "": self.address,
                "": self.channel,
            }

    def __init__(self, version: Version, store_id: str, profile_id: str):
        """
        Initialize the RecallContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "profile_id": profile_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/{profile_id}/Recall".format(
            **self._solution
        )

    def _fetch(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        if not (
            accept_encoding is values.unset
            or (isinstance(accept_encoding, str) and not accept_encoding)
        ):
            headers["Accept-Encoding"] = accept_encoding

        if not (
            content_encoding is values.unset
            or (isinstance(content_encoding, str) and not content_encoding)
        ):
            headers["Content-Encoding"] = content_encoding

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="POST", uri=self._uri, headers=headers
        )

    def fetch(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> RecallInstance:
        """
        Fetch the RecallInstance

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: The fetched RecallInstance
        """
        payload, _, _ = self._fetch(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )
        return RecallInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )

    def fetch_with_http_info(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Fetch the RecallInstance and return response metadata

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )
        instance = RecallInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        if not (
            accept_encoding is values.unset
            or (isinstance(accept_encoding, str) and not accept_encoding)
        ):
            headers["Accept-Encoding"] = accept_encoding

        if not (
            content_encoding is values.unset
            or (isinstance(content_encoding, str) and not content_encoding)
        ):
            headers["Content-Encoding"] = content_encoding

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="POST", uri=self._uri, headers=headers
        )

    async def fetch_async(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> RecallInstance:
        """
        Asynchronous coroutine to fetch the RecallInstance

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: The fetched RecallInstance
        """
        payload, _, _ = await self._fetch_async(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )
        return RecallInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )

    async def fetch_with_http_info_async(
        self,
        memory_retrieval_request: MemoryRetrievalRequest,
        accept_encoding: Union[str, object] = values.unset,
        content_encoding: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the RecallInstance and return response metadata

        :param memory_retrieval_request:
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param content_encoding: Compression algorithm used for the request body (e.g., gzip, deflate, br)

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async(
            memory_retrieval_request=memory_retrieval_request,
            accept_encoding=accept_encoding,
            content_encoding=content_encoding,
        )
        instance = RecallInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.RecallContext {}>".format(context)


class RecallList(ListResource):
    class CommunicationContent(object):
        """
        :ivar text: Primary text content (optional).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.text: Optional[str] = payload.get("text")

        def to_dict(self):
            return {
                "": self.text,
            }

    class MemoryRetrievalRequest(object):
        """
        :ivar conversation_id: A unique identifier for the conversation using Twilio Type ID (TTID) format.
        :ivar query: Hybrid search query for finding relevant memories. Omit to use query expansion to generate query from previous 10 communications in conversation.
        :ivar begin_date: Start date for filtering memories (inclusive).
        :ivar end_date: End date for filtering memories (exclusive).
        :ivar communications_limit: Maximum number of conversational session memories to return. If omitted or set to 0, no session memories will be fetched.
        :ivar observations_limit: Maximum number of observation memories to return. If omitted, defaults to 20. If set to 0, no observation memories will be fetched.
        :ivar summaries_limit: Maximum number of summary memories to return. If omitted, defaults to 5. If set to 0, no summary memories will be fetched.
        :ivar relevance_threshold: Minimum relevance score threshold for observations and summaries to be returned. Only memories with a relevance score greater than or equal to this threshold will be included in the response.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.query: Optional[str] = payload.get("query")
            self.begin_date: Optional[datetime] = payload.get("begin_date")
            self.end_date: Optional[datetime] = payload.get("end_date")
            self.communications_limit: Optional[int] = payload.get(
                "communications_limit"
            )
            self.observations_limit: Optional[int] = payload.get("observations_limit")
            self.summaries_limit: Optional[int] = payload.get("summaries_limit")
            self.relevance_threshold: Optional[float] = payload.get(
                "relevance_threshold"
            )

        def to_dict(self):
            return {
                "conversation_id": self.conversation_id,
                "query": self.query,
                "begin_date": self.begin_date,
                "end_date": self.end_date,
                "communications_limit": self.communications_limit,
                "observations_limit": self.observations_limit,
                "summaries_limit": self.summaries_limit,
                "relevance_threshold": self.relevance_threshold,
            }

    class Participant(object):
        """
        :ivar id: Participant identifier.
        :ivar name: Participant display name
        :ivar type:
        :ivar profile_id: The canonical profile ID.
        :ivar address: Address of the Participant (e.g., phone number, email address)
        :ivar channel: The channel on which the message originated
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id: Optional[str] = payload.get("id")
            self.name: Optional[str] = payload.get("name")
            self.type: Optional[ParticipantType] = payload.get("type")
            self.profile_id: Optional[str] = payload.get("profile_id")
            self.address: Optional[str] = payload.get("address")
            self.channel: Optional[str] = payload.get("channel")

        def to_dict(self):
            return {
                "": self.id,
                "": self.name,
                "": self.type.to_dict() if self.type is not None else None,
                "": self.profile_id,
                "": self.address,
                "": self.channel,
            }

    def __init__(self, version: Version):
        """
        Initialize the RecallList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str, profile_id: str) -> RecallContext:
        """
        Constructs a RecallContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        """
        return RecallContext(self._version, store_id=store_id, profile_id=profile_id)

    def __call__(self, store_id: str, profile_id: str) -> RecallContext:
        """
        Constructs a RecallContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        """
        return RecallContext(self._version, store_id=store_id, profile_id=profile_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.RecallList>"
