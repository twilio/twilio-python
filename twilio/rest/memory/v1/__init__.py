r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from typing import Optional
from twilio.base.version import Version
from twilio.base.domain import Domain
from twilio.rest.memory.v1.bulk import BulkList
from twilio.rest.memory.v1.conversation_summary import ConversationSummaryList
from twilio.rest.memory.v1.data_mapping import DataMappingList
from twilio.rest.memory.v1.event import EventList
from twilio.rest.memory.v1.identifier import IdentifierList
from twilio.rest.memory.v1.identity_resolution_setting import (
    IdentityResolutionSettingList,
)
from twilio.rest.memory.v1.import_ import ImportList
from twilio.rest.memory.v1.lookup import LookupList
from twilio.rest.memory.v1.observation import ObservationList
from twilio.rest.memory.v1.operation import OperationList
from twilio.rest.memory.v1.profile import ProfileList
from twilio.rest.memory.v1.recall import RecallList
from twilio.rest.memory.v1.revision import RevisionList
from twilio.rest.memory.v1.store import StoreList
from twilio.rest.memory.v1.trait import TraitList
from twilio.rest.memory.v1.trait_group import TraitGroupList


class V1(Version):

    def __init__(self, domain: Domain):
        """
        Initialize the V1 version of Memory

        :param domain: The Twilio.memory domain
        """
        super().__init__(domain, "v1")
        self._operations: Optional[OperationList] = None
        self._stores: Optional[StoreList] = None

    def bulk(self, store_id: str, bulk_id: str = None):
        """
        Access the BulkList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param bulk_id: Optional instance ID to directly access BulkContext
        :returns: BulkList instance if bulk_id is None, otherwise BulkContext
        """
        list_instance = BulkList(self)
        return list_instance(store_id)

    def conversation_summaries(
        self, store_id: str, profile_id: str, conversation_summary_id: str = None
    ):
        """
        Access the ConversationSummaryList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param conversation_summary_id: Optional instance ID to directly access ConversationSummaryContext
        :returns: ConversationSummaryList instance if conversation_summary_id is None, otherwise ConversationSummaryContext
        """
        list_instance = ConversationSummaryList(self, store_id, profile_id)
        if conversation_summary_id is not None:
            return list_instance(conversation_summary_id)
        return list_instance

    def data_mappings(self, store_id: str, data_mapping_id: str = None):
        """
        Access the DataMappingList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param data_mapping_id: Optional instance ID to directly access DataMappingContext
        :returns: DataMappingList instance if data_mapping_id is None, otherwise DataMappingContext
        """
        list_instance = DataMappingList(self, store_id)
        if data_mapping_id is not None:
            return list_instance(data_mapping_id)
        return list_instance

    def events(self, store_id: str, profile_id: str, event_id: str = None):
        """
        Access the EventList resource

        :param store_id: The storeId identifier

        :param profile_id: The profileId identifier

        :param event_id: Optional instance ID to directly access EventContext
        :returns: EventList instance if event_id is None, otherwise EventContext
        """
        list_instance = EventList(self, store_id, profile_id)
        if event_id is not None:
            return list_instance(event_id)
        return list_instance

    def identifiers(self, store_id: str, profile_id: str, identifier_id: str = None):
        """
        Access the IdentifierList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param identifier_id: Optional instance ID to directly access IdentifierContext
        :returns: IdentifierList instance if identifier_id is None, otherwise IdentifierContext
        """
        list_instance = IdentifierList(self, store_id, profile_id)
        if identifier_id is not None:
            return list_instance(identifier_id)
        return list_instance

    def identity_resolution_settings(
        self, store_id: str, identity_resolution_setting_id: str = None
    ):
        """
        Access the IdentityResolutionSettingList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param identity_resolution_setting_id: Optional instance ID to directly access IdentityResolutionSettingContext
        :returns: IdentityResolutionSettingList instance if identity_resolution_setting_id is None, otherwise IdentityResolutionSettingContext
        """
        list_instance = IdentityResolutionSettingList(self)
        return list_instance(store_id)

    def imports(self, store_id: str, import_id: str = None):
        """
        Access the ImportList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param import_id: Optional instance ID to directly access ImportContext
        :returns: ImportList instance if import_id is None, otherwise ImportContext
        """
        list_instance = ImportList(self, store_id)
        if import_id is not None:
            return list_instance(import_id)
        return list_instance

    def lookup(self, store_id: str, lookup_id: str = None):
        """
        Access the LookupList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param lookup_id: Optional instance ID to directly access LookupContext
        :returns: LookupList instance if lookup_id is None, otherwise LookupContext
        """
        list_instance = LookupList(self, store_id)
        if lookup_id is not None:
            return list_instance(lookup_id)
        return list_instance

    def observations(self, store_id: str, profile_id: str, observation_id: str = None):
        """
        Access the ObservationList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param observation_id: Optional instance ID to directly access ObservationContext
        :returns: ObservationList instance if observation_id is None, otherwise ObservationContext
        """
        list_instance = ObservationList(self, store_id, profile_id)
        if observation_id is not None:
            return list_instance(observation_id)
        return list_instance

    @property
    def operations(self) -> OperationList:
        if self._operations is None:
            self._operations = OperationList(self)
        return self._operations

    def profiles(self, store_id: str, profile_id: str = None):
        """
        Access the ProfileList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: Optional instance ID to directly access ProfileContext
        :returns: ProfileList instance if profile_id is None, otherwise ProfileContext
        """
        list_instance = ProfileList(self, store_id)
        if profile_id is not None:
            return list_instance(profile_id)
        return list_instance

    def recall(self, store_id: str, profile_id: str, recall_id: str = None):
        """
        Access the RecallList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param recall_id: Optional instance ID to directly access RecallContext
        :returns: RecallList instance if recall_id is None, otherwise RecallContext
        """
        list_instance = RecallList(self, store_id, profile_id)
        if recall_id is not None:
            return list_instance(recall_id)
        return list_instance

    def revisions(
        self,
        store_id: str,
        profile_id: str,
        observation_id: str,
        revision_id: str = None,
    ):
        """
        Access the RevisionList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param observation_id: The observation ID.

        :param revision_id: Optional instance ID to directly access RevisionContext
        :returns: RevisionList instance if revision_id is None, otherwise RevisionContext
        """
        list_instance = RevisionList(self, store_id, profile_id, observation_id)
        if revision_id is not None:
            return list_instance(revision_id)
        return list_instance

    @property
    def stores(self) -> StoreList:
        if self._stores is None:
            self._stores = StoreList(self)
        return self._stores

    def traits(self, store_id: str, profile_id: str, trait_id: str = None):
        """
        Access the TraitList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.

        :param trait_id: Optional instance ID to directly access TraitContext
        :returns: TraitList instance if trait_id is None, otherwise TraitContext
        """
        list_instance = TraitList(self, store_id, profile_id)
        if trait_id is not None:
            return list_instance(trait_id)
        return list_instance

    def trait_groups(self, store_id: str, trait_group_id: str = None):
        """
        Access the TraitGroupList resource

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        :param trait_group_id: Optional instance ID to directly access TraitGroupContext
        :returns: TraitGroupList instance if trait_group_id is None, otherwise TraitGroupContext
        """
        list_instance = TraitGroupList(self, store_id)
        if trait_group_id is not None:
            return list_instance(trait_group_id)
        return list_instance

    def __repr__(self) -> str:
        """
        Provide a friendly representation
        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1>"
