r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class StoreInstance(InstanceResource):
    class ServiceRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    class UpdateStoreRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    """
    :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
    :ivar description: A human readable description of this resource, up to 128 characters.
    :ivar id: The unique identifier for the Memory Store
    :ivar status: The current status of the Memory Store.  A store begins in the QUEUED state as it is scheduled for processing.  It then moves to PROVISIONING at the beginning of processing. It transitions to ACTIVE once all dependent resources are provisioned, including Conversational Intelligence capabilities.  If there is an issue provisioning resources, the store will move to the FAILED state.
    :ivar intelligence_service_id: The ID of the associated intelligence service that was provisioned for memory extraction.
    :ivar version: The current version number of the Memory Store. Incremented on each successful update.
    """

    def __init__(
        self, version: Version, payload: Dict[str, Any], store_id: Optional[str] = None
    ):
        super().__init__(version)

        self.display_name: Optional[str] = payload.get("displayName")
        self.description: Optional[str] = payload.get("description")
        self.id: Optional[str] = payload.get("id")
        self.status: Optional[str] = payload.get("status")
        self.intelligence_service_id: Optional[str] = payload.get(
            "intelligenceServiceId"
        )
        self.version: Optional[int] = deserialize.integer(payload.get("version"))

        self._solution = {
            "store_id": store_id or self.store_id,
        }
        self._context: Optional[StoreContext] = None

    @property
    def _proxy(self) -> "StoreContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: StoreContext for this StoreInstance
        """
        if self._context is None:
            self._context = StoreContext(
                self._version,
                store_id=self._solution["store_id"],
            )
        return self._context

    def fetch(self) -> "StoreInstance":
        """
        Fetch the StoreInstance


        :returns: The fetched StoreInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "StoreInstance":
        """
        Asynchronous coroutine to fetch the StoreInstance


        :returns: The fetched StoreInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the StoreInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the StoreInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> "StoreInstance":
        """
        Update the StoreInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: The updated StoreInstance
        """
        return self._proxy.update(
            if_match=if_match,
            prefer=prefer,
            update_store_request=update_store_request,
        )

    async def update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> "StoreInstance":
        """
        Asynchronous coroutine to update the StoreInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: The updated StoreInstance
        """
        return await self._proxy.update_async(
            if_match=if_match,
            prefer=prefer,
            update_store_request=update_store_request,
        )

    def update_with_http_info(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the StoreInstance with HTTP info

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            if_match=if_match,
            prefer=prefer,
            update_store_request=update_store_request,
        )

    async def update_with_http_info_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the StoreInstance with HTTP info

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            if_match=if_match,
            prefer=prefer,
            update_store_request=update_store_request,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.StoreInstance {}>".format(context)


class StoreContext(InstanceContext):
    class ServiceRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    class UpdateStoreRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    def __init__(self, version: Version, store_id: str):
        """
        Initialize the StoreContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
        }
        self._uri = "/ControlPlane/Stores/{store_id}".format(**self._solution)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> StoreInstance:
        """
        Fetch the StoreInstance


        :returns: The fetched StoreInstance
        """
        payload, _, _ = self._fetch()
        return StoreInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the StoreInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = StoreInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> StoreInstance:
        """
        Asynchronous coroutine to fetch the StoreInstance


        :returns: The fetched StoreInstance
        """
        payload, _, _ = await self._fetch_async()
        return StoreInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the StoreInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = StoreInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_store_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> StoreInstance:
        """
        Update the StoreInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: The updated StoreInstance
        """
        payload, _, _ = self._update(
            if_match=if_match, prefer=prefer, update_store_request=update_store_request
        )
        return StoreInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    def update_with_http_info(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the StoreInstance and return response metadata

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            if_match=if_match, prefer=prefer, update_store_request=update_store_request
        )
        instance = StoreInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_store_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        if not (prefer is values.unset or (isinstance(prefer, str) and not prefer)):
            headers["Prefer"] = prefer

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> StoreInstance:
        """
        Asynchronous coroutine to update the StoreInstance

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: The updated StoreInstance
        """
        payload, _, _ = await self._update_async(
            if_match=if_match, prefer=prefer, update_store_request=update_store_request
        )
        return StoreInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    async def update_with_http_info_async(
        self,
        if_match: Union[str, object] = values.unset,
        prefer: Union[str, object] = values.unset,
        update_store_request: Union[UpdateStoreRequest, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the StoreInstance and return response metadata

        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches one provided, preventing accidental overwrites and responding with 412 Precondition Failed if they don't match.
        :param prefer: Allows specifying a preference for waiting for completion of the operation before responding. Set **respond-async; wait=X**, where **X** is the number of seconds for the server  to wait. The operation will return a 2xx success code if it completes within the specified wait time, otherwise a 202 Accepted is returned and processing continues asynchronously.
        :param update_store_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            if_match=if_match, prefer=prefer, update_store_request=update_store_request
        )
        instance = StoreInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.StoreContext {}>".format(context)


class StorePage(TokenPagination):
    def get_instance(self, payload: Dict[str, Any]) -> StoreInstance:
        """
        Build an instance of StoreInstance

        :param payload: Payload response from the API
        """
        return StoreInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.StorePage>"


class StoreList(ListResource):
    class ServiceRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    class UpdateStoreRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Store. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.display_name: Optional[str] = payload.get("display_name")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    def __init__(self, version: Version):
        """
        Initialize the StoreList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/ControlPlane/Stores"

    def _create(self, service_request: ServiceRequest) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = service_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, service_request: ServiceRequest) -> StoreInstance:
        """
        Create the StoreInstance

        :param service_request:

        :returns: The created StoreInstance
        """
        payload, _, _ = self._create(service_request=service_request)
        return StoreInstance(self._version, payload)

    def create_with_http_info(self, service_request: ServiceRequest) -> ApiResponse:
        """
        Create the StoreInstance and return response metadata

        :param service_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(service_request=service_request)
        instance = StoreInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, service_request: ServiceRequest) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = service_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(self, service_request: ServiceRequest) -> StoreInstance:
        """
        Asynchronously create the StoreInstance

        :param service_request:

        :returns: The created StoreInstance
        """
        payload, _, _ = await self._create_async(service_request=service_request)
        return StoreInstance(self._version, payload)

    async def create_with_http_info_async(
        self, service_request: ServiceRequest
    ) -> ApiResponse:
        """
        Asynchronously create the StoreInstance and return response metadata

        :param service_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            service_request=service_request
        )
        instance = StoreInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[StoreInstance]:
        """
        Streams StoreInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[StoreInstance]:
        """
        Asynchronously streams StoreInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams StoreInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams StoreInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[StoreInstance]:
        """
        Lists StoreInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                page_token=page_token,
                order_by=order_by,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[StoreInstance]:
        """
        Asynchronously lists StoreInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                page_token=page_token,
                order_by=order_by,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists StoreInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            order_by=order_by,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists StoreInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            order_by=order_by,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> StorePage:
        """
        Retrieve a single page of StoreInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :returns: Page of StoreInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return StorePage(self._version, response, uri=self._uri, params=data)

    async def page_async(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> StorePage:
        """
        Asynchronously retrieve a single page of StoreInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :returns: Page of StoreInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return StorePage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(
        self,
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with StorePage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "orderBy": order_by,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = StorePage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with StorePage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "orderBy": order_by,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        (
            response,
            status_code,
            response_headers,
        ) = await self._version.page_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = StorePage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> StorePage:
        """
        Retrieve a specific page of StoreInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of StoreInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return StorePage(self._version, response)

    async def get_page_async(self, target_url: str) -> StorePage:
        """
        Asynchronously retrieve a specific page of StoreInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of StoreInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return StorePage(self._version, response)

    def get(self, store_id: str) -> StoreContext:
        """
        Constructs a StoreContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return StoreContext(self._version, store_id=store_id)

    def __call__(self, store_id: str) -> StoreContext:
        """
        Constructs a StoreContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return StoreContext(self._version, store_id=store_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.StoreList>"
