r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from typing import Any, Dict, List, Optional
from twilio.base import values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class LookupInstance(InstanceResource):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    """
    :ivar normalized_value: Identifier value after normalization that was used for the lookup.
    :ivar profiles: 
    """

    def __init__(
        self, version: Version, payload: Dict[str, Any], store_id: Optional[str] = None
    ):
        super().__init__(version)

        self.normalized_value: Optional[str] = payload.get("normalizedValue")
        self.profiles: Optional[List[str]] = payload.get("profiles")

        self._solution = {
            "store_id": store_id or self.store_id,
        }
        self._context: Optional[LookupContext] = None

    @property
    def _proxy(self) -> "LookupContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: LookupContext for this LookupInstance
        """
        if self._context is None:
            self._context = LookupContext(
                self._version,
                store_id=self._solution["store_id"],
            )
        return self._context

    def create(self, identifier: Identifier) -> "LookupInstance":
        """
        Create the LookupInstance

        :param identifier:

        :returns: The created LookupInstance
        """
        return self._proxy.create(
            identifier,
        )

    async def create_async(self, identifier: Identifier) -> "LookupInstance":
        """
        Asynchronous coroutine to create the LookupInstance

        :param identifier:

        :returns: The created LookupInstance
        """
        return await self._proxy.create_async(
            identifier,
        )

    def create_with_http_info(self, identifier: Identifier) -> ApiResponse:
        """
        Create the LookupInstance with HTTP info

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.create_with_http_info(
            identifier,
        )

    async def create_with_http_info_async(self, identifier: Identifier) -> ApiResponse:
        """
        Asynchronous coroutine to create the LookupInstance with HTTP info

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.create_with_http_info_async(
            identifier,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.LookupInstance {}>".format(context)


class LookupContext(InstanceContext):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    def __init__(self, version: Version, store_id: str):
        """
        Initialize the LookupContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/Lookup".format(**self._solution)

    def _create(self, identifier: Identifier) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, identifier: Identifier) -> LookupInstance:
        """
        Create the LookupInstance

        :param identifier:

        :returns: The created LookupInstance
        """
        payload, _, _ = self._create(identifier=identifier)
        return LookupInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    def create_with_http_info(self, identifier: Identifier) -> ApiResponse:
        """
        Create the LookupInstance and return response metadata

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(identifier=identifier)
        instance = LookupInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, identifier: Identifier) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = identifier.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(self, identifier: Identifier) -> LookupInstance:
        """
        Asynchronous coroutine to create the LookupInstance

        :param identifier:

        :returns: The created LookupInstance
        """
        payload, _, _ = await self._create_async(identifier=identifier)
        return LookupInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    async def create_with_http_info_async(self, identifier: Identifier) -> ApiResponse:
        """
        Asynchronous coroutine to create the LookupInstance and return response metadata

        :param identifier:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(identifier=identifier)
        instance = LookupInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.LookupContext {}>".format(context)


class LookupList(ListResource):
    class Identifier(object):
        """
        :ivar id_type: Identifier type as configured in the service's Identity Resolution Settings. Must match an `idType` defined in the corresponding identifier settings and must satisfy the same length constraints.
        :ivar value: Raw value captured for the identifier. The service may normalize this value according to the `normalization` rule defined in the identifier settings before storage or matching (for example E.164 formatting for phone numbers).
        """

        def __init__(self, payload: Dict[str, Any]):
            self.id_type: Optional[str] = payload.get("id_type")
            self.value: Optional[str] = payload.get("value")

        def to_dict(self):
            return {
                "idType": self.id_type,
                "value": self.value,
            }

    def __init__(self, version: Version):
        """
        Initialize the LookupList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str) -> LookupContext:
        """
        Constructs a LookupContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return LookupContext(self._version, store_id=store_id)

    def __call__(self, store_id: str) -> LookupContext:
        """
        Constructs a LookupContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return LookupContext(self._version, store_id=store_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.LookupList>"
