r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional, Union
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class ImportInstance(InstanceResource):
    """
    :ivar imports:
    :ivar status: Current processing status of the import task
    :ivar filename: Original filename of the uploaded CSV
    :ivar created_at: Timestamp when the import was created
    :ivar updated_at: Timestamp when the import was last updated
    :ivar file_size: Size of the uploaded file in bytes (1 byte to 100 MiB)
    :ivar column_mappings: Mappings of CSV header columns to traits' fields
    :ivar summary:
    :ivar import_id: ID of the import task.
    :ivar url: Pre-signed URL to upload the CSV via a single PUT request.
    """

    def __init__(
        self,
        version: Version,
        payload: ResponseResource,
        store_id: str,
        import_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.imports: Optional[List[str]] = payload.get("imports")
        self.status: Optional["ImportInstance.str"] = payload.get("status")
        self.filename: Optional[str] = payload.get("filename")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )
        self.file_size: Optional[int] = deserialize.integer(payload.get("fileSize"))
        self.column_mappings: Optional[List[ColumnMappingItem]] = payload.get(
            "columnMappings"
        )
        self.summary: Optional[FetchProfileImportV2200ResponseSummary] = payload.get(
            "summary"
        )
        self.import_id: Optional[str] = payload.get("importId")
        self.url: Optional[str] = payload.get("url")

        # Only set _solution if path params are provided (not None)
        if store_id is not None or import_id is not None:
            self._solution = {
                "store_id": store_id,
                "import_id": import_id,
            }

        self._context: Optional[ImportContext] = None

    @property
    def _proxy(self) -> "ImportContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: ImportContext for this ImportInstance
        """
        if self._context is None:
            self._context = ImportContext(
                self._version,
                store_id=self._solution["store_id"],
                import_id=self._solution["import_id"],
            )
        return self._context

    def fetch(self) -> "ImportInstance":
        """
        Fetch the ImportInstance


        :returns: The fetched ImportInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "ImportInstance":
        """
        Asynchronous coroutine to fetch the ImportInstance


        :returns: The fetched ImportInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ImportInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ImportInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.ImportInstance {}>".format(context)


class ImportContext(InstanceContext):

    def __init__(self, version: Version, store_id: str, import_id: str):
        """
        Initialize the ImportContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param import_id: The task identifier for the import process.
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "import_id": import_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/Imports/{import_id}".format(
            **self._solution
        )

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> ImportInstance:
        """
        Fetch the ImportInstance


        :returns: The fetched ImportInstance
        """
        payload, _, _ = self._fetch()
        return ImportInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            import_id=self._solution["import_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the ImportInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = ImportInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            import_id=self._solution["import_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> ImportInstance:
        """
        Asynchronous coroutine to fetch the ImportInstance


        :returns: The fetched ImportInstance
        """
        payload, _, _ = await self._fetch_async()
        return ImportInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            import_id=self._solution["import_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the ImportInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = ImportInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            import_id=self._solution["import_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.ImportContext {}>".format(context)


class ImportPage(TokenPagination):

    def get_instance(self, payload: str) -> str:
        """
        Build an instance of str

        :param payload: Payload response from the API
        """

        return payload  # Return string ID directly

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.ImportPage>"


class ImportList(ListResource):

    class ColumnMappingItem(object):
        """
        :ivar column_name: The name of the column in the CSV header
        :ivar trait_group: The trait group to which this trait belongs
        :ivar trait_name: The name of the trait in the trait group
        """

        def __init__(self, payload: Dict[str, Any]):

            self.column_name: Optional[str] = payload.get("columnName")
            self.trait_group: Optional[str] = payload.get("traitGroup")
            self.trait_name: Optional[str] = payload.get("traitName")

        def to_dict(self):
            return {
                "columnName": self.column_name,
                "traitGroup": self.trait_group,
                "traitName": self.trait_name,
            }

    class CreateProfilesImportV2Request(object):
        """
        :ivar filename: The name of the file to generate a presigned URL
        :ivar file_size: The size of the file in bytes (1 byte to 100 MiB)
        :ivar column_mappings: Mappings of CSV header columns to traits' fields
        """

        def __init__(self, payload: Dict[str, Any]):

            self.filename: Optional[str] = payload.get("filename")
            self.file_size: Optional[int] = payload.get("fileSize")
            self.column_mappings: Optional[List[ImportList.ColumnMappingItem]] = (
                payload.get("columnMappings")
            )

        def to_dict(self):
            return {
                "filename": self.filename,
                "fileSize": self.file_size,
                "columnMappings": (
                    [
                        column_mappings.to_dict()
                        for column_mappings in self.column_mappings
                    ]
                    if self.column_mappings is not None
                    else None
                ),
            }

    class FetchProfileImportV2200ResponseSummary(object):
        """
        :ivar errors: Total count of errors encountered during import
        :ivar warnings: Total count of warnings encountered during import
        """

        def __init__(self, payload: Dict[str, Any]):

            self.errors: Optional[int] = payload.get("errors")
            self.warnings: Optional[int] = payload.get("warnings")

        def to_dict(self):
            return {
                "errors": self.errors,
                "warnings": self.warnings,
            }

    def __init__(self, version: Version, store_id: str):
        """
        Initialize the ImportList

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/Imports".format(**self._solution)

    def _create(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_profiles_import_v2_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> ImportInstance:
        """
        Create the ImportInstance

        :param create_profiles_import_v2_request:

        :returns: The created ImportInstance
        """
        payload, _, _ = self._create(
            create_profiles_import_v2_request=create_profiles_import_v2_request
        )
        return ImportInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    def create_with_http_info(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> ApiResponse:
        """
        Create the ImportInstance and return response metadata

        :param create_profiles_import_v2_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            create_profiles_import_v2_request=create_profiles_import_v2_request
        )
        instance = ImportInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = create_profiles_import_v2_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> ImportInstance:
        """
        Asynchronously create the ImportInstance

        :param create_profiles_import_v2_request:

        :returns: The created ImportInstance
        """
        payload, _, _ = await self._create_async(
            create_profiles_import_v2_request=create_profiles_import_v2_request
        )
        return ImportInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )

    async def create_with_http_info_async(
        self, create_profiles_import_v2_request: CreateProfilesImportV2Request
    ) -> ApiResponse:
        """
        Asynchronously create the ImportInstance and return response metadata

        :param create_profiles_import_v2_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            create_profiles_import_v2_request=create_profiles_import_v2_request
        )
        instance = ImportInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def list(
        self,
    ) -> List[str]:
        """
        Lists ImportInstance IDs from the API as a list.

        :returns: list that will contain up to limit results
        """

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        data = values.of({})
        payload, _, _ = self._version.fetch_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )

        return list(payload["imports"])

    async def list_async(
        self,
    ) -> List[str]:
        """
        Asynchronously lists ImportInstance IDs from the API as a list.

        :returns: list that will contain up to limit results
        """

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        data = values.of({})
        payload, _, _ = await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )

        return list(payload["imports"])

    def list_with_http_info(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists ImportInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists ImportInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
    ) -> ImportPage:
        """
        Retrieve a single page of ImportInstance records from the API.
        Request is executed immediately

        :returns: Page of ImportInstance
        """
        data = values.of({})

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return ImportPage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    async def page_async(
        self,
    ) -> ImportPage:
        """
        Asynchronously retrieve a single page of ImportInstance records from the API.
        Request is executed immediately

        :returns: Page of ImportInstance
        """
        data = values.of({})

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return ImportPage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    def page_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ImportPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = ImportPage(
            self._version, response, uri=self._uri, solution=self._solution
        )
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with ImportPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = ImportPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> ImportPage:
        """
        Retrieve a specific page of ImportInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ImportInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return ImportPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> ImportPage:
        """
        Asynchronously retrieve a specific page of ImportInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of ImportInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return ImportPage(self._version, response, solution=self._solution)

    def get(self, import_id: str) -> ImportContext:
        """
        Constructs a ImportContext

        :param import_id: The task identifier for the import process.
        """
        return ImportContext(
            self._version, store_id=self._solution["store_id"], import_id=import_id
        )

    def __call__(self, import_id: str) -> ImportContext:
        """
        Constructs a ImportContext

        :param import_id: The task identifier for the import process.
        """
        return ImportContext(
            self._version, store_id=self._solution["store_id"], import_id=import_id
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.ImportList>"
