r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse

from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class RevisionInstance(InstanceResource):
    """
    :ivar content: The main content of the observation.
    :ivar occurred_at: The timestamp when the observation originally occurred.
    :ivar source: The source system that generated this observation. Allows letters, numbers, spaces, and URL-safe symbols. Excludes URL-unsafe characters like quotes, angle brackets, and control characters.
    :ivar conversation_ids: Array of conversation IDs associated with this observation.
    :ivar id: A unique identifier for the observation using Twilio Type ID (TTID) format.
    :ivar created_at: The timestamp when the observation was created.
    :ivar updated_at: The timestamp when the observation was last updated.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        store_id: str,
        profile_id: str,
        observation_id: str,
    ):
        super().__init__(version)

        self.content: Optional[str] = payload.get("content")
        self.occurred_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("occurredAt")
        )
        self.source: Optional[str] = payload.get("source")
        self.conversation_ids: Optional[List[str]] = payload.get("conversationIds")
        self.id: Optional[str] = payload.get("id")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )

        # Only set _solution if path params are provided (not None)
        if store_id is not None or profile_id is not None or observation_id is not None:
            self._solution = {
                "store_id": store_id,
                "profile_id": profile_id,
                "observation_id": observation_id,
            }

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.RevisionInstance {}>".format(context)


class RevisionPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> RevisionInstance:
        """
        Build an instance of RevisionInstance

        :param payload: Payload response from the API
        """

        return RevisionInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
            observation_id=self._solution["observation_id"],
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.RevisionPage>"


class RevisionList(ListResource):

    def __init__(
        self, version: Version, store_id: str, profile_id: str, observation_id: str
    ):
        """
        Initialize the RevisionList

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param observation_id: The observation ID.

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "profile_id": profile_id,
            "observation_id": observation_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/{profile_id}/Observations/{observation_id}/Revisions".format(
            **self._solution
        )

    def stream(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[RevisionInstance]:
        """
        Streams RevisionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token,
            accept_encoding=accept_encoding,
            page_size=limits["page_size"],
        )

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[RevisionInstance]:
        """
        Asynchronously streams RevisionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token,
            accept_encoding=accept_encoding,
            page_size=limits["page_size"],
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams RevisionInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token,
            accept_encoding=accept_encoding,
            page_size=limits["page_size"],
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams RevisionInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token,
            accept_encoding=accept_encoding,
            page_size=limits["page_size"],
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[RevisionInstance]:
        """
        Lists RevisionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return list(
            self.stream(
                page_token=page_token,
                accept_encoding=accept_encoding,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[RevisionInstance]:
        """
        Asynchronously lists RevisionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return [
            record
            async for record in await self.stream_async(
                page_token=page_token,
                accept_encoding=accept_encoding,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists RevisionInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            accept_encoding=accept_encoding,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists RevisionInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            accept_encoding=accept_encoding,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
    ) -> RevisionPage:
        """
        Retrieve a single page of RevisionInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 1000.
        :param page_token: The token for the page of results to retrieve.
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :returns: Page of RevisionInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "Accept-Encoding": accept_encoding,
            }
        )

        headers = values.of(
            {
                "Accept-Encoding": accept_encoding,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return RevisionPage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    async def page_async(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        accept_encoding: Union[str, object] = values.unset,
    ) -> RevisionPage:
        """
        Asynchronously retrieve a single page of RevisionInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 1000.
        :param page_token: The token for the page of results to retrieve.
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :returns: Page of RevisionInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "Accept-Encoding": accept_encoding,
            }
        )

        headers = values.of(
            {
                "Accept-Encoding": accept_encoding,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return RevisionPage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    def page_with_http_info(
        self,
        accept_encoding: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RevisionPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "Accept-Encoding": accept_encoding,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of(
            {
                "Accept-Encoding": accept_encoding,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = RevisionPage(
            self._version, response, uri=self._uri, solution=self._solution
        )
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        accept_encoding: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param accept_encoding: Compression algorithms supported by the client (e.g., gzip, deflate, br)
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with RevisionPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "Accept-Encoding": accept_encoding,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of(
            {
                "Accept-Encoding": accept_encoding,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = RevisionPage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> RevisionPage:
        """
        Retrieve a specific page of RevisionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RevisionInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return RevisionPage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> RevisionPage:
        """
        Asynchronously retrieve a specific page of RevisionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of RevisionInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return RevisionPage(self._version, response, solution=self._solution)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.RevisionList>"
