r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import date, datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator, Protocol
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination



class RevisionInstance(InstanceResource):


    """
    :ivar revisions: Array of observation revisions ordered chronologically descending by update time (newest first).
    :ivar meta: 
    """

    def __init__(self, version: Version, payload:Dict[str, Any], store_id: Optional[str] = None, profile_id: Optional[str] = None, observation_id: Optional[str] = None):
        super().__init__(version)

        
        self.revisions: Optional[List[Dict[str, object]]] = payload.get("revisions")
        self.meta: Optional[str] = payload.get("meta")

        
        self._solution = { 
            "store_id": store_id or self.store_id,
            "profile_id": profile_id or self.profile_id,
            "observation_id": observation_id or self.observation_id,
        }
        self._context: Optional[RevisionContext] = None

    @property
    def _proxy(self) -> "RevisionContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: RevisionContext for this RevisionInstance
        """
        if self._context is None:
            self._context = RevisionContext(self._version, store_id=self._solution['store_id'], profile_id=self._solution['profile_id'], observation_id=self._solution['observation_id'],)
        return self._context
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Memory.V1.RevisionInstance {}>'.format(context)

class RevisionContext(InstanceContext):

    def __init__(self, version: Version, store_id: str, profile_id: str, observation_id: str):
        """
        Initialize the RevisionContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param observation_id: The observation ID.
        """
        super().__init__(version)

        
        # Path Solution
        self._solution = { 
            'store_id': store_id,
            'profile_id': profile_id,
            'observation_id': observation_id,
        }
        self._uri = '/Stores/{store_id}/Profiles/{profile_id}/Observations/{observation_id}/Revisions'.format(**self._solution)
        
    
    
    
    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = ' '.join('{}={}'.format(k, v) for k, v in self._solution.items())
        return '<Twilio.Memory.V1.RevisionContext {}>'.format(context)



class RevisionPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> RevisionInstance:
        """
        Build an instance of RevisionInstance

        :param payload: Payload response from the API
        """
        return RevisionInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.RevisionPage>"





class RevisionList(ListResource):
    
    def __init__(self, version: Version):
        """
        Initialize the RevisionList

        :param version: Version that contains the resource
        
        """
        super().__init__(version)

        
        
        
        

    def get(self, store_id: str, profile_id: str, observation_id: str) -> RevisionContext:
        """
        Constructs a RevisionContext
        
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param observation_id: The observation ID.
        """
        return RevisionContext(self._version, store_id=store_id, profile_id=profile_id, observation_id=observation_id)

    def __call__(self, store_id: str, profile_id: str, observation_id: str) -> RevisionContext:
        """
        Constructs a RevisionContext
        
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        :param profile_id: The unique identifier for the profile using Twilio Type ID (TTID) format.
        :param observation_id: The observation ID.
        """
        return RevisionContext(self._version, store_id=store_id, profile_id=profile_id, observation_id=observation_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return '<Twilio.Memory.V1.RevisionList>'

