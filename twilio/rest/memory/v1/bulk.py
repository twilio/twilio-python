r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from typing import Any, Dict, List, Optional
from twilio.base import values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class BulkInstance(InstanceResource):
    class ProfileData(object):
        """
        :ivar traits: Multiple trait groups.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.traits: Optional[Dict[str, Dict[str, object]]] = payload.get("traits")

        def to_dict(self):
            return {
                "traits": self.traits,
            }

    class UpdateProfilesBulkRequest(object):
        """
        :ivar profiles:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.profiles: Optional[List[BulkList.ProfileData]] = payload.get(
                "profiles"
            )

        def to_dict(self):
            return {
                "profiles": [profiles.to_dict() for profiles in self.profiles]
                if self.profiles is not None
                else None,
            }

    """
    :ivar message: 
    """

    def __init__(
        self, version: Version, payload: Dict[str, Any], store_id: Optional[str] = None
    ):
        super().__init__(version)

        self.message: Optional[str] = payload.get("message")

        self._solution = {
            "store_id": store_id or self.store_id,
        }
        self._context: Optional[BulkContext] = None

    @property
    def _proxy(self) -> "BulkContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: BulkContext for this BulkInstance
        """
        if self._context is None:
            self._context = BulkContext(
                self._version,
                store_id=self._solution["store_id"],
            )
        return self._context

    def update(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> "BulkInstance":
        """
        Update the BulkInstance

        :param update_profiles_bulk_request:

        :returns: The updated BulkInstance
        """
        return self._proxy.update(
            update_profiles_bulk_request=update_profiles_bulk_request,
        )

    async def update_async(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> "BulkInstance":
        """
        Asynchronous coroutine to update the BulkInstance

        :param update_profiles_bulk_request:

        :returns: The updated BulkInstance
        """
        return await self._proxy.update_async(
            update_profiles_bulk_request=update_profiles_bulk_request,
        )

    def update_with_http_info(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> ApiResponse:
        """
        Update the BulkInstance with HTTP info

        :param update_profiles_bulk_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            update_profiles_bulk_request=update_profiles_bulk_request,
        )

    async def update_with_http_info_async(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the BulkInstance with HTTP info

        :param update_profiles_bulk_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            update_profiles_bulk_request=update_profiles_bulk_request,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.BulkInstance {}>".format(context)


class BulkContext(InstanceContext):
    class ProfileData(object):
        """
        :ivar traits: Multiple trait groups.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.traits: Optional[Dict[str, Dict[str, object]]] = payload.get("traits")

        def to_dict(self):
            return {
                "traits": self.traits,
            }

    class UpdateProfilesBulkRequest(object):
        """
        :ivar profiles:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.profiles: Optional[List[BulkList.ProfileData]] = payload.get(
                "profiles"
            )

        def to_dict(self):
            return {
                "profiles": [profiles.to_dict() for profiles in self.profiles]
                if self.profiles is not None
                else None,
            }

    def __init__(self, version: Version, store_id: str):
        """
        Initialize the BulkContext

        :param version: Version that contains the resource
        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/Bulk".format(**self._solution)

    def _update(self, update_profiles_bulk_request: UpdateProfilesBulkRequest) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_profiles_bulk_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    def update(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> BulkInstance:
        """
        Update the BulkInstance

        :param update_profiles_bulk_request:

        :returns: The updated BulkInstance
        """
        payload, _, _ = self._update(
            update_profiles_bulk_request=update_profiles_bulk_request
        )
        return BulkInstance(self._version, payload, store_id=self._solution["store_id"])

    def update_with_http_info(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> ApiResponse:
        """
        Update the BulkInstance and return response metadata

        :param update_profiles_bulk_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            update_profiles_bulk_request=update_profiles_bulk_request
        )
        instance = BulkInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_profiles_bulk_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> BulkInstance:
        """
        Asynchronous coroutine to update the BulkInstance

        :param update_profiles_bulk_request:

        :returns: The updated BulkInstance
        """
        payload, _, _ = await self._update_async(
            update_profiles_bulk_request=update_profiles_bulk_request
        )
        return BulkInstance(self._version, payload, store_id=self._solution["store_id"])

    async def update_with_http_info_async(
        self, update_profiles_bulk_request: UpdateProfilesBulkRequest
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the BulkInstance and return response metadata

        :param update_profiles_bulk_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            update_profiles_bulk_request=update_profiles_bulk_request
        )
        instance = BulkInstance(
            self._version, payload, store_id=self._solution["store_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.BulkContext {}>".format(context)


class BulkList(ListResource):
    class ProfileData(object):
        """
        :ivar traits: Multiple trait groups.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.traits: Optional[Dict[str, Dict[str, object]]] = payload.get("traits")

        def to_dict(self):
            return {
                "traits": self.traits,
            }

    class UpdateProfilesBulkRequest(object):
        """
        :ivar profiles:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.profiles: Optional[List[BulkList.ProfileData]] = payload.get(
                "profiles"
            )

        def to_dict(self):
            return {
                "profiles": [profiles.to_dict() for profiles in self.profiles]
                if self.profiles is not None
                else None,
            }

    def __init__(self, version: Version):
        """
        Initialize the BulkList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str) -> BulkContext:
        """
        Constructs a BulkContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return BulkContext(self._version, store_id=store_id)

    def __call__(self, store_id: str) -> BulkContext:
        """
        Constructs a BulkContext

        :param store_id: A unique Memory Store ID using Twilio Type ID (TTID) format
        """
        return BulkContext(self._version, store_id=store_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.BulkList>"
