r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Memory API
    APIs for managing memory stores, profiles, events, and conversational intelligence capabilities.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union
from twilio.base import values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version


class EventInstance(InstanceResource):
    class CommunicationLifecycleEventRecipient(object):
        """
        :ivar address: The recipient's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the recipient participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class CommunicationLifecycleEventSender(object):
        """
        :ivar address: The sender's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the sender participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class ProfileEventRequest(object):
        """
        :ivar type: The type of event being sent.
        :ivar events:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["EventInstance.str"] = payload.get("type")
            self.events: Optional[
                List[EventList.ProfileEventRequestEvents]
            ] = payload.get("events")

        def to_dict(self):
            return {
                "type": self.type,
                "events": [events.to_dict() for events in self.events]
                if self.events is not None
                else None,
            }

    class ProfileEventRequestEvents(object):
        """
        :ivar timestamp: The time the event occurred in ISO8601 format with millisecond resolution. Defaults to received time if not provided.
        :ivar lifecycle: The lifecycle event type of the communication.
        :ivar conversation_id: The unique identifier for the conversation.
        :ivar communication_id: The unique identifier for the communication.
        :ivar communication_type: The communication channel type.
        :ivar communication_status: The lifecycle status of the communication.
        :ivar direction: The direction of the communication.
        :ivar sender:
        :ivar recipient:
        :ivar error_code: Error code for FAILED communication status.
        :ivar error_message: Error message for FAILED communication status.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.timestamp: Optional[datetime] = payload.get("timestamp")
            self.lifecycle: Optional[str] = payload.get("lifecycle")
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.communication_id: Optional[str] = payload.get("communication_id")
            self.communication_type: Optional[str] = payload.get("communication_type")
            self.communication_status: Optional[str] = payload.get(
                "communication_status"
            )
            self.direction: Optional["EventInstance.str"] = payload.get("direction")
            self.sender: Optional[
                EventList.CommunicationLifecycleEventSender
            ] = payload.get("sender")
            self.recipient: Optional[
                EventList.CommunicationLifecycleEventRecipient
            ] = payload.get("recipient")
            self.error_code: Optional[str] = payload.get("error_code")
            self.error_message: Optional[str] = payload.get("error_message")

        def to_dict(self):
            return {
                "timestamp": self.timestamp,
                "lifecycle": self.lifecycle,
                "conversation_id": self.conversation_id,
                "communication_id": self.communication_id,
                "communication_type": self.communication_type,
                "communication_status": self.communication_status,
                "direction": self.direction,
                "sender": self.sender.to_dict() if self.sender is not None else None,
                "recipient": self.recipient.to_dict()
                if self.recipient is not None
                else None,
                "error_code": self.error_code,
                "error_message": self.error_message,
            }

    """
    :ivar message: 
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        store_id: Optional[str] = None,
        profile_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.message: Optional[str] = payload.get("message")

        self._solution = {
            "store_id": store_id or self.store_id,
            "profile_id": profile_id or self.profile_id,
        }
        self._context: Optional[EventContext] = None

    @property
    def _proxy(self) -> "EventContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: EventContext for this EventInstance
        """
        if self._context is None:
            self._context = EventContext(
                self._version,
                store_id=self._solution["store_id"],
                profile_id=self._solution["profile_id"],
            )
        return self._context

    def create(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> "EventInstance":
        """
        Create the EventInstance

        :param profile_event_request:

        :returns: The created EventInstance
        """
        return self._proxy.create(
            profile_event_request=profile_event_request,
        )

    async def create_async(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> "EventInstance":
        """
        Asynchronous coroutine to create the EventInstance

        :param profile_event_request:

        :returns: The created EventInstance
        """
        return await self._proxy.create_async(
            profile_event_request=profile_event_request,
        )

    def create_with_http_info(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> ApiResponse:
        """
        Create the EventInstance with HTTP info

        :param profile_event_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.create_with_http_info(
            profile_event_request=profile_event_request,
        )

    async def create_with_http_info_async(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine to create the EventInstance with HTTP info

        :param profile_event_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.create_with_http_info_async(
            profile_event_request=profile_event_request,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.EventInstance {}>".format(context)


class EventContext(InstanceContext):
    class CommunicationLifecycleEventRecipient(object):
        """
        :ivar address: The recipient's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the recipient participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class CommunicationLifecycleEventSender(object):
        """
        :ivar address: The sender's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the sender participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class ProfileEventRequest(object):
        """
        :ivar type: The type of event being sent.
        :ivar events:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["EventInstance.str"] = payload.get("type")
            self.events: Optional[
                List[EventList.ProfileEventRequestEvents]
            ] = payload.get("events")

        def to_dict(self):
            return {
                "type": self.type,
                "events": [events.to_dict() for events in self.events]
                if self.events is not None
                else None,
            }

    class ProfileEventRequestEvents(object):
        """
        :ivar timestamp: The time the event occurred in ISO8601 format with millisecond resolution. Defaults to received time if not provided.
        :ivar lifecycle: The lifecycle event type of the communication.
        :ivar conversation_id: The unique identifier for the conversation.
        :ivar communication_id: The unique identifier for the communication.
        :ivar communication_type: The communication channel type.
        :ivar communication_status: The lifecycle status of the communication.
        :ivar direction: The direction of the communication.
        :ivar sender:
        :ivar recipient:
        :ivar error_code: Error code for FAILED communication status.
        :ivar error_message: Error message for FAILED communication status.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.timestamp: Optional[datetime] = payload.get("timestamp")
            self.lifecycle: Optional[str] = payload.get("lifecycle")
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.communication_id: Optional[str] = payload.get("communication_id")
            self.communication_type: Optional[str] = payload.get("communication_type")
            self.communication_status: Optional[str] = payload.get(
                "communication_status"
            )
            self.direction: Optional["EventInstance.str"] = payload.get("direction")
            self.sender: Optional[
                EventList.CommunicationLifecycleEventSender
            ] = payload.get("sender")
            self.recipient: Optional[
                EventList.CommunicationLifecycleEventRecipient
            ] = payload.get("recipient")
            self.error_code: Optional[str] = payload.get("error_code")
            self.error_message: Optional[str] = payload.get("error_message")

        def to_dict(self):
            return {
                "timestamp": self.timestamp,
                "lifecycle": self.lifecycle,
                "conversation_id": self.conversation_id,
                "communication_id": self.communication_id,
                "communication_type": self.communication_type,
                "communication_status": self.communication_status,
                "direction": self.direction,
                "sender": self.sender.to_dict() if self.sender is not None else None,
                "recipient": self.recipient.to_dict()
                if self.recipient is not None
                else None,
                "error_code": self.error_code,
                "error_message": self.error_message,
            }

    def __init__(self, version: Version, store_id: str, profile_id: str):
        """
        Initialize the EventContext

        :param version: Version that contains the resource
        :param store_id: The storeId identifier
        :param profile_id: The profileId identifier
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "store_id": store_id,
            "profile_id": profile_id,
        }
        self._uri = "/Stores/{store_id}/Profiles/{profile_id}/Events".format(
            **self._solution
        )

    def _create(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = profile_event_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> EventInstance:
        """
        Create the EventInstance

        :param profile_event_request:

        :returns: The created EventInstance
        """
        payload, _, _ = self._create(profile_event_request=profile_event_request)
        return EventInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )

    def create_with_http_info(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> ApiResponse:
        """
        Create the EventInstance and return response metadata

        :param profile_event_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            profile_event_request=profile_event_request
        )
        instance = EventInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = profile_event_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> EventInstance:
        """
        Asynchronous coroutine to create the EventInstance

        :param profile_event_request:

        :returns: The created EventInstance
        """
        payload, _, _ = await self._create_async(
            profile_event_request=profile_event_request
        )
        return EventInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )

    async def create_with_http_info_async(
        self, profile_event_request: Union[ProfileEventRequest, object] = values.unset
    ) -> ApiResponse:
        """
        Asynchronous coroutine to create the EventInstance and return response metadata

        :param profile_event_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            profile_event_request=profile_event_request
        )
        instance = EventInstance(
            self._version,
            payload,
            store_id=self._solution["store_id"],
            profile_id=self._solution["profile_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Memory.V1.EventContext {}>".format(context)


class EventList(ListResource):
    class CommunicationLifecycleEventRecipient(object):
        """
        :ivar address: The recipient's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the recipient participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class CommunicationLifecycleEventSender(object):
        """
        :ivar address: The sender's address. Must be a valid address matching the channel type such as E.164 format for phone numbers.
        :ivar participant_id: The unique identifier for the sender participant.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.address: Optional[str] = payload.get("address")
            self.participant_id: Optional[str] = payload.get("participant_id")

        def to_dict(self):
            return {
                "address": self.address,
                "participant_id": self.participant_id,
            }

    class ProfileEventRequest(object):
        """
        :ivar type: The type of event being sent.
        :ivar events:
        """

        def __init__(self, payload: Dict[str, Any]):
            self.type: Optional["EventInstance.str"] = payload.get("type")
            self.events: Optional[
                List[EventList.ProfileEventRequestEvents]
            ] = payload.get("events")

        def to_dict(self):
            return {
                "type": self.type,
                "events": [events.to_dict() for events in self.events]
                if self.events is not None
                else None,
            }

    class ProfileEventRequestEvents(object):
        """
        :ivar timestamp: The time the event occurred in ISO8601 format with millisecond resolution. Defaults to received time if not provided.
        :ivar lifecycle: The lifecycle event type of the communication.
        :ivar conversation_id: The unique identifier for the conversation.
        :ivar communication_id: The unique identifier for the communication.
        :ivar communication_type: The communication channel type.
        :ivar communication_status: The lifecycle status of the communication.
        :ivar direction: The direction of the communication.
        :ivar sender:
        :ivar recipient:
        :ivar error_code: Error code for FAILED communication status.
        :ivar error_message: Error message for FAILED communication status.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.timestamp: Optional[datetime] = payload.get("timestamp")
            self.lifecycle: Optional[str] = payload.get("lifecycle")
            self.conversation_id: Optional[str] = payload.get("conversation_id")
            self.communication_id: Optional[str] = payload.get("communication_id")
            self.communication_type: Optional[str] = payload.get("communication_type")
            self.communication_status: Optional[str] = payload.get(
                "communication_status"
            )
            self.direction: Optional["EventInstance.str"] = payload.get("direction")
            self.sender: Optional[
                EventList.CommunicationLifecycleEventSender
            ] = payload.get("sender")
            self.recipient: Optional[
                EventList.CommunicationLifecycleEventRecipient
            ] = payload.get("recipient")
            self.error_code: Optional[str] = payload.get("error_code")
            self.error_message: Optional[str] = payload.get("error_message")

        def to_dict(self):
            return {
                "timestamp": self.timestamp,
                "lifecycle": self.lifecycle,
                "conversation_id": self.conversation_id,
                "communication_id": self.communication_id,
                "communication_type": self.communication_type,
                "communication_status": self.communication_status,
                "direction": self.direction,
                "sender": self.sender.to_dict() if self.sender is not None else None,
                "recipient": self.recipient.to_dict()
                if self.recipient is not None
                else None,
                "error_code": self.error_code,
                "error_message": self.error_message,
            }

    def __init__(self, version: Version):
        """
        Initialize the EventList

        :param version: Version that contains the resource

        """
        super().__init__(version)

    def get(self, store_id: str, profile_id: str) -> EventContext:
        """
        Constructs a EventContext

        :param store_id: The storeId identifier
        :param profile_id: The profileId identifier
        """
        return EventContext(self._version, store_id=store_id, profile_id=profile_id)

    def __call__(self, store_id: str, profile_id: str) -> EventContext:
        """
        Constructs a EventContext

        :param store_id: The storeId identifier
        :param profile_id: The profileId identifier
        """
        return EventContext(self._version, store_id=store_id, profile_id=profile_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Memory.V1.EventList>"
