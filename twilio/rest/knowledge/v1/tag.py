r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Knowledge
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page


class TagInstance(InstanceResource):
    class KnowledgeV1ServiceCreateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        :ivar tag_id: The ID of the tag.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")
            self.tag_id: Optional[str] = payload.get("tag_id")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
                "tag_id": self.tag_id,
            }

    class KnowledgeV1ServiceUpdateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
            }

    """
    :ivar id: The ID of the tag value.
    :ivar value: The value of the tag.
    :ivar enabled: Whether the tag value is enabled.
    :ivar tag_id: The ID of the tag.
    :ivar date_created: The date and time in GMT when the Tag Value was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar date_updated: The date and time in GMT when the Tag Value was updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar url: The URL of the Tag Value resource.
    :ivar resource_id: The ID of the resource.
    :ivar resource_type: The type of the resource.
    :ivar account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the Tag Value resource.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        resource_id: Optional[str] = None,
        id: Optional[str] = None,
    ):
        super().__init__(version)

        self.id: Optional[str] = payload.get("id")
        self.value: Optional[str] = payload.get("value")
        self.enabled: Optional[bool] = payload.get("enabled")
        self.tag_id: Optional[str] = payload.get("tag_id")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("date_created")
        )
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("date_updated")
        )
        self.url: Optional[str] = payload.get("url")
        self.resource_id: Optional[str] = payload.get("resource_id")
        self.resource_type: Optional[str] = payload.get("resource_type")
        self.account_sid: Optional[str] = payload.get("account_sid")

        self._solution = {
            "resource_id": resource_id or self.resource_id,
            "id": id or self.id,
        }
        self._context: Optional[TagContext] = None

    @property
    def _proxy(self) -> "TagContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: TagContext for this TagInstance
        """
        if self._context is None:
            self._context = TagContext(
                self._version,
                resource_id=self._solution["resource_id"],
                id=self._solution["id"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the TagInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the TagInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the TagInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the TagInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()

    def update(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> "TagInstance":
        """
        Update the TagInstance

        :param knowledge_v1_service_update_tag_value_request:

        :returns: The updated TagInstance
        """
        return self._proxy.update(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request,
        )

    async def update_async(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> "TagInstance":
        """
        Asynchronous coroutine to update the TagInstance

        :param knowledge_v1_service_update_tag_value_request:

        :returns: The updated TagInstance
        """
        return await self._proxy.update_async(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request,
        )

    def update_with_http_info(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Update the TagInstance with HTTP info

        :param knowledge_v1_service_update_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request,
        )

    async def update_with_http_info_async(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TagInstance with HTTP info

        :param knowledge_v1_service_update_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V1.TagInstance {}>".format(context)


class TagContext(InstanceContext):
    class KnowledgeV1ServiceCreateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        :ivar tag_id: The ID of the tag.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")
            self.tag_id: Optional[str] = payload.get("tag_id")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
                "tag_id": self.tag_id,
            }

    class KnowledgeV1ServiceUpdateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
            }

    def __init__(self, version: Version, resource_id: str, id: str):
        """
        Initialize the TagContext

        :param version: Version that contains the resource
        :param resource_id:
        :param id:
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "resource_id": resource_id,
            "id": id,
        }
        self._uri = "/Knowledge/{resource_id}/Tags/{id}".format(**self._solution)

    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self) -> bool:
        """
        Deletes the TagInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the TagInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the TagInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the TagInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _update(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_v1_service_update_tag_value_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> TagInstance:
        """
        Update the TagInstance

        :param knowledge_v1_service_update_tag_value_request:

        :returns: The updated TagInstance
        """
        payload, _, _ = self._update(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request
        )
        return TagInstance(
            self._version,
            payload,
            resource_id=self._solution["resource_id"],
            id=self._solution["id"],
        )

    def update_with_http_info(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Update the TagInstance and return response metadata

        :param knowledge_v1_service_update_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request
        )
        instance = TagInstance(
            self._version,
            payload,
            resource_id=self._solution["resource_id"],
            id=self._solution["id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_v1_service_update_tag_value_request.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> TagInstance:
        """
        Asynchronous coroutine to update the TagInstance

        :param knowledge_v1_service_update_tag_value_request:

        :returns: The updated TagInstance
        """
        payload, _, _ = await self._update_async(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request
        )
        return TagInstance(
            self._version,
            payload,
            resource_id=self._solution["resource_id"],
            id=self._solution["id"],
        )

    async def update_with_http_info_async(
        self,
        knowledge_v1_service_update_tag_value_request: Union[
            KnowledgeV1ServiceUpdateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TagInstance and return response metadata

        :param knowledge_v1_service_update_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            knowledge_v1_service_update_tag_value_request=knowledge_v1_service_update_tag_value_request
        )
        instance = TagInstance(
            self._version,
            payload,
            resource_id=self._solution["resource_id"],
            id=self._solution["id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V1.TagContext {}>".format(context)


class TagPage(Page):
    def get_instance(self, payload: Dict[str, Any]) -> TagInstance:
        """
        Build an instance of TagInstance

        :param payload: Payload response from the API
        """
        return TagInstance(
            self._version, payload, resource_id=self._solution["resource_id"]
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V1.TagPage>"


class TagList(ListResource):
    class KnowledgeV1ServiceCreateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        :ivar tag_id: The ID of the tag.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")
            self.tag_id: Optional[str] = payload.get("tag_id")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
                "tag_id": self.tag_id,
            }

    class KnowledgeV1ServiceUpdateTagValueRequest(object):
        """
        :ivar value: The value of the tag.
        :ivar enabled: Whether the tag value is enabled.
        """

        def __init__(self, payload: Dict[str, Any]):
            self.value: Optional[str] = payload.get("value")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "value": self.value,
                "enabled": self.enabled,
            }

    def __init__(self, version: Version, resource_id: str):
        """
        Initialize the TagList

        :param version: Version that contains the resource
        :param resource_id:

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "resource_id": resource_id,
        }
        self._uri = "/Knowledge/{resource_id}/Tags".format(**self._solution)

    def _create(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_v1_service_create_tag_value_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> TagInstance:
        """
        Create the TagInstance

        :param knowledge_v1_service_create_tag_value_request:

        :returns: The created TagInstance
        """
        payload, _, _ = self._create(
            knowledge_v1_service_create_tag_value_request=knowledge_v1_service_create_tag_value_request
        )
        return TagInstance(
            self._version, payload, resource_id=self._solution["resource_id"]
        )

    def create_with_http_info(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Create the TagInstance and return response metadata

        :param knowledge_v1_service_create_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            knowledge_v1_service_create_tag_value_request=knowledge_v1_service_create_tag_value_request
        )
        instance = TagInstance(
            self._version, payload, resource_id=self._solution["resource_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_v1_service_create_tag_value_request.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> TagInstance:
        """
        Asynchronously create the TagInstance

        :param knowledge_v1_service_create_tag_value_request:

        :returns: The created TagInstance
        """
        payload, _, _ = await self._create_async(
            knowledge_v1_service_create_tag_value_request=knowledge_v1_service_create_tag_value_request
        )
        return TagInstance(
            self._version, payload, resource_id=self._solution["resource_id"]
        )

    async def create_with_http_info_async(
        self,
        knowledge_v1_service_create_tag_value_request: Union[
            KnowledgeV1ServiceCreateTagValueRequest, object
        ] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the TagInstance and return response metadata

        :param knowledge_v1_service_create_tag_value_request:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            knowledge_v1_service_create_tag_value_request=knowledge_v1_service_create_tag_value_request
        )
        instance = TagInstance(
            self._version, payload, resource_id=self._solution["resource_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[TagInstance]:
        """
        Streams TagInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[TagInstance]:
        """
        Asynchronously streams TagInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(page_size=limits["page_size"])

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams TagInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(page_size=limits["page_size"])

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams TagInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[TagInstance]:
        """
        Lists TagInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[TagInstance]:
        """
        Asynchronously lists TagInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists TagInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists TagInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TagPage:
        """
        Retrieve a single page of TagInstance records from the API.
        Request is executed immediately

        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TagInstance
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return TagPage(self._version, response, self._solution)

    async def page_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TagPage:
        """
        Asynchronously retrieve a single page of TagInstance records from the API.
        Request is executed immediately

        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TagInstance
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return TagPage(self._version, response, self._solution)

    def page_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TagPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = TagPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TagPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        (
            response,
            status_code,
            response_headers,
        ) = await self._version.page_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = TagPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> TagPage:
        """
        Retrieve a specific page of TagInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TagInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return TagPage(self._version, response, self._solution)

    async def get_page_async(self, target_url: str) -> TagPage:
        """
        Asynchronously retrieve a specific page of TagInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TagInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return TagPage(self._version, response, self._solution)

    def get(self, resource_id: str, id: str) -> TagContext:
        """
        Constructs a TagContext

        :param resource_id:
        :param id:
        """
        return TagContext(self._version, resource_id=resource_id, id=id)

    def __call__(self, resource_id: str, id: str) -> TagContext:
        """
        Constructs a TagContext

        :param resource_id:
        :param id:
        """
        return TagContext(self._version, resource_id=resource_id, id=id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V1.TagList>"
