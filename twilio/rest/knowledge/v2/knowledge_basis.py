r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Knowledge API
    APIs for managing knowledge bases and knowledge content for AI-powered applications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class KnowledgeBasisInstance(InstanceResource):
    """
    :ivar message:
    :ivar display_name: Provides a unique and addressable name to be assigned to this Knowledge Base. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
    :ivar description: A human readable description of this resource, up to 128 characters.
    :ivar id: The unique identifier for the Knowledge Base
    :ivar status: The provisioning status of the Knowledge Base
    :ivar created_at: The ISO 8601 timestamp when the Knowledge Base was created.
    :ivar updated_at: The ISO 8601 timestamp when the Knowledge Base was last updated.
    :ivar version: The current version number of the Knowledge Base. Incremented on each successful mutable update.
    :ivar status_url: URI to check operation status.
    """

    def __init__(
        self, version: Version, payload: ResponseResource, kb_id: Optional[str] = None
    ):
        super().__init__(version)

        self.message: Optional[str] = payload.get("message")
        self.display_name: Optional[str] = payload.get("displayName")
        self.description: Optional[str] = payload.get("description")
        self.id: Optional[str] = payload.get("id")
        self.status: Optional["KnowledgeBasisInstance.str"] = payload.get("status")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )
        self.version: Optional[int] = deserialize.integer(payload.get("version"))
        self.status_url: Optional[str] = payload.get("statusUrl")

        # Only set _solution if path params are provided (not None)
        if kb_id is not None:
            self._solution = {
                "kb_id": kb_id,
            }

        self._context: Optional[KnowledgeBasisContext] = None

    @property
    def _proxy(self) -> "KnowledgeBasisContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: KnowledgeBasisContext for this KnowledgeBasisInstance
        """
        if self._context is None:
            self._context = KnowledgeBasisContext(
                self._version,
                kb_id=self._solution["kb_id"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the KnowledgeBasisInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the KnowledgeBasisInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the KnowledgeBasisInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the KnowledgeBasisInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()

    def fetch(self) -> "KnowledgeBasisInstance":
        """
        Fetch the KnowledgeBasisInstance


        :returns: The fetched KnowledgeBasisInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "KnowledgeBasisInstance":
        """
        Asynchronous coroutine to fetch the KnowledgeBasisInstance


        :returns: The fetched KnowledgeBasisInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the KnowledgeBasisInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the KnowledgeBasisInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> "KnowledgeBasisInstance":
        """
        Update the KnowledgeBasisInstance

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: The updated KnowledgeBasisInstance
        """
        return self._proxy.update(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )

    async def update_async(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> "KnowledgeBasisInstance":
        """
        Asynchronous coroutine to update the KnowledgeBasisInstance

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: The updated KnowledgeBasisInstance
        """
        return await self._proxy.update_async(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )

    def update_with_http_info(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the KnowledgeBasisInstance with HTTP info

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )

    async def update_with_http_info_async(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the KnowledgeBasisInstance with HTTP info

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V2.KnowledgeBasisInstance {}>".format(context)


class KnowledgeBasisContext(InstanceContext):

    def __init__(self, version: Version, kb_id: str):
        """
        Initialize the KnowledgeBasisContext

        :param version: Version that contains the resource
        :param kb_id: A unique Knowledge Base ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "kb_id": kb_id,
        }
        self._uri = "/ControlPlane/KnowledgeBases/{kb_id}".format(**self._solution)

    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self) -> bool:
        """
        Deletes the KnowledgeBasisInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the KnowledgeBasisInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the KnowledgeBasisInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the KnowledgeBasisInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> KnowledgeBasisInstance:
        """
        Fetch the KnowledgeBasisInstance


        :returns: The fetched KnowledgeBasisInstance
        """
        payload, _, _ = self._fetch()
        return KnowledgeBasisInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the KnowledgeBasisInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = KnowledgeBasisInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> KnowledgeBasisInstance:
        """
        Asynchronous coroutine to fetch the KnowledgeBasisInstance


        :returns: The fetched KnowledgeBasisInstance
        """
        payload, _, _ = await self._fetch_async()
        return KnowledgeBasisInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the KnowledgeBasisInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = KnowledgeBasisInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_knowledge_base_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> KnowledgeBasisInstance:
        """
        Update the KnowledgeBasisInstance

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: The updated KnowledgeBasisInstance
        """
        payload, _, _ = self._update(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )
        return KnowledgeBasisInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )

    def update_with_http_info(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the KnowledgeBasisInstance and return response metadata

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )
        instance = KnowledgeBasisInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = update_knowledge_base_request.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PATCH", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> KnowledgeBasisInstance:
        """
        Asynchronous coroutine to update the KnowledgeBasisInstance

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: The updated KnowledgeBasisInstance
        """
        payload, _, _ = await self._update_async(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )
        return KnowledgeBasisInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )

    async def update_with_http_info_async(
        self,
        update_knowledge_base_request: UpdateKnowledgeBaseRequest,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the KnowledgeBasisInstance and return response metadata

        :param update_knowledge_base_request:
        :param if_match: Allows for optimistic concurrency control by making the request conditional. Server will only act if the resource's current Entity Tag (ETag) matches the one provided, preventing accidental overwrites.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            update_knowledge_base_request=update_knowledge_base_request,
            if_match=if_match,
        )
        instance = KnowledgeBasisInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V2.KnowledgeBasisContext {}>".format(context)


class KnowledgeBasisPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> KnowledgeBasisInstance:
        """
        Build an instance of KnowledgeBasisInstance

        :param payload: Payload response from the API
        """

        return KnowledgeBasisInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V2.KnowledgeBasisPage>"


class KnowledgeBasisList(ListResource):

    class KnowledgeBaseCore(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Knowledge Base. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    class UpdateKnowledgeBaseRequest(object):
        """
        :ivar display_name: Provides a unique and addressable name to be assigned to this Knowledge Base. This name is assigned by the developer and can be used in addition to the ID. It is intended to be human-readable and unique within the account.
        :ivar description: A human readable description of this resource, up to 128 characters.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")

        def to_dict(self):
            return {
                "displayName": self.display_name,
                "description": self.description,
            }

    def __init__(self, version: Version):
        """
        Initialize the KnowledgeBasisList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/ControlPlane/KnowledgeBases"

    def _create(self, knowledge_base_core: KnowledgeBaseCore) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_base_core.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, knowledge_base_core: KnowledgeBaseCore) -> KnowledgeBasisInstance:
        """
        Create the KnowledgeBasisInstance

        :param knowledge_base_core:

        :returns: The created KnowledgeBasisInstance
        """
        payload, _, _ = self._create(knowledge_base_core=knowledge_base_core)
        return KnowledgeBasisInstance(self._version, payload)

    def create_with_http_info(
        self, knowledge_base_core: KnowledgeBaseCore
    ) -> ApiResponse:
        """
        Create the KnowledgeBasisInstance and return response metadata

        :param knowledge_base_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            knowledge_base_core=knowledge_base_core
        )
        instance = KnowledgeBasisInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, knowledge_base_core: KnowledgeBaseCore) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_base_core.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self, knowledge_base_core: KnowledgeBaseCore
    ) -> KnowledgeBasisInstance:
        """
        Asynchronously create the KnowledgeBasisInstance

        :param knowledge_base_core:

        :returns: The created KnowledgeBasisInstance
        """
        payload, _, _ = await self._create_async(
            knowledge_base_core=knowledge_base_core
        )
        return KnowledgeBasisInstance(self._version, payload)

    async def create_with_http_info_async(
        self, knowledge_base_core: KnowledgeBaseCore
    ) -> ApiResponse:
        """
        Asynchronously create the KnowledgeBasisInstance and return response metadata

        :param knowledge_base_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            knowledge_base_core=knowledge_base_core
        )
        instance = KnowledgeBasisInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[KnowledgeBasisInstance]:
        """
        Streams KnowledgeBasisInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[KnowledgeBasisInstance]:
        """
        Asynchronously streams KnowledgeBasisInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams KnowledgeBasisInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams KnowledgeBasisInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token, order_by=order_by, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[KnowledgeBasisInstance]:
        """
        Lists KnowledgeBasisInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return list(
            self.stream(
                page_token=page_token,
                order_by=order_by,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[KnowledgeBasisInstance]:
        """
        Asynchronously lists KnowledgeBasisInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return [
            record
            async for record in await self.stream_async(
                page_token=page_token,
                order_by=order_by,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists KnowledgeBasisInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            order_by=order_by,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists KnowledgeBasisInstance and returns headers from first page


        :param str page_token: The token for the page of results to retrieve.
        :param str order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            order_by=order_by,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> KnowledgeBasisPage:
        """
        Retrieve a single page of KnowledgeBasisInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :returns: Page of KnowledgeBasisInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return KnowledgeBasisPage(self._version, response, uri=self._uri, params=data)

    async def page_async(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        order_by: Union[str, object] = values.unset,
    ) -> KnowledgeBasisPage:
        """
        Asynchronously retrieve a single page of KnowledgeBasisInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of items to return per page, maximum of 100.
        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :returns: Page of KnowledgeBasisInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "orderBy": order_by,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return KnowledgeBasisPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(
        self,
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with KnowledgeBasisPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "orderBy": order_by,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = KnowledgeBasisPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        order_by: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: The token for the page of results to retrieve.
        :param order_by: Either 'ASC' or 'DESC' to sort results ascending or descending respectively.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with KnowledgeBasisPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "orderBy": order_by,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = KnowledgeBasisPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> KnowledgeBasisPage:
        """
        Retrieve a specific page of KnowledgeBasisInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of KnowledgeBasisInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return KnowledgeBasisPage(self._version, response)

    async def get_page_async(self, target_url: str) -> KnowledgeBasisPage:
        """
        Asynchronously retrieve a specific page of KnowledgeBasisInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of KnowledgeBasisInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return KnowledgeBasisPage(self._version, response)

    def get(self, kb_id: str) -> KnowledgeBasisContext:
        """
        Constructs a KnowledgeBasisContext

        :param kb_id: A unique Knowledge Base ID using Twilio Type ID (TTID) format
        """
        return KnowledgeBasisContext(self._version, kb_id=kb_id)

    def __call__(self, kb_id: str) -> KnowledgeBasisContext:
        """
        Constructs a KnowledgeBasisContext

        :param kb_id: A unique Knowledge Base ID using Twilio Type ID (TTID) format
        """
        return KnowledgeBasisContext(self._version, kb_id=kb_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V2.KnowledgeBasisList>"
