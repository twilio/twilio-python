r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Knowledge API
    APIs for managing knowledge bases and knowledge content for AI-powered applications.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class KnowledgeInstance(InstanceResource):

    class SupportedFileMimeType(object):
        TEXT_CSV = "text/csv"
        TEXT_MARKDOWN = "text/markdown"
        TEXT_MDX = "text/mdx"
        APPLICATION_PDF = "application/pdf"
        TEXT_TAB_SEPARATED_VALUES = "text/tab-separated-values"
        TEXT_PLAIN = "text/plain"

    """
    :ivar name: The name of the knowledge source.
    :ivar description: A detailed description of the knowledge source and when to use it.  This helps provide context about the content and its intended purpose.
    :ivar source: 
    :ivar id: The unique identifier of knowledge source.
    :ivar status: The status of processing the knowledge source ('SCHEDULED', 'QUEUED', 'PROCESSING', 'COMPLETED', 'FAILED').
    :ivar created_at: The date and time in GMT when the Knowledge was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar updated_at: The date and time in GMT when the Knowledge was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar code: The Twilio error code.
    :ivar message: A human readable message describing the error.
    :ivar more_info: A URL to a [Twilio error directory](https://www.twilio.com/docs/api/errors) page with more information about the error code.
    """

    def __init__(
        self,
        version: Version,
        payload: ResponseResource,
        kb_id: str,
        knowledge_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.name: Optional[str] = payload.get("name")
        self.description: Optional[str] = payload.get("description")
        self.source: Optional[str] = payload.get("source")
        self.id: Optional[str] = payload.get("id")
        self.status: Optional["KnowledgeInstance.str"] = payload.get("status")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.updated_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("updatedAt")
        )
        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.more_info: Optional[str] = payload.get("more_info")

        # Only set _solution if path params are provided (not None)
        if kb_id is not None or knowledge_id is not None:
            self._solution = {
                "kb_id": kb_id,
                "knowledge_id": knowledge_id,
            }

        self._context: Optional[KnowledgeContext] = None

    @property
    def _proxy(self) -> "KnowledgeContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: KnowledgeContext for this KnowledgeInstance
        """
        if self._context is None:
            self._context = KnowledgeContext(
                self._version,
                kb_id=self._solution["kb_id"],
                knowledge_id=self._solution["knowledge_id"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the KnowledgeInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the KnowledgeInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the KnowledgeInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the KnowledgeInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()

    def fetch(self) -> "KnowledgeInstance":
        """
        Fetch the KnowledgeInstance


        :returns: The fetched KnowledgeInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "KnowledgeInstance":
        """
        Asynchronous coroutine to fetch the KnowledgeInstance


        :returns: The fetched KnowledgeInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the KnowledgeInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the KnowledgeInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> "KnowledgeInstance":
        """
        Update the KnowledgeInstance

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: The updated KnowledgeInstance
        """
        return self._proxy.update(
            refresh=refresh,
            knowledge_core=knowledge_core,
        )

    async def update_async(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> "KnowledgeInstance":
        """
        Asynchronous coroutine to update the KnowledgeInstance

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: The updated KnowledgeInstance
        """
        return await self._proxy.update_async(
            refresh=refresh,
            knowledge_core=knowledge_core,
        )

    def update_with_http_info(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the KnowledgeInstance with HTTP info

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            refresh=refresh,
            knowledge_core=knowledge_core,
        )

    async def update_with_http_info_async(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the KnowledgeInstance with HTTP info

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            refresh=refresh,
            knowledge_core=knowledge_core,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V2.KnowledgeInstance {}>".format(context)


class KnowledgeContext(InstanceContext):

    def __init__(self, version: Version, kb_id: str, knowledge_id: str):
        """
        Initialize the KnowledgeContext

        :param version: Version that contains the resource
        :param kb_id: A unique Knowledge Base ID using Twilio Type ID (TTID) format
        :param knowledge_id: A unique Knowledge resource ID using Twilio Type ID (TTID) format
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "kb_id": kb_id,
            "knowledge_id": knowledge_id,
        }
        self._uri = "/KnowledgeBases/{kb_id}/Knowledge/{knowledge_id}".format(
            **self._solution
        )

    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self) -> bool:
        """
        Deletes the KnowledgeInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the KnowledgeInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the KnowledgeInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the KnowledgeInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> KnowledgeInstance:
        """
        Fetch the KnowledgeInstance


        :returns: The fetched KnowledgeInstance
        """
        payload, _, _ = self._fetch()
        return KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the KnowledgeInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> KnowledgeInstance:
        """
        Asynchronous coroutine to fetch the KnowledgeInstance


        :returns: The fetched KnowledgeInstance
        """
        payload, _, _ = await self._fetch_async()
        return KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the KnowledgeInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_core.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        params = values.of(
            {
                "refresh": serialize.boolean_to_string(refresh),
            }
        )

        return self._version.update_with_response_info(
            method="PATCH", uri=self._uri, data=data, headers=headers, params=params
        )

    def update(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> KnowledgeInstance:
        """
        Update the KnowledgeInstance

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: The updated KnowledgeInstance
        """
        payload, _, _ = self._update(refresh=refresh, knowledge_core=knowledge_core)
        return KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )

    def update_with_http_info(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the KnowledgeInstance and return response metadata

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            refresh=refresh, knowledge_core=knowledge_core
        )
        instance = KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_core.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        params = values.of(
            {
                "refresh": serialize.boolean_to_string(refresh),
            }
        )

        return await self._version.update_with_response_info_async(
            method="PATCH", uri=self._uri, data=data, headers=headers, params=params
        )

    async def update_async(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> KnowledgeInstance:
        """
        Asynchronous coroutine to update the KnowledgeInstance

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: The updated KnowledgeInstance
        """
        payload, _, _ = await self._update_async(
            refresh=refresh, knowledge_core=knowledge_core
        )
        return KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )

    async def update_with_http_info_async(
        self,
        refresh: Union[bool, object] = values.unset,
        knowledge_core: Union[KnowledgeCore, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the KnowledgeInstance and return response metadata

        :param refresh: When true, re-queues processing for this knowledge resource. Idempotent while the resource is already QUEUED or PROCESSING.
        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            refresh=refresh, knowledge_core=knowledge_core
        )
        instance = KnowledgeInstance(
            self._version,
            payload,
            kb_id=self._solution["kb_id"],
            knowledge_id=self._solution["knowledge_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Knowledge.V2.KnowledgeContext {}>".format(context)


class KnowledgePage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> KnowledgeInstance:
        """
        Build an instance of KnowledgeInstance

        :param payload: Payload response from the API
        """

        return KnowledgeInstance(self._version, payload, kb_id=self._solution["kb_id"])

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V2.KnowledgePage>"


class KnowledgeList(ListResource):

    class KnowledgeCore(object):
        """
        :ivar name: The name of the knowledge source.
        :ivar description: A detailed description of the knowledge source and when to use it.  This helps provide context about the content and its intended purpose.
        :ivar source:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.name: Optional[str] = payload.get("name")
            self.description: Optional[str] = payload.get("description")
            self.source: Optional[KnowledgeList.KnowledgeSourceTypes] = payload.get(
                "source"
            )

        def to_dict(self):
            return {
                "name": self.name,
                "description": self.description,
                "source": self.source.to_dict() if self.source is not None else None,
            }

    class KnowledgeSourceTypes(object):
        """
        :ivar type: Raw text knowledge sources
        :ivar content: The raw text content to be processed
        :ivar url: The URL to crawl for web content
        :ivar crawl_depth: The maximum depth to crawl from the source URL
        :ivar crawl_period: Frequency of re-crawling the website for updated content
        :ivar file_name: Name of the file to be uploaded
        :ivar file_size: Expected size of the file in bytes
        :ivar mime_type:
        :ivar import_url: Presigned S3 URL for file upload (when status is SCHEDULED) or the permanent S3 location after upload completes. Use PUT method to upload the file to this URL when status is SCHEDULED.
        :ivar upload_expiration: Expiration time of the presigned upload URL in ISO 8601 format (only present when status is SCHEDULED)
        """

        def __init__(self, payload: Dict[str, Any]):

            self.type: Optional["KnowledgeInstance.str"] = payload.get("type")
            self.content: Optional[str] = payload.get("content")
            self.url: Optional[str] = payload.get("url")
            self.crawl_depth: Optional[int] = payload.get("crawlDepth")
            self.crawl_period: Optional["KnowledgeInstance.str"] = payload.get(
                "crawlPeriod"
            )
            self.file_name: Optional[str] = payload.get("fileName")
            self.file_size: Optional[int] = payload.get("fileSize")
            self.mime_type: Optional["KnowledgeInstance.SupportedFileMimeType"] = (
                payload.get("mimeType")
            )
            self.import_url: Optional[str] = payload.get("importUrl")
            self.upload_expiration: Optional[datetime] = payload.get("uploadExpiration")

        def to_dict(self):
            return {
                "type": self.type,
                "content": self.content,
                "url": self.url,
                "crawlDepth": self.crawl_depth,
                "crawlPeriod": self.crawl_period,
                "fileName": self.file_name,
                "fileSize": self.file_size,
                "mimeType": self.mime_type,
                "importUrl": self.import_url,
                "uploadExpiration": self.upload_expiration,
            }

    def __init__(self, version: Version, kb_id: str):
        """
        Initialize the KnowledgeList

        :param version: Version that contains the resource
        :param kb_id: A unique Knowledge Base ID using Twilio Type ID (TTID) format

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "kb_id": kb_id,
        }
        self._uri = "/KnowledgeBases/{kb_id}/Knowledge".format(**self._solution)

    def _create(self, knowledge_core: KnowledgeCore) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_core.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, knowledge_core: KnowledgeCore) -> KnowledgeInstance:
        """
        Create the KnowledgeInstance

        :param knowledge_core:

        :returns: The created KnowledgeInstance
        """
        payload, _, _ = self._create(knowledge_core=knowledge_core)
        return KnowledgeInstance(self._version, payload, kb_id=self._solution["kb_id"])

    def create_with_http_info(self, knowledge_core: KnowledgeCore) -> ApiResponse:
        """
        Create the KnowledgeInstance and return response metadata

        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(knowledge_core=knowledge_core)
        instance = KnowledgeInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, knowledge_core: KnowledgeCore) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = knowledge_core.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(self, knowledge_core: KnowledgeCore) -> KnowledgeInstance:
        """
        Asynchronously create the KnowledgeInstance

        :param knowledge_core:

        :returns: The created KnowledgeInstance
        """
        payload, _, _ = await self._create_async(knowledge_core=knowledge_core)
        return KnowledgeInstance(self._version, payload, kb_id=self._solution["kb_id"])

    async def create_with_http_info_async(
        self, knowledge_core: KnowledgeCore
    ) -> ApiResponse:
        """
        Asynchronously create the KnowledgeInstance and return response metadata

        :param knowledge_core:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            knowledge_core=knowledge_core
        )
        instance = KnowledgeInstance(
            self._version, payload, kb_id=self._solution["kb_id"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[KnowledgeInstance]:
        """
        Streams KnowledgeInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page=page, page_token=page_token, page_size=limits["page_size"]
        )

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[KnowledgeInstance]:
        """
        Asynchronously streams KnowledgeInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page=page, page_token=page_token, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams KnowledgeInstance and returns headers from first page


        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page=page, page_token=page_token, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams KnowledgeInstance and returns headers from first page


        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page=page, page_token=page_token, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[KnowledgeInstance]:
        """
        Lists KnowledgeInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return list(
            self.stream(
                page=page,
                page_token=page_token,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[KnowledgeInstance]:
        """
        Asynchronously lists KnowledgeInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return [
            record
            async for record in await self.stream_async(
                page=page,
                page_token=page_token,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists KnowledgeInstance and returns headers from first page


        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page=page,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists KnowledgeInstance and returns headers from first page


        :param int page: The page index. This value is simply for client state.
        :param str page_token: The token for the page of results to retrieve.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page=page,
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
    ) -> KnowledgePage:
        """
        Retrieve a single page of KnowledgeInstance records from the API.
        Request is executed immediately

        :param page: The page index. This value is simply for client state.
        :param page_size: The maximum number of items to return per page, maximum of 1000.
        :param page_token: The token for the page of results to retrieve.
        :returns: Page of KnowledgeInstance
        """
        data = values.of(
            {
                "page": page,
                "pageSize": page_size,
                "pageToken": page_token,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return KnowledgePage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    async def page_async(
        self,
        page: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
    ) -> KnowledgePage:
        """
        Asynchronously retrieve a single page of KnowledgeInstance records from the API.
        Request is executed immediately

        :param page: The page index. This value is simply for client state.
        :param page_size: The maximum number of items to return per page, maximum of 1000.
        :param page_token: The token for the page of results to retrieve.
        :returns: Page of KnowledgeInstance
        """
        data = values.of(
            {
                "page": page,
                "pageSize": page_size,
                "pageToken": page_token,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return KnowledgePage(
            self._version, response, uri=self._uri, params=data, solution=self._solution
        )

    def page_with_http_info(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page: The page index. This value is simply for client state.
        :param page_token: The token for the page of results to retrieve.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with KnowledgePage, status code, and headers
        """
        data = values.of(
            {
                "page": page,
                "pageToken": page_token,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = KnowledgePage(
            self._version, response, uri=self._uri, solution=self._solution
        )
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page: The page index. This value is simply for client state.
        :param page_token: The token for the page of results to retrieve.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with KnowledgePage, status code, and headers
        """
        data = values.of(
            {
                "page": page,
                "pageToken": page_token,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = KnowledgePage(self._version, response, solution=self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> KnowledgePage:
        """
        Retrieve a specific page of KnowledgeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of KnowledgeInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return KnowledgePage(self._version, response, solution=self._solution)

    async def get_page_async(self, target_url: str) -> KnowledgePage:
        """
        Asynchronously retrieve a specific page of KnowledgeInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of KnowledgeInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return KnowledgePage(self._version, response, solution=self._solution)

    def get(self, knowledge_id: str) -> KnowledgeContext:
        """
        Constructs a KnowledgeContext

        :param knowledge_id: A unique Knowledge resource ID using Twilio Type ID (TTID) format
        """
        return KnowledgeContext(
            self._version, kb_id=self._solution["kb_id"], knowledge_id=knowledge_id
        )

    def __call__(self, knowledge_id: str) -> KnowledgeContext:
        """
        Constructs a KnowledgeContext

        :param knowledge_id: A unique Knowledge resource ID using Twilio Type ID (TTID) format
        """
        return KnowledgeContext(
            self._version, kb_id=self._solution["kb_id"], knowledge_id=knowledge_id
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Knowledge.V2.KnowledgeList>"
