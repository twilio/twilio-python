r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Insights
    Insights Domain V3 API. Includes synchronous Query, Metadata, and Async Query (long-running operation) endpoints. 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class QueryJobInstance(InstanceResource):

    class LongRunningOperationStatus(object):
        PENDING = "PENDING"
        RUNNING = "RUNNING"
        CANCELLED = "CANCELLED"
        COMPLETED = "COMPLETED"
        FAILED = "FAILED"

    """
    :ivar operation_id: 
    :ivar status: 
    :ivar status_url: 
    :ivar created_at: 
    :ivar completed_at: When the operation reached a terminal state.
    :ivar error: Error details. Present only when status is FAILED.
    :ivar result_url: URL to retrieve query results. Present only when status is COMPLETED. Supports pagination via pageSize and pageToken query parameters. 
    :ivar result_retention_period: ISO 8601 duration for how long results are retained.
    """

    def __init__(
        self,
        version: Version,
        payload: ResponseResource,
        operation_id: Optional[str] = None,
    ):
        super().__init__(version)

        self.operation_id: Optional[str] = payload.get("operationId")
        self.status: Optional["QueryJobInstance.LongRunningOperationStatus"] = (
            payload.get("status")
        )
        self.status_url: Optional[str] = payload.get("statusUrl")
        self.created_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("createdAt")
        )
        self.completed_at: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("completedAt")
        )
        self.error: Optional[OperationError] = payload.get("error")
        self.result_url: Optional[str] = payload.get("resultUrl")
        self.result_retention_period: Optional[str] = payload.get(
            "resultRetentionPeriod"
        )

        # Only set _solution if path params are provided (not None)
        if operation_id is not None:
            self._solution = {
                "operation_id": operation_id,
            }

        self._context: Optional[QueryJobContext] = None

    @property
    def _proxy(self) -> "QueryJobContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: QueryJobContext for this QueryJobInstance
        """
        if self._context is None:
            self._context = QueryJobContext(
                self._version,
                operation_id=self._solution["operation_id"],
            )
        return self._context

    def fetch(self) -> "QueryJobInstance":
        """
        Fetch the QueryJobInstance


        :returns: The fetched QueryJobInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "QueryJobInstance":
        """
        Asynchronous coroutine to fetch the QueryJobInstance


        :returns: The fetched QueryJobInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the QueryJobInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the QueryJobInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Insights.V3.QueryJobInstance {}>".format(context)


class QueryJobContext(InstanceContext):

    def __init__(self, version: Version, operation_id: str):
        """
        Initialize the QueryJobContext

        :param version: Version that contains the resource
        :param operation_id: The unique identifier for the async query operation (TTID format).
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "operation_id": operation_id,
        }
        self._uri = "/InsightsDomains/Conversations/QueryJobs/{operation_id}".format(
            **self._solution
        )

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> QueryJobInstance:
        """
        Fetch the QueryJobInstance


        :returns: The fetched QueryJobInstance
        """
        payload, _, _ = self._fetch()
        return QueryJobInstance(
            self._version,
            payload,
            operation_id=self._solution["operation_id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the QueryJobInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = QueryJobInstance(
            self._version,
            payload,
            operation_id=self._solution["operation_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> QueryJobInstance:
        """
        Asynchronous coroutine to fetch the QueryJobInstance


        :returns: The fetched QueryJobInstance
        """
        payload, _, _ = await self._fetch_async()
        return QueryJobInstance(
            self._version,
            payload,
            operation_id=self._solution["operation_id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the QueryJobInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = QueryJobInstance(
            self._version,
            payload,
            operation_id=self._solution["operation_id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Insights.V3.QueryJobContext {}>".format(context)


class QueryJobPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> QueryJobInstance:
        """
        Build an instance of QueryJobInstance

        :param payload: Payload response from the API
        """

        return QueryJobInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Insights.V3.QueryJobPage>"


class QueryJobList(ListResource):

    class InsightsQueryRequest(object):
        """
        :ivar domain: The business domain to execute the query against
        :ivar query:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.domain: Optional[str] = payload.get("domain")
            self.query: Optional[QueryList.QueryDefinition] = payload.get("query")

        def to_dict(self):
            return {
                "domain": self.domain,
                "query": self.query.to_dict() if self.query is not None else None,
            }

    class OperationError(object):
        """
        :ivar code: Twilio-specific error code
        :ivar message: A human readable error message
        :ivar http_status_code: HTTP status code that would have been returned for a synchronous failure
        :ivar detail: Additional context about the failure
        """

        def __init__(self, payload: Dict[str, Any]):

            self.code: Optional[int] = payload.get("code")
            self.message: Optional[str] = payload.get("message")
            self.http_status_code: Optional[int] = payload.get("httpStatusCode")
            self.detail: Optional[str] = payload.get("detail")

        def to_dict(self):
            return {
                "code": self.code,
                "message": self.message,
                "httpStatusCode": self.http_status_code,
                "detail": self.detail,
            }

    class QueryDefinition(object):
        """
        :ivar measures: Array of measures to retrieve, representing quantitative values or metrics to be calculated
        :ivar dimensions: Array of dimensions to retrieve, representing categorical attributes for grouping and organizing data
        :ivar filters: Nested filter conditions. Always use `op` and `expressions`.
        :ivar order_by: Specifications for sorting the query results by specific fields in ascending or descending order
        """

        def __init__(self, payload: Dict[str, Any]):

            self.measures: Optional[List[str]] = payload.get("measures")
            self.dimensions: Optional[List[str]] = payload.get("dimensions")
            self.filters: Optional[List[QueryList.QueryDefinitionFilters]] = (
                payload.get("filters")
            )
            self.order_by: Optional[List[QueryList.QueryDefinitionOrderBy]] = (
                payload.get("orderBy")
            )

        def to_dict(self):
            return {
                "measures": self.measures,
                "dimensions": self.dimensions,
                "filters": (
                    [filters.to_dict() for filters in self.filters]
                    if self.filters is not None
                    else None
                ),
                "orderBy": (
                    [order_by.to_dict() for order_by in self.order_by]
                    if self.order_by is not None
                    else None
                ),
            }

    class QueryDefinitionFilters(object):
        """
        :ivar op:
        :ivar expressions:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.op: Optional["QueryInstance.str"] = payload.get("op")
            self.expressions: Optional[
                List[QueryList.QueryDefinitionFiltersExpressions]
            ] = payload.get("expressions")

        def to_dict(self):
            return {
                "op": self.op,
                "expressions": (
                    [expressions.to_dict() for expressions in self.expressions]
                    if self.expressions is not None
                    else None
                ),
            }

    class QueryDefinitionFiltersExpressions(object):
        """
        :ivar op:
        :ivar field:
        :ivar values:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.op: Optional["QueryInstance.str"] = payload.get("op")
            self.field: Optional[str] = payload.get("field")
            self.values: Optional[List[str]] = payload.get("values")

        def to_dict(self):
            return {
                "op": self.op,
                "field": self.field,
                "values": self.values,
            }

    class QueryDefinitionOrderBy(object):
        """
        :ivar field: Dimension or measure to order by
        :ivar direction: Sort order direction, ascending or descending
        """

        def __init__(self, payload: Dict[str, Any]):

            self.field: Optional[str] = payload.get("field")
            self.direction: Optional["QueryInstance.str"] = payload.get("direction")

        def to_dict(self):
            return {
                "field": self.field,
                "direction": self.direction,
            }

    def __init__(self, version: Version):
        """
        Initialize the QueryJobList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/InsightsDomains/Conversations/QueryJobs"

    def _create(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = insights_query_request.to_dict()

        headers = values.of(
            {
                "Idempotency-Key": idempotency_key,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> QueryJobInstance:
        """
        Create the QueryJobInstance

        :param insights_query_request:
        :param idempotency_key: Client-generated UUIDv7 key to ensure idempotent behavior. Submitting the same key for identical requests returns the same operation without re-execution. Scoped to Account + Region. Retained for 24 hours.

        :returns: The created QueryJobInstance
        """
        payload, _, _ = self._create(
            insights_query_request=insights_query_request,
            idempotency_key=idempotency_key,
        )
        return QueryJobInstance(self._version, payload)

    def create_with_http_info(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Create the QueryJobInstance and return response metadata

        :param insights_query_request:
        :param idempotency_key: Client-generated UUIDv7 key to ensure idempotent behavior. Submitting the same key for identical requests returns the same operation without re-execution. Scoped to Account + Region. Retained for 24 hours.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            insights_query_request=insights_query_request,
            idempotency_key=idempotency_key,
        )
        instance = QueryJobInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = insights_query_request.to_dict()

        headers = values.of(
            {
                "Idempotency-Key": idempotency_key,
                "Content-Type": "application/x-www-form-urlencoded",
            }
        )

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> QueryJobInstance:
        """
        Asynchronously create the QueryJobInstance

        :param insights_query_request:
        :param idempotency_key: Client-generated UUIDv7 key to ensure idempotent behavior. Submitting the same key for identical requests returns the same operation without re-execution. Scoped to Account + Region. Retained for 24 hours.

        :returns: The created QueryJobInstance
        """
        payload, _, _ = await self._create_async(
            insights_query_request=insights_query_request,
            idempotency_key=idempotency_key,
        )
        return QueryJobInstance(self._version, payload)

    async def create_with_http_info_async(
        self,
        insights_query_request: InsightsQueryRequest,
        idempotency_key: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the QueryJobInstance and return response metadata

        :param insights_query_request:
        :param idempotency_key: Client-generated UUIDv7 key to ensure idempotent behavior. Submitting the same key for identical requests returns the same operation without re-execution. Scoped to Account + Region. Retained for 24 hours.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            insights_query_request=insights_query_request,
            idempotency_key=idempotency_key,
        )
        instance = QueryJobInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[QueryJobInstance]:
        """
        Streams QueryJobInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(
            page_token=page_token, status=status, page_size=limits["page_size"]
        )

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[QueryJobInstance]:
        """
        Asynchronously streams QueryJobInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token, status=status, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams QueryJobInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token, status=status, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams QueryJobInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token, status=status, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[QueryJobInstance]:
        """
        Lists QueryJobInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return list(
            self.stream(
                page_token=page_token,
                status=status,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[QueryJobInstance]:
        """
        Asynchronously lists QueryJobInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return [
            record
            async for record in await self.stream_async(
                page_token=page_token,
                status=status,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists QueryJobInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            status=status,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists QueryJobInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param LongRunningOperationStatus status: Filter by operation status.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            status=status,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
    ) -> QueryJobPage:
        """
        Retrieve a single page of QueryJobInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of resources to return
        :param page_token: Token for pagination
        :param status: Filter by operation status.
        :returns: Page of QueryJobInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "status": status,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return QueryJobPage(self._version, response, uri=self._uri, params=data)

    async def page_async(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        status: Union[LongRunningOperationStatus, object] = values.unset,
    ) -> QueryJobPage:
        """
        Asynchronously retrieve a single page of QueryJobInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of resources to return
        :param page_token: Token for pagination
        :param status: Filter by operation status.
        :returns: Page of QueryJobInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
                "status": status,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return QueryJobPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(
        self,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: Token for pagination
        :param status: Filter by operation status.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with QueryJobPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = QueryJobPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        status: Union[LongRunningOperationStatus, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: Token for pagination
        :param status: Filter by operation status.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with QueryJobPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = QueryJobPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> QueryJobPage:
        """
        Retrieve a specific page of QueryJobInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of QueryJobInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return QueryJobPage(self._version, response)

    async def get_page_async(self, target_url: str) -> QueryJobPage:
        """
        Asynchronously retrieve a specific page of QueryJobInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of QueryJobInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return QueryJobPage(self._version, response)

    def get(self, operation_id: str) -> QueryJobContext:
        """
        Constructs a QueryJobContext

        :param operation_id: The unique identifier for the async query operation (TTID format).
        """
        return QueryJobContext(self._version, operation_id=operation_id)

    def __call__(self, operation_id: str) -> QueryJobContext:
        """
        Constructs a QueryJobContext

        :param operation_id: The unique identifier for the async query operation (TTID format).
        """
        return QueryJobContext(self._version, operation_id=operation_id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Insights.V3.QueryJobList>"
