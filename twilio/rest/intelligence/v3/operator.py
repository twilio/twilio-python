r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio Conversational Intelligence API
    The Conversational Intelligence API includes resources to create and manage intelligence configurations, define and run language operators, and retrieve processed conversations and operator results.  * Use the Configurations resource to create and manage intelligence configurations and define rules that control how and when language operators analyze and transform conversations.  * Use the Operators resource to create custom language operators or retrieve Twilio-author and custom operators. * Use the Conversations resource to retrieve conversations processed with an intelligence configuration, and the OperatorResults resource to retrieve language operator results for those conversations. 

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from __future__ import annotations

from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.token_pagination import TokenPagination


class OperatorInstance(InstanceResource):

    class OperatorAuthor(object):
        SELF = "SELF"
        TWILIO = "TWILIO"

    class OperatorOutputFormat(object):
        TEXT = "TEXT"
        JSON = "JSON"
        CLASSIFICATION = "CLASSIFICATION"

    """
    :ivar code: Twilio-specific error code
    :ivar message: A human readable error message
    :ivar http_status_code: HTTP response status code
    :ivar user_error: Whether the error is a user error (true) or a system error (false)
    :ivar params: A map of parameters related to the error, for example, a `params.twilioErrorCodeUrl` might hold a URL or link to additional information
    :ivar id: The unique identifier for the Language Operator. Assigned by Twilio (TTID).
    :ivar display_name: Display name of the Language Operator describing its purpose.
    :ivar description: Description of the Language Operator further explaining its purpose.
    :ivar version: Numeric Operator version. Automatically incremented with each update on the resource, used to ensure integrity when updating the Operator.
    :ivar author: 
    :ivar prompt: The natural language instructions used by the operator to analyze the conversation.  Within the prompt, users can reference parameters using the `{{parameters.[param_name]}}` syntax. Parameter values are provided to the Operator by the Intelligence Configuration Rule at runtime.  **Note**: Prompts will only be exposed for Custom Operators (`author` = `SELF`). Twilio-authored Operators (`author` = `TWILIO`) will have their prompts omitted from the API.
    :ivar output_format: 
    :ivar output_schema: Required for `JSON` output only. this will be set to a JSON Schema object describing the properties & data types of the response. Please see https://platform.openai.com/docs/guides/structured-outputs#supported-schemas   Will include the following keywords: - `type` : Must be set to `object` - `properties`:  An object containing the property names and their data types you would like the LLM to return  Additional details on JSON output formatting: - The root level `type` of a JSON schema must be set to `object` - The following property data types are supported : `string`, `number`, `boolean`, `integer`, `object`, `array`, `anyOf` - Definitions with `$defs` / `$ref` are supported - Max 100 object properties and 10 levels of nesting are supported - Max 1000 enum values across all enum properties are supported - Notable JSON Schema keywords not supported include:   - For `strings`: `minLength`, `maxLength`   - For `objects`: `patternProperties`, `unevaluatedProperties`, `propertyNames`, `minProperties`, `maxProperties`   - For `arrays`: `unevaluatedItems`, `contains`, `minContains`, `maxContains`, `uniqueItems` - Structured Operator Results will be returned in the same order as the ordering of keys in the schema - In the event an Operator execution request is refused for safety reasons the Operator Result API response will include a new field called `refusal` to indicate that the LLM refused to fulfill the request - Twilio will automatically set `additionalProperties` to false and specify all provided fields as required (constraints of Structured Outputs). You don't need to pass these fields as part of your JSON schema. Twilio will automatically overwrite any user-provided values for these fields.
    :ivar training_examples: An array of example input/output pairs used to illustrate the intended behavior of the Language Operator. These examples help guide the model's understanding of expected input–output relationships and improve consistency during evaluation and testing.  **Note**: Training examples will only be exposed for Custom Operators (`author` = `SELF`). Twilio-authored Operators (`author` = `TWILIO`) will have their training examples omitted from the API.
    :ivar context: 
    :ivar parameters: Defines the schema of the parameters that are provided when running the operator, including required and optional values that determine the operator's behavior. The values of the parameters themselves are passed in by the attached Intelligence Configuration.
    """

    def __init__(
        self, version: Version, payload: ResponseResource, id: Optional[str] = None
    ):
        super().__init__(version)

        self.code: Optional[int] = payload.get("code")
        self.message: Optional[str] = payload.get("message")
        self.http_status_code: Optional[int] = payload.get("httpStatusCode")
        self.user_error: Optional[bool] = payload.get("userError")
        self.params: Optional[Dict[str, str]] = payload.get("params")
        self.id: Optional[str] = payload.get("id")
        self.display_name: Optional[str] = payload.get("displayName")
        self.description: Optional[str] = payload.get("description")
        self.version: Optional[int] = deserialize.integer(payload.get("version"))
        self.author: Optional["OperatorInstance.OperatorAuthor"] = payload.get("author")
        self.prompt: Optional[str] = payload.get("prompt")
        self.output_format: Optional["OperatorInstance.str"] = payload.get(
            "outputFormat"
        )
        self.output_schema: Optional[Dict[str, object]] = payload.get("outputSchema")
        self.training_examples: Optional[List[str]] = payload.get("trainingExamples")
        self.context: Optional[str] = payload.get("context")
        self.parameters: Optional[Dict[str, str]] = payload.get("parameters")

        # Only set _solution if path params are provided (not None)
        if id is not None:
            self._solution = {
                "id": id,
            }

        self._context: Optional[OperatorContext] = None

    @property
    def _proxy(self) -> "OperatorContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: OperatorContext for this OperatorInstance
        """
        if self._context is None:
            self._context = OperatorContext(
                self._version,
                id=self._solution["id"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the OperatorInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the OperatorInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the OperatorInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the OperatorInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()

    def fetch(self) -> "OperatorInstance":
        """
        Fetch the OperatorInstance


        :returns: The fetched OperatorInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "OperatorInstance":
        """
        Asynchronous coroutine to fetch the OperatorInstance


        :returns: The fetched OperatorInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the OperatorInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the OperatorInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> "OperatorInstance":
        """
        Update the OperatorInstance

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: The updated OperatorInstance
        """
        return self._proxy.update(
            language_operator=language_operator,
            if_match=if_match,
        )

    async def update_async(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> "OperatorInstance":
        """
        Asynchronous coroutine to update the OperatorInstance

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: The updated OperatorInstance
        """
        return await self._proxy.update_async(
            language_operator=language_operator,
            if_match=if_match,
        )

    def update_with_http_info(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the OperatorInstance with HTTP info

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            language_operator=language_operator,
            if_match=if_match,
        )

    async def update_with_http_info_async(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the OperatorInstance with HTTP info

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            language_operator=language_operator,
            if_match=if_match,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Intelligence.V3.OperatorInstance {}>".format(context)


class OperatorContext(InstanceContext):

    def __init__(self, version: Version, id: str):
        """
        Initialize the OperatorContext

        :param version: Version that contains the resource
        :param id: The unique identifier (TTID) of the Language Operator.
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "id": id,
        }
        self._uri = "/ControlPlane/Operators/{id}".format(**self._solution)

    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self) -> bool:
        """
        Deletes the OperatorInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the OperatorInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the OperatorInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the OperatorInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> OperatorInstance:
        """
        Fetch the OperatorInstance


        :returns: The fetched OperatorInstance
        """
        payload, _, _ = self._fetch()
        return OperatorInstance(
            self._version,
            payload,
            id=self._solution["id"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the OperatorInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = OperatorInstance(
            self._version,
            payload,
            id=self._solution["id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> OperatorInstance:
        """
        Asynchronous coroutine to fetch the OperatorInstance


        :returns: The fetched OperatorInstance
        """
        payload, _, _ = await self._fetch_async()
        return OperatorInstance(
            self._version,
            payload,
            id=self._solution["id"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the OperatorInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = OperatorInstance(
            self._version,
            payload,
            id=self._solution["id"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = language_operator.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> OperatorInstance:
        """
        Update the OperatorInstance

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: The updated OperatorInstance
        """
        payload, _, _ = self._update(
            language_operator=language_operator, if_match=if_match
        )
        return OperatorInstance(self._version, payload, id=self._solution["id"])

    def update_with_http_info(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the OperatorInstance and return response metadata

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            language_operator=language_operator, if_match=if_match
        )
        instance = OperatorInstance(self._version, payload, id=self._solution["id"])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = language_operator.to_dict()

        headers = values.of({})

        if not (
            if_match is values.unset or (isinstance(if_match, str) and not if_match)
        ):
            headers["If-Match"] = if_match

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> OperatorInstance:
        """
        Asynchronous coroutine to update the OperatorInstance

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: The updated OperatorInstance
        """
        payload, _, _ = await self._update_async(
            language_operator=language_operator, if_match=if_match
        )
        return OperatorInstance(self._version, payload, id=self._solution["id"])

    async def update_with_http_info_async(
        self,
        language_operator: LanguageOperator,
        if_match: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the OperatorInstance and return response metadata

        :param language_operator: Full replacement of a Language Operator configuration
        :param if_match: Current ETag/version required for conditional update

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            language_operator=language_operator, if_match=if_match
        )
        instance = OperatorInstance(self._version, payload, id=self._solution["id"])
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Intelligence.V3.OperatorContext {}>".format(context)


class OperatorPage(TokenPagination):

    def get_instance(self, payload: Dict[str, Any]) -> OperatorInstance:
        """
        Build an instance of OperatorInstance

        :param payload: Payload response from the API
        """

        return OperatorInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Intelligence.V3.OperatorPage>"


class OperatorList(ListResource):

    class LanguageOperator(object):
        """
        :ivar id: The unique identifier for the Language Operator. Assigned by Twilio (TTID).
        :ivar display_name: Display name of the Language Operator describing its purpose.
        :ivar description: Description of the Language Operator further explaining its purpose.
        :ivar version: Numeric Operator version. Automatically incremented with each update on the resource, used to ensure integrity when updating the Operator.
        :ivar author:
        :ivar prompt: The natural language instructions used by the operator to analyze the conversation.  Within the prompt, users can reference parameters using the `{{parameters.[param_name]}}` syntax. Parameter values are provided to the Operator by the Intelligence Configuration Rule at runtime.  **Note**: Prompts will only be exposed for Custom Operators (`author` = `SELF`). Twilio-authored Operators (`author` = `TWILIO`) will have their prompts omitted from the API.
        :ivar output_format:
        :ivar output_schema: Required for `JSON` output only. this will be set to a JSON Schema object describing the properties & data types of the response. Please see https://platform.openai.com/docs/guides/structured-outputs#supported-schemas   Will include the following keywords: - `type` : Must be set to `object` - `properties`:  An object containing the property names and their data types you would like the LLM to return  Additional details on JSON output formatting: - The root level `type` of a JSON schema must be set to `object` - The following property data types are supported : `string`, `number`, `boolean`, `integer`, `object`, `array`, `anyOf` - Definitions with `$defs` / `$ref` are supported - Max 100 object properties and 10 levels of nesting are supported - Max 1000 enum values across all enum properties are supported - Notable JSON Schema keywords not supported include:   - For `strings`: `minLength`, `maxLength`   - For `objects`: `patternProperties`, `unevaluatedProperties`, `propertyNames`, `minProperties`, `maxProperties`   - For `arrays`: `unevaluatedItems`, `contains`, `minContains`, `maxContains`, `uniqueItems` - Structured Operator Results will be returned in the same order as the ordering of keys in the schema - In the event an Operator execution request is refused for safety reasons the Operator Result API response will include a new field called `refusal` to indicate that the LLM refused to fulfill the request - Twilio will automatically set `additionalProperties` to false and specify all provided fields as required (constraints of Structured Outputs). You don't need to pass these fields as part of your JSON schema. Twilio will automatically overwrite any user-provided values for these fields.
        :ivar training_examples: An array of example input/output pairs used to illustrate the intended behavior of the Language Operator. These examples help guide the model's understanding of expected input–output relationships and improve consistency during evaluation and testing.  **Note**: Training examples will only be exposed for Custom Operators (`author` = `SELF`). Twilio-authored Operators (`author` = `TWILIO`) will have their training examples omitted from the API.
        :ivar context:
        :ivar parameters: Defines the schema of the parameters that are provided when running the operator, including required and optional values that determine the operator's behavior. The values of the parameters themselves are passed in by the attached Intelligence Configuration.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.id: Optional[str] = payload.get("id")
            self.display_name: Optional[str] = payload.get("displayName")
            self.description: Optional[str] = payload.get("description")
            self.version: Optional[int] = deserialize.integer(payload.get("version"))
            self.author: Optional[OperatorAuthorEnum] = payload.get("author")
            self.prompt: Optional[str] = payload.get("prompt")
            self.output_format: Optional["OperatorInstance.OperatorOutputFormat"] = (
                payload.get("outputFormat")
            )
            self.output_schema: Optional[Dict[str, object]] = payload.get(
                "outputSchema"
            )
            self.training_examples: Optional[
                List[OperatorList.OperatorTrainingExample]
            ] = payload.get("trainingExamples")
            self.context: Optional[OperatorList.OperatorContext] = payload.get(
                "context"
            )
            self.parameters: Optional[Dict[str, OperatorParameter]] = payload.get(
                "parameters"
            )

        def to_dict(self):
            return {
                "id": self.id,
                "displayName": self.display_name,
                "description": self.description,
                "version": self.version,
                "author": self.author,
                "prompt": self.prompt,
                "outputFormat": self.output_format,
                "outputSchema": self.output_schema,
                "trainingExamples": (
                    [
                        training_examples.to_dict()
                        for training_examples in self.training_examples
                    ]
                    if self.training_examples is not None
                    else None
                ),
                "context": self.context.to_dict() if self.context is not None else None,
                "parameters": (
                    {k: v.to_dict() for k, v in self.parameters.items()}
                    if self.parameters is not None
                    else None
                ),
            }

    class OperatorContext(object):
        """
        :ivar memory:
        :ivar knowledge:
        """

        def __init__(self, payload: Dict[str, Any]):

            self.memory: Optional[OperatorList.OperatorContextMemory] = payload.get(
                "memory"
            )
            self.knowledge: Optional[OperatorList.OperatorContextKnowledge] = (
                payload.get("knowledge")
            )

        def to_dict(self):
            return {
                "memory": self.memory.to_dict() if self.memory is not None else None,
                "knowledge": (
                    self.knowledge.to_dict() if self.knowledge is not None else None
                ),
            }

    class OperatorContextKnowledge(object):
        """
        :ivar enabled: Set to true to allow access to Knowledge Sources at runtime.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "enabled": self.enabled,
            }

    class OperatorContextMemory(object):
        """
        :ivar enabled: Set to true to allow access to Memory at runtime.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "enabled": self.enabled,
            }

    class OperatorTrainingExample(object):
        """
        :ivar input: A sample input text that demonstrates the type of content the Operator processes.
        :ivar output: The expected output corresponding to the provided input example. This value must be consistent with the defined `output_format` and `output_schema` of the Operator.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.input: Optional[str] = payload.get("input")
            self.output: Optional[str] = payload.get("output")

        def to_dict(self):
            return {
                "input": self.input,
                "output": self.output,
            }

    def __init__(self, version: Version):
        """
        Initialize the OperatorList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/ControlPlane/Operators"

    def _create(self, language_operator: LanguageOperator) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = language_operator.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(self, language_operator: LanguageOperator) -> OperatorInstance:
        """
        Create the OperatorInstance

        :param language_operator: Create Language Operator request

        :returns: The created OperatorInstance
        """
        payload, _, _ = self._create(language_operator=language_operator)
        return OperatorInstance(self._version, payload)

    def create_with_http_info(self, language_operator: LanguageOperator) -> ApiResponse:
        """
        Create the OperatorInstance and return response metadata

        :param language_operator: Create Language Operator request

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            language_operator=language_operator
        )
        instance = OperatorInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(self, language_operator: LanguageOperator) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """
        data = language_operator.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self, language_operator: LanguageOperator
    ) -> OperatorInstance:
        """
        Asynchronously create the OperatorInstance

        :param language_operator: Create Language Operator request

        :returns: The created OperatorInstance
        """
        payload, _, _ = await self._create_async(language_operator=language_operator)
        return OperatorInstance(self._version, payload)

    async def create_with_http_info_async(
        self, language_operator: LanguageOperator
    ) -> ApiResponse:
        """
        Asynchronously create the OperatorInstance and return response metadata

        :param language_operator: Create Language Operator request

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            language_operator=language_operator
        )
        instance = OperatorInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[OperatorInstance]:
        """
        Streams OperatorInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(page_token=page_token, page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[OperatorInstance]:
        """
        Asynchronously streams OperatorInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            page_token=page_token, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams OperatorInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            page_token=page_token, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams OperatorInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_token=page_token, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[OperatorInstance]:
        """
        Lists OperatorInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return list(
            self.stream(
                page_token=page_token,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[OperatorInstance]:
        """
        Asynchronously lists OperatorInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """

        return [
            record
            async for record in await self.stream_async(
                page_token=page_token,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists OperatorInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists OperatorInstance and returns headers from first page


        :param str page_token: Token for pagination
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            page_token=page_token,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
    ) -> OperatorPage:
        """
        Retrieve a single page of OperatorInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of resources to return
        :param page_token: Token for pagination
        :returns: Page of OperatorInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return OperatorPage(self._version, response, uri=self._uri, params=data)

    async def page_async(
        self,
        page_size: Union[int, object] = values.unset,
        page_token: Union[str, object] = values.unset,
    ) -> OperatorPage:
        """
        Asynchronously retrieve a single page of OperatorInstance records from the API.
        Request is executed immediately

        :param page_size: The maximum number of resources to return
        :param page_token: Token for pagination
        :returns: Page of OperatorInstance
        """
        data = values.of(
            {
                "pageSize": page_size,
                "pageToken": page_token,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return OperatorPage(self._version, response, uri=self._uri, params=data)

    def page_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: Token for pagination
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with OperatorPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = OperatorPage(self._version, response, uri=self._uri)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: Token for pagination
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with OperatorPage, status code, and headers
        """
        data = values.of(
            {
                "pageToken": page_token,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = (
            await self._version.page_with_response_info_async(
                method="GET", uri=self._uri, params=data, headers=headers
            )
        )
        page = OperatorPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> OperatorPage:
        """
        Retrieve a specific page of OperatorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of OperatorInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return OperatorPage(self._version, response)

    async def get_page_async(self, target_url: str) -> OperatorPage:
        """
        Asynchronously retrieve a specific page of OperatorInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of OperatorInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return OperatorPage(self._version, response)

    def get(self, id: str) -> OperatorContext:
        """
        Constructs a OperatorContext

        :param id: The unique identifier (TTID) of the Language Operator.
        """
        return OperatorContext(self._version, id=id)

    def __call__(self, id: str) -> OperatorContext:
        """
        Constructs a OperatorContext

        :param id: The unique identifier (TTID) of the Language Operator.
        """
        return OperatorContext(self._version, id=id)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Intelligence.V3.OperatorList>"
