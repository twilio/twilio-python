r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Alarms
    Manages Twilio Alarms

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""

from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page


class AlarmInstance(InstanceResource):

    class MonitorV2AlarmCreateObject(object):
        """
        :ivar friendly_name: Friendly name for alarm
        :ivar query_type:
        :ivar query: Value to query for
        :ivar trigger_value: Threshold to send customer alarm notification
        :ivar time_window:
        :ivar email: Email notifications to send
        :ivar webhook: Webhook notification to send
        :ivar console_indicator: Whether to send console notifications
        :ivar description: Description for the alarm.
        :ivar enabled: Is alarm enbled? Default is true.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.query_type: Optional["AlarmInstance.QueryType"] = payload.get(
                "query_type"
            )
            self.query: Optional[str] = payload.get("query")
            self.trigger_value: Optional[int] = payload.get("trigger_value")
            self.time_window: Optional["AlarmInstance.TimeWindow"] = payload.get(
                "time_window"
            )
            self.email: Optional[List[str]] = payload.get("email")
            self.webhook: Optional[str] = payload.get("webhook")
            self.console_indicator: Optional[bool] = payload.get("console_indicator")
            self.description: Optional[str] = payload.get("description")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "friendly_name": self.friendly_name,
                "query_type": self.query_type,
                "query": self.query,
                "trigger_value": self.trigger_value,
                "time_window": self.time_window,
                "email": self.email,
                "webhook": self.webhook,
                "console_indicator": self.console_indicator,
                "description": self.description,
                "enabled": self.enabled,
            }

    class QueryType(object):
        ERROR_CODE = "ERROR_CODE"
        LOG_LEVEL = "LOG_LEVEL"
        ALL = "ALL"

    class TimeWindow(object):
        FIVE_MINS = "FIVE_MINS"
        FIFTEEN_MINS = "FIFTEEN_MINS"
        ONE_HOUR = "ONE_HOUR"
        TWELVE_HOURS = "TWELVE_HOURS"
        ONE_DAY = "ONE_DAY"

    """
    :ivar sid: 
    :ivar message: Error message
    :ivar code: Twilio error code
    :ivar user_error: Indicates if it was a user error
    :ivar http_status_code: Http error code returned
    :ivar params: 
    :ivar more_info: More information link
    :ivar status: HTTP status code
    :ivar friendly_name: Friendly name for alarm
    :ivar query_type: 
    :ivar query: Value to query for
    :ivar trigger_value: Threshold to send customer alarm notification
    :ivar time_window: 
    :ivar email: Email notifications to send
    :ivar webhook: Webhook notification to send
    :ivar console_indicator: Whether to send console notifications
    :ivar description: Description for the alarm.
    :ivar enabled: Is alarm enbled? Default is true.
    :ivar product: Product the alarm is related to.
    :ivar alert_config_sid: Sid for the alarm
    :ivar date_updated: The date and time in UTC when the alarm was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format
    :ivar date_created: The date and time in UTC when the alarm was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    """

    def __init__(
        self, version: Version, payload: Dict[str, Any], sid: Optional[str] = None
    ):
        super().__init__(version)

        self.sid: Optional[str] = payload.get("sid")
        self.message: Optional[str] = payload.get("message")
        self.code: Optional[int] = deserialize.integer(payload.get("code"))
        self.user_error: Optional[bool] = payload.get("user_error")
        self.http_status_code: Optional[int] = deserialize.integer(
            payload.get("http_status_code")
        )
        self.params: Optional[Dict[str, str]] = payload.get("params")
        self.more_info: Optional[str] = payload.get("more_info")
        self.status: Optional[int] = deserialize.integer(payload.get("status"))
        self.friendly_name: Optional[str] = payload.get("friendlyName")
        self.query_type: Optional[str] = payload.get("queryType")
        self.query: Optional[str] = payload.get("query")
        self.trigger_value: Optional[int] = deserialize.integer(
            payload.get("triggerValue")
        )
        self.time_window: Optional[str] = payload.get("timeWindow")
        self.email: Optional[List[str]] = payload.get("email")
        self.webhook: Optional[str] = payload.get("webhook")
        self.console_indicator: Optional[bool] = payload.get("consoleIndicator")
        self.description: Optional[str] = payload.get("description")
        self.enabled: Optional[bool] = payload.get("enabled")
        self.product: Optional[str] = payload.get("product")
        self.alert_config_sid: Optional[str] = payload.get("alertConfigSid")
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("dateUpdated")
        )
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("dateCreated")
        )

        self._solution = {
            "sid": sid or self.sid,
        }
        self._context: Optional[AlarmContext] = None

    @property
    def _proxy(self) -> "AlarmContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AlarmContext for this AlarmInstance
        """
        if self._context is None:
            self._context = AlarmContext(
                self._version,
                sid=self._solution["sid"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the AlarmInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AlarmInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def fetch(self) -> "AlarmInstance":
        """
        Fetch the AlarmInstance


        :returns: The fetched AlarmInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "AlarmInstance":
        """
        Asynchronous coroutine to fetch the AlarmInstance


        :returns: The fetched AlarmInstance
        """
        return await self._proxy.fetch_async()

    def update(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> "AlarmInstance":
        """
        Update the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The updated AlarmInstance
        """
        return self._proxy.update(
            monitor_v2_alarm_create_object=monitor_v2_alarm_create_object,
        )

    async def update_async(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> "AlarmInstance":
        """
        Asynchronous coroutine to update the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The updated AlarmInstance
        """
        return await self._proxy.update_async(
            monitor_v2_alarm_create_object=monitor_v2_alarm_create_object,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Monitor.V2.AlarmInstance {}>".format(context)


class AlarmContext(InstanceContext):

    class MonitorV2AlarmCreateObject(object):
        """
        :ivar friendly_name: Friendly name for alarm
        :ivar query_type:
        :ivar query: Value to query for
        :ivar trigger_value: Threshold to send customer alarm notification
        :ivar time_window:
        :ivar email: Email notifications to send
        :ivar webhook: Webhook notification to send
        :ivar console_indicator: Whether to send console notifications
        :ivar description: Description for the alarm.
        :ivar enabled: Is alarm enbled? Default is true.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.query_type: Optional["AlarmInstance.QueryType"] = payload.get(
                "query_type"
            )
            self.query: Optional[str] = payload.get("query")
            self.trigger_value: Optional[int] = payload.get("trigger_value")
            self.time_window: Optional["AlarmInstance.TimeWindow"] = payload.get(
                "time_window"
            )
            self.email: Optional[List[str]] = payload.get("email")
            self.webhook: Optional[str] = payload.get("webhook")
            self.console_indicator: Optional[bool] = payload.get("console_indicator")
            self.description: Optional[str] = payload.get("description")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "friendly_name": self.friendly_name,
                "query_type": self.query_type,
                "query": self.query,
                "trigger_value": self.trigger_value,
                "time_window": self.time_window,
                "email": self.email,
                "webhook": self.webhook,
                "console_indicator": self.console_indicator,
                "description": self.description,
                "enabled": self.enabled,
            }

    def __init__(self, version: Version, sid: str):
        """
        Initialize the AlarmContext

        :param version: Version that contains the resource
        :param sid: Sid
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "sid": sid,
        }
        self._uri = "/Alarms/{sid}".format(**self._solution)

    def delete(self) -> bool:
        """
        Deletes the AlarmInstance


        :returns: True if delete succeeds, False otherwise
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.delete(method="DELETE", uri=self._uri, headers=headers)

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AlarmInstance


        :returns: True if delete succeeds, False otherwise
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.delete_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    def fetch(self) -> AlarmInstance:
        """
        Fetch the AlarmInstance


        :returns: The fetched AlarmInstance
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        payload = self._version.fetch(method="GET", uri=self._uri, headers=headers)

        return AlarmInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )

    async def fetch_async(self) -> AlarmInstance:
        """
        Asynchronous coroutine to fetch the AlarmInstance


        :returns: The fetched AlarmInstance
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        payload = await self._version.fetch_async(
            method="GET", uri=self._uri, headers=headers
        )

        return AlarmInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )

    def update(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> AlarmInstance:
        """
        Update the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The updated AlarmInstance
        """
        data = monitor_v2_alarm_create_object.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        payload = self._version.update(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

        return AlarmInstance(self._version, payload, sid=self._solution["sid"])

    async def update_async(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> AlarmInstance:
        """
        Asynchronous coroutine to update the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The updated AlarmInstance
        """
        data = monitor_v2_alarm_create_object.to_dict()

        headers = values.of({})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        payload = await self._version.update_async(
            method="PUT", uri=self._uri, data=data, headers=headers
        )

        return AlarmInstance(self._version, payload, sid=self._solution["sid"])

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Monitor.V2.AlarmContext {}>".format(context)


class AlarmPage(Page):

    def get_instance(self, payload: Dict[str, Any]) -> AlarmInstance:
        """
        Build an instance of AlarmInstance

        :param payload: Payload response from the API
        """
        return AlarmInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Monitor.V2.AlarmPage>"


class AlarmList(ListResource):

    class MonitorV2AlarmCreateObject(object):
        """
        :ivar friendly_name: Friendly name for alarm
        :ivar query_type:
        :ivar query: Value to query for
        :ivar trigger_value: Threshold to send customer alarm notification
        :ivar time_window:
        :ivar email: Email notifications to send
        :ivar webhook: Webhook notification to send
        :ivar console_indicator: Whether to send console notifications
        :ivar description: Description for the alarm.
        :ivar enabled: Is alarm enbled? Default is true.
        """

        def __init__(self, payload: Dict[str, Any]):

            self.friendly_name: Optional[str] = payload.get("friendly_name")
            self.query_type: Optional["AlarmInstance.QueryType"] = payload.get(
                "query_type"
            )
            self.query: Optional[str] = payload.get("query")
            self.trigger_value: Optional[int] = payload.get("trigger_value")
            self.time_window: Optional["AlarmInstance.TimeWindow"] = payload.get(
                "time_window"
            )
            self.email: Optional[List[str]] = payload.get("email")
            self.webhook: Optional[str] = payload.get("webhook")
            self.console_indicator: Optional[bool] = payload.get("console_indicator")
            self.description: Optional[str] = payload.get("description")
            self.enabled: Optional[bool] = payload.get("enabled")

        def to_dict(self):
            return {
                "friendly_name": self.friendly_name,
                "query_type": self.query_type,
                "query": self.query,
                "trigger_value": self.trigger_value,
                "time_window": self.time_window,
                "email": self.email,
                "webhook": self.webhook,
                "console_indicator": self.console_indicator,
                "description": self.description,
                "enabled": self.enabled,
            }

    def __init__(self, version: Version):
        """
        Initialize the AlarmList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/Alarms"

    def create(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> AlarmInstance:
        """
        Create the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The created AlarmInstance
        """
        data = monitor_v2_alarm_create_object.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        payload = self._version.create(
            method="POST", uri=self._uri, data=data, headers=headers
        )

        return AlarmInstance(self._version, payload)

    async def create_async(
        self, monitor_v2_alarm_create_object: MonitorV2AlarmCreateObject
    ) -> AlarmInstance:
        """
        Asynchronously create the AlarmInstance

        :param monitor_v2_alarm_create_object:

        :returns: The created AlarmInstance
        """
        data = monitor_v2_alarm_create_object.to_dict()

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        payload = await self._version.create_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

        return AlarmInstance(self._version, payload)

    def stream(
        self,
        sort_order: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[AlarmInstance]:
        """
        Streams AlarmInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str sort_order:
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(sort_order=sort_order, page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        sort_order: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[AlarmInstance]:
        """
        Asynchronously streams AlarmInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str sort_order:
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            sort_order=sort_order, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def list(
        self,
        sort_order: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AlarmInstance]:
        """
        Lists AlarmInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str sort_order:
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                sort_order=sort_order,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        sort_order: Union[str, object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AlarmInstance]:
        """
        Asynchronously lists AlarmInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str sort_order:
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                sort_order=sort_order,
                limit=limit,
                page_size=page_size,
            )
        ]

    def page(
        self,
        sort_order: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AlarmPage:
        """
        Retrieve a single page of AlarmInstance records from the API.
        Request is executed immediately

        :param sort_order:
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AlarmInstance
        """
        data = values.of(
            {
                "SortOrder": sort_order,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return AlarmPage(self._version, response)

    async def page_async(
        self,
        sort_order: Union[str, object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AlarmPage:
        """
        Asynchronously retrieve a single page of AlarmInstance records from the API.
        Request is executed immediately

        :param sort_order:
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AlarmInstance
        """
        data = values.of(
            {
                "SortOrder": sort_order,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return AlarmPage(self._version, response)

    def get_page(self, target_url: str) -> AlarmPage:
        """
        Retrieve a specific page of AlarmInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AlarmInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return AlarmPage(self._version, response)

    async def get_page_async(self, target_url: str) -> AlarmPage:
        """
        Asynchronously retrieve a specific page of AlarmInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AlarmInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return AlarmPage(self._version, response)

    def get(self, sid: str) -> AlarmContext:
        """
        Constructs a AlarmContext

        :param sid: Sid
        """
        return AlarmContext(self._version, sid=sid)

    def __call__(self, sid: str) -> AlarmContext:
        """
        Constructs a AlarmContext

        :param sid: Sid
        """
        return AlarmContext(self._version, sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Monitor.V2.AlarmList>"
