r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Api
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page


class TwimlSessionInstance(InstanceResource):

    """
    :ivar account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TwiML Session resource.
    :ivar call_sid: The SID of the [Call](https://www.twilio.com/docs/voice/api/call-resource) the TwiML Session resource is associated with.
    :ivar sid: The unique string that we created to identify the TwiML Session resource.
    :ivar date_updated: The date and time in GMT that the resource was last updated, specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
    :ivar date_created: The date and time in GMT that the resource was created, specified in [RFC 2822](https://www.ietf.org/rfc/rfc2822.txt) format.
    :ivar session_input: The input direction provided to the TwiML Session.
    :ivar session_output: The output direction generated by the TwiML Session.
    :ivar name: The name of the TwiML Session resource.
    :ivar status: The status of the TwiML Session. Possible values include `in-progress` or `completed`.
    """

    def __init__(
        self,
        version: Version,
        payload: Dict[str, Any],
        account_sid: Optional[str] = None,
        call_sid: Optional[str] = None,
        sid: Optional[str] = None,
    ):
        super().__init__(version)

        self.account_sid: Optional[str] = payload.get("account_sid")
        self.call_sid: Optional[str] = payload.get("call_sid")
        self.sid: Optional[str] = payload.get("sid")
        self.date_updated: Optional[datetime] = deserialize.rfc2822_datetime(
            payload.get("date_updated")
        )
        self.date_created: Optional[datetime] = deserialize.rfc2822_datetime(
            payload.get("date_created")
        )
        self.session_input: Optional[str] = payload.get("session_input")
        self.session_output: Optional[str] = payload.get("session_output")
        self.name: Optional[str] = payload.get("name")
        self.status: Optional[str] = payload.get("status")

        self._solution = {
            "account_sid": account_sid or self.account_sid,
            "call_sid": call_sid or self.call_sid,
            "sid": sid or self.sid,
        }
        self._context: Optional[TwimlSessionContext] = None

    @property
    def _proxy(self) -> "TwimlSessionContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: TwimlSessionContext for this TwimlSessionInstance
        """
        if self._context is None:
            self._context = TwimlSessionContext(
                self._version,
                account_sid=self._solution["account_sid"],
                call_sid=self._solution["call_sid"],
                sid=self._solution["sid"],
            )
        return self._context

    def fetch(self) -> "TwimlSessionInstance":
        """
        Fetch the TwimlSessionInstance


        :returns: The fetched TwimlSessionInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "TwimlSessionInstance":
        """
        Asynchronous coroutine to fetch the TwimlSessionInstance


        :returns: The fetched TwimlSessionInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the TwimlSessionInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the TwimlSessionInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> "TwimlSessionInstance":
        """
        Update the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: The updated TwimlSessionInstance
        """
        return self._proxy.update(
            url=url,
            method=method,
            twiml=twiml,
            status=status,
        )

    async def update_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> "TwimlSessionInstance":
        """
        Asynchronous coroutine to update the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: The updated TwimlSessionInstance
        """
        return await self._proxy.update_async(
            url=url,
            method=method,
            twiml=twiml,
            status=status,
        )

    def update_with_http_info(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the TwimlSessionInstance with HTTP info

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            url=url,
            method=method,
            twiml=twiml,
            status=status,
        )

    async def update_with_http_info_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TwimlSessionInstance with HTTP info

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            url=url,
            method=method,
            twiml=twiml,
            status=status,
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Api.V2010.TwimlSessionInstance {}>".format(context)


class TwimlSessionContext(InstanceContext):
    def __init__(self, version: Version, account_sid: str, call_sid: str, sid: str):
        """
        Initialize the TwimlSessionContext

        :param version: Version that contains the resource
        :param account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TwiML Session resource to update.
        :param call_sid: The SID of the [Call](https://www.twilio.com/docs/voice/api/call-resource) associated with the TwiML Session resource to update.
        :param sid: The unique string that identifies the TwiML Session resource to update.
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "account_sid": account_sid,
            "call_sid": call_sid,
            "sid": sid,
        }
        self._uri = (
            "/Accounts/{account_sid}/Calls/{call_sid}/TwimlSessions/{sid}.json".format(
                **self._solution
            )
        )

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> TwimlSessionInstance:
        """
        Fetch the TwimlSessionInstance


        :returns: The fetched TwimlSessionInstance
        """
        payload, _, _ = self._fetch()
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the TwimlSessionInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> TwimlSessionInstance:
        """
        Asynchronous coroutine to fetch the TwimlSessionInstance


        :returns: The fetched TwimlSessionInstance
        """
        payload, _, _ = await self._fetch_async()
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the TwimlSessionInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "Url": url,
                "Method": method,
                "Twiml": twiml,
                "Status": status,
            }
        )
        headers = values.of({})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> TwimlSessionInstance:
        """
        Update the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: The updated TwimlSessionInstance
        """
        payload, _, _ = self._update(url=url, method=method, twiml=twiml, status=status)
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )

    def update_with_http_info(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Update the TwimlSessionInstance and return response metadata

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            url=url, method=method, twiml=twiml, status=status
        )
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "Url": url,
                "Method": method,
                "Twiml": twiml,
                "Status": status,
            }
        )
        headers = values.of({})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> TwimlSessionInstance:
        """
        Asynchronous coroutine to update the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: The updated TwimlSessionInstance
        """
        payload, _, _ = await self._update_async(
            url=url, method=method, twiml=twiml, status=status
        )
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )

    async def update_with_http_info_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        status: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the TwimlSessionInstance and return response metadata

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param status: The new status of the TwiML Session. Possible values include `stopped`.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            url=url, method=method, twiml=twiml, status=status
        )
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Api.V2010.TwimlSessionContext {}>".format(context)


class TwimlSessionPage(Page):
    def get_instance(self, payload: Dict[str, Any]) -> TwimlSessionInstance:
        """
        Build an instance of TwimlSessionInstance

        :param payload: Payload response from the API
        """
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Api.V2010.TwimlSessionPage>"


class TwimlSessionList(ListResource):
    def __init__(self, version: Version, account_sid: str, call_sid: str):
        """
        Initialize the TwimlSessionList

        :param version: Version that contains the resource
        :param account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TwiML Session resources to read.
        :param call_sid: The SID of the [Call](https://www.twilio.com/docs/voice/api/call-resource) associated with the TwiML Session resources to read.

        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "account_sid": account_sid,
            "call_sid": call_sid,
        }
        self._uri = (
            "/Accounts/{account_sid}/Calls/{call_sid}/TwimlSessions.json".format(
                **self._solution
            )
        )

    def _create(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "Url": url,
                "Method": method,
                "Twiml": twiml,
                "SessionInput": session_input,
                "SessionOutput": session_output,
                "Name": name,
            }
        )
        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> TwimlSessionInstance:
        """
        Create the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param session_input: The input direction to provide to the TwiML Session.
        :param session_output: The output direction to generate from the TwiML Session.
        :param name: The name of the TwiML Session resource.

        :returns: The created TwimlSessionInstance
        """
        payload, _, _ = self._create(
            url=url,
            method=method,
            twiml=twiml,
            session_input=session_input,
            session_output=session_output,
            name=name,
        )
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
        )

    def create_with_http_info(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Create the TwimlSessionInstance and return response metadata

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param session_input: The input direction to provide to the TwiML Session.
        :param session_output: The output direction to generate from the TwiML Session.
        :param name: The name of the TwiML Session resource.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            url=url,
            method=method,
            twiml=twiml,
            session_input=session_input,
            session_output=session_output,
            name=name,
        )
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "Url": url,
                "Method": method,
                "Twiml": twiml,
                "SessionInput": session_input,
                "SessionOutput": session_output,
                "Name": name,
            }
        )
        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> TwimlSessionInstance:
        """
        Asynchronously create the TwimlSessionInstance

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param session_input: The input direction to provide to the TwiML Session.
        :param session_output: The output direction to generate from the TwiML Session.
        :param name: The name of the TwiML Session resource.

        :returns: The created TwimlSessionInstance
        """
        payload, _, _ = await self._create_async(
            url=url,
            method=method,
            twiml=twiml,
            session_input=session_input,
            session_output=session_output,
            name=name,
        )
        return TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
        )

    async def create_with_http_info_async(
        self,
        url: Union[str, object] = values.unset,
        method: Union[str, object] = values.unset,
        twiml: Union[str, object] = values.unset,
        session_input: Union[str, object] = values.unset,
        session_output: Union[str, object] = values.unset,
        name: Union[str, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the TwimlSessionInstance and return response metadata

        :param url: The URL to fetch TwiML instructions from during the session.
        :param method: The HTTP method to use when fetching the TwiML instructions from the URL. Possible values are `GET` or `POST`.
        :param twiml: The TwiML instructions to execute during the session.
        :param session_input: The input direction to provide to the TwiML Session.
        :param session_output: The output direction to generate from the TwiML Session.
        :param name: The name of the TwiML Session resource.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            url=url,
            method=method,
            twiml=twiml,
            session_input=session_input,
            session_output=session_output,
            name=name,
        )
        instance = TwimlSessionInstance(
            self._version,
            payload,
            account_sid=self._solution["account_sid"],
            call_sid=self._solution["call_sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[TwimlSessionInstance]:
        """
        Streams TwimlSessionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[TwimlSessionInstance]:
        """
        Asynchronously streams TwimlSessionInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(page_size=limits["page_size"])

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams TwimlSessionInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(page_size=limits["page_size"])

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams TwimlSessionInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[TwimlSessionInstance]:
        """
        Lists TwimlSessionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[TwimlSessionInstance]:
        """
        Asynchronously lists TwimlSessionInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists TwimlSessionInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists TwimlSessionInstance and returns headers from first page


        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TwimlSessionPage:
        """
        Retrieve a single page of TwimlSessionInstance records from the API.
        Request is executed immediately

        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TwimlSessionInstance
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return TwimlSessionPage(self._version, response, self._solution)

    async def page_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> TwimlSessionPage:
        """
        Asynchronously retrieve a single page of TwimlSessionInstance records from the API.
        Request is executed immediately

        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of TwimlSessionInstance
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return TwimlSessionPage(self._version, response, self._solution)

    def page_with_http_info(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TwimlSessionPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = TwimlSessionPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with TwimlSessionPage, status code, and headers
        """
        data = values.of(
            {
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        (
            response,
            status_code,
            response_headers,
        ) = await self._version.page_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = TwimlSessionPage(self._version, response, self._solution)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> TwimlSessionPage:
        """
        Retrieve a specific page of TwimlSessionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TwimlSessionInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return TwimlSessionPage(self._version, response, self._solution)

    async def get_page_async(self, target_url: str) -> TwimlSessionPage:
        """
        Asynchronously retrieve a specific page of TwimlSessionInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of TwimlSessionInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return TwimlSessionPage(self._version, response, self._solution)

    def get(self, account_sid: str, call_sid: str, sid: str) -> TwimlSessionContext:
        """
        Constructs a TwimlSessionContext

        :param account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TwiML Session resource to update.
        :param call_sid: The SID of the [Call](https://www.twilio.com/docs/voice/api/call-resource) associated with the TwiML Session resource to update.
        :param sid: The unique string that identifies the TwiML Session resource to update.
        """
        return TwimlSessionContext(
            self._version, account_sid=account_sid, call_sid=call_sid, sid=sid
        )

    def __call__(
        self, account_sid: str, call_sid: str, sid: str
    ) -> TwimlSessionContext:
        """
        Constructs a TwimlSessionContext

        :param account_sid: The SID of the [Account](https://www.twilio.com/docs/iam/api/account) that created the TwiML Session resource to update.
        :param call_sid: The SID of the [Call](https://www.twilio.com/docs/voice/api/call-resource) associated with the TwiML Session resource to update.
        :param sid: The unique string that identifies the TwiML Session resource to update.
        """
        return TwimlSessionContext(
            self._version, account_sid=account_sid, call_sid=call_sid, sid=sid
        )

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Api.V2010.TwimlSessionList>"
