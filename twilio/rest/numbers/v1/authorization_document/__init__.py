r"""
    This code was generated by
   ___ _ _ _ _ _    _ ____    ____ ____ _    ____ ____ _  _ ____ ____ ____ ___ __   __
    |  | | | | |    | |  | __ |  | |__| | __ | __ |___ |\ | |___ |__/ |__|  | |  | |__/
    |  |_|_| | |___ | |__|    |__| |  | |    |__] |___ | \| |___ |  \ |  |  | |__| |  \

    Twilio - Numbers
    This is the public Twilio REST API.

    NOTE: This class is auto generated by OpenAPI Generator.
    https://openapi-generator.tech
    Do not edit the class manually.
"""


from datetime import datetime
from typing import Any, Dict, List, Optional, Union, Iterator, AsyncIterator
from twilio.base import deserialize, serialize, values
from twilio.base.api_response import ApiResponse
from twilio.base.instance_context import InstanceContext
from twilio.base.instance_resource import InstanceResource
from twilio.base.list_resource import ListResource
from twilio.base.version import Version
from twilio.base.page import Page
from twilio.rest.numbers.v1.authorization_document.dependent_order import (
    DependentOrderList,
)


class AuthorizationDocumentInstance(InstanceResource):
    class Status(object):
        OPENED = "opened"
        SIGNING = "signing"
        SIGNED = "signed"
        CANCELED = "canceled"
        FAILED = "failed"

    """
    :ivar address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument resource.
    :ivar cc_emails: The email addresses that the authorization document will be copied to.
    :ivar date_created: The date and time in GMT when the resource was created specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar date_updated: The date and time in GMT when the resource was last updated specified in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) format.
    :ivar email: The email address that the authorization document will be sent to for signing.
    :ivar links: A list of the URLs to the HostedNumberOrder resources that the authorization document will authorize for hosting phone number capabilities on our platform.
    :ivar sid: The unique string that we created to identify the AuthorizationDocument resource.
    :ivar status: 
    :ivar url: The absolute URL of the AuthorizationDocument resource.
    """

    def __init__(
        self, version: Version, payload: Dict[str, Any], sid: Optional[str] = None
    ):
        super().__init__(version)

        self.address_sid: Optional[str] = payload.get("address_sid")
        self.cc_emails: Optional[List[str]] = payload.get("cc_emails")
        self.date_created: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("date_created")
        )
        self.date_updated: Optional[datetime] = deserialize.iso8601_datetime(
            payload.get("date_updated")
        )
        self.email: Optional[str] = payload.get("email")
        self.links: Optional[Dict[str, object]] = payload.get("links")
        self.sid: Optional[str] = payload.get("sid")
        self.status: Optional["AuthorizationDocumentInstance.Status"] = payload.get(
            "status"
        )
        self.url: Optional[str] = payload.get("url")

        self._solution = {
            "sid": sid or self.sid,
        }
        self._context: Optional[AuthorizationDocumentContext] = None

    @property
    def _proxy(self) -> "AuthorizationDocumentContext":
        """
        Generate an instance context for the instance, the context is capable of
        performing various actions. All instance actions are proxied to the context

        :returns: AuthorizationDocumentContext for this AuthorizationDocumentInstance
        """
        if self._context is None:
            self._context = AuthorizationDocumentContext(
                self._version,
                sid=self._solution["sid"],
            )
        return self._context

    def delete(self) -> bool:
        """
        Deletes the AuthorizationDocumentInstance


        :returns: True if delete succeeds, False otherwise
        """
        return self._proxy.delete()

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AuthorizationDocumentInstance


        :returns: True if delete succeeds, False otherwise
        """
        return await self._proxy.delete_async()

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the AuthorizationDocumentInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return self._proxy.delete_with_http_info()

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AuthorizationDocumentInstance with HTTP info


        :returns: ApiResponse with success boolean, status code, and headers
        """
        return await self._proxy.delete_with_http_info_async()

    def fetch(self) -> "AuthorizationDocumentInstance":
        """
        Fetch the AuthorizationDocumentInstance


        :returns: The fetched AuthorizationDocumentInstance
        """
        return self._proxy.fetch()

    async def fetch_async(self) -> "AuthorizationDocumentInstance":
        """
        Asynchronous coroutine to fetch the AuthorizationDocumentInstance


        :returns: The fetched AuthorizationDocumentInstance
        """
        return await self._proxy.fetch_async()

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the AuthorizationDocumentInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.fetch_with_http_info()

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the AuthorizationDocumentInstance with HTTP info


        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.fetch_with_http_info_async()

    def update(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> "AuthorizationDocumentInstance":
        """
        Update the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: The updated AuthorizationDocumentInstance
        """
        return self._proxy.update(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )

    async def update_async(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> "AuthorizationDocumentInstance":
        """
        Asynchronous coroutine to update the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: The updated AuthorizationDocumentInstance
        """
        return await self._proxy.update_async(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )

    def update_with_http_info(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> ApiResponse:
        """
        Update the AuthorizationDocumentInstance with HTTP info

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: ApiResponse with instance, status code, and headers
        """
        return self._proxy.update_with_http_info(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )

    async def update_with_http_info_async(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the AuthorizationDocumentInstance with HTTP info

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: ApiResponse with instance, status code, and headers
        """
        return await self._proxy.update_with_http_info_async(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )

    @property
    def dependent_orders(self) -> DependentOrderList:
        """
        Access the dependent_orders
        """
        return self._proxy.dependent_orders

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Numbers.V1.AuthorizationDocumentInstance {}>".format(context)


class AuthorizationDocumentContext(InstanceContext):
    def __init__(self, version: Version, sid: str):
        """
        Initialize the AuthorizationDocumentContext

        :param version: Version that contains the resource
        :param sid: The SID of the AuthorizationDocument resource to update.
        """
        super().__init__(version)

        # Path Solution
        self._solution = {
            "sid": sid,
        }
        self._uri = "/HostedNumber/AuthorizationDocuments/{sid}".format(
            **self._solution
        )

        self._dependent_orders: Optional[DependentOrderList] = None

    def _delete(self) -> tuple:
        """
        Internal helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        return self._version.delete_with_response_info(
            method="DELETE", uri=self._uri, headers=headers
        )

    def delete(self) -> bool:
        """
        Deletes the AuthorizationDocumentInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = self._delete()
        return success

    def delete_with_http_info(self) -> ApiResponse:
        """
        Deletes the AuthorizationDocumentInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = self._delete()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    async def _delete_async(self) -> tuple:
        """
        Internal async helper for delete operation

        Returns:
            tuple: (success_boolean, status_code, headers)
        """

        headers = values.of({})

        return await self._version.delete_with_response_info_async(
            method="DELETE", uri=self._uri, headers=headers
        )

    async def delete_async(self) -> bool:
        """
        Asynchronous coroutine that deletes the AuthorizationDocumentInstance


        :returns: True if delete succeeds, False otherwise
        """
        success, _, _ = await self._delete_async()
        return success

    async def delete_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine that deletes the AuthorizationDocumentInstance and return response metadata


        :returns: ApiResponse with success boolean, status code, and headers
        """
        success, status_code, headers = await self._delete_async()
        return ApiResponse(data=success, status_code=status_code, headers=headers)

    def _fetch(self) -> tuple:
        """
        Internal helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return self._version.fetch_with_response_info(
            method="GET", uri=self._uri, headers=headers
        )

    def fetch(self) -> AuthorizationDocumentInstance:
        """
        Fetch the AuthorizationDocumentInstance


        :returns: The fetched AuthorizationDocumentInstance
        """
        payload, _, _ = self._fetch()
        return AuthorizationDocumentInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )

    def fetch_with_http_info(self) -> ApiResponse:
        """
        Fetch the AuthorizationDocumentInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._fetch()
        instance = AuthorizationDocumentInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _fetch_async(self) -> tuple:
        """
        Internal async helper for fetch operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        headers = values.of({})

        headers["Accept"] = "application/json"

        return await self._version.fetch_with_response_info_async(
            method="GET", uri=self._uri, headers=headers
        )

    async def fetch_async(self) -> AuthorizationDocumentInstance:
        """
        Asynchronous coroutine to fetch the AuthorizationDocumentInstance


        :returns: The fetched AuthorizationDocumentInstance
        """
        payload, _, _ = await self._fetch_async()
        return AuthorizationDocumentInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )

    async def fetch_with_http_info_async(self) -> ApiResponse:
        """
        Asynchronous coroutine to fetch the AuthorizationDocumentInstance and return response metadata


        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._fetch_async()
        instance = AuthorizationDocumentInstance(
            self._version,
            payload,
            sid=self._solution["sid"],
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def _update(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> tuple:
        """
        Internal helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "AddressSid": address_sid,
                "CcEmails": serialize.map(cc_emails, lambda e: e),
                "Email": email,
                "HostedNumberOrderSids": serialize.map(
                    hosted_number_order_sids, lambda e: e
                ),
                "Status": status,
            }
        )
        headers = values.of({})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return self._version.update_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def update(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> AuthorizationDocumentInstance:
        """
        Update the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: The updated AuthorizationDocumentInstance
        """
        payload, _, _ = self._update(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )
        return AuthorizationDocumentInstance(
            self._version, payload, sid=self._solution["sid"]
        )

    def update_with_http_info(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> ApiResponse:
        """
        Update the AuthorizationDocumentInstance and return response metadata

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._update(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )
        instance = AuthorizationDocumentInstance(
            self._version, payload, sid=self._solution["sid"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _update_async(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for update operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "AddressSid": address_sid,
                "CcEmails": serialize.map(cc_emails, lambda e: e),
                "Email": email,
                "HostedNumberOrderSids": serialize.map(
                    hosted_number_order_sids, lambda e: e
                ),
                "Status": status,
            }
        )
        headers = values.of({})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return await self._version.update_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def update_async(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> AuthorizationDocumentInstance:
        """
        Asynchronous coroutine to update the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: The updated AuthorizationDocumentInstance
        """
        payload, _, _ = await self._update_async(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )
        return AuthorizationDocumentInstance(
            self._version, payload, sid=self._solution["sid"]
        )

    async def update_with_http_info_async(
        self,
        address_sid: Union[str, object] = values.unset,
        cc_emails: Union[List[str], object] = values.unset,
        email: Union[str, object] = values.unset,
        hosted_number_order_sids: Union[List[str], object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronous coroutine to update the AuthorizationDocumentInstance and return response metadata

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument to update.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.
        :param email: The email address that the authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the authorization document will authorize for hosting phone number capabilities on our platform.
        :param status:

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._update_async(
            address_sid=address_sid,
            cc_emails=cc_emails,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            status=status,
        )
        instance = AuthorizationDocumentInstance(
            self._version, payload, sid=self._solution["sid"]
        )
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    @property
    def dependent_orders(self) -> DependentOrderList:
        """
        Access the dependent_orders
        """
        if self._dependent_orders is None:
            self._dependent_orders = DependentOrderList(
                self._version,
                self._solution["sid"],
            )
        return self._dependent_orders

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        context = " ".join("{}={}".format(k, v) for k, v in self._solution.items())
        return "<Twilio.Numbers.V1.AuthorizationDocumentContext {}>".format(context)


class AuthorizationDocumentPage(Page):
    def get_instance(self, payload: Dict[str, Any]) -> AuthorizationDocumentInstance:
        """
        Build an instance of AuthorizationDocumentInstance

        :param payload: Payload response from the API
        """
        return AuthorizationDocumentInstance(self._version, payload)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Numbers.V1.AuthorizationDocumentPage>"


class AuthorizationDocumentList(ListResource):
    def __init__(self, version: Version):
        """
        Initialize the AuthorizationDocumentList

        :param version: Version that contains the resource

        """
        super().__init__(version)

        self._uri = "/HostedNumber/AuthorizationDocuments"

    def _create(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> tuple:
        """
        Internal helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "AddressSid": address_sid,
                "ContactPhoneNumber": contact_phone_number,
                "ContactTitle": contact_title,
                "Email": email,
                "HostedNumberOrderSids": serialize.map(
                    hosted_number_order_sids, lambda e: e
                ),
                "CcEmails": serialize.map(cc_emails, lambda e: e),
            }
        )
        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return self._version.create_with_response_info(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    def create(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> AuthorizationDocumentInstance:
        """
        Create the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument resource to update.
        :param contact_phone_number: The [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number of the person who will sign the new authorization document.
        :param contact_title: The formal title of the person who will sign the authorization document. For example, `Mr`, `Product Manager`, or `Marketing Director`.
        :param email: The email address that the new authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the new authorization document will authorize for hosting phone number capabilities on our platform.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.

        :returns: The created AuthorizationDocumentInstance
        """
        payload, _, _ = self._create(
            address_sid=address_sid,
            contact_phone_number=contact_phone_number,
            contact_title=contact_title,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            cc_emails=cc_emails,
        )
        return AuthorizationDocumentInstance(self._version, payload)

    def create_with_http_info(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> ApiResponse:
        """
        Create the AuthorizationDocumentInstance and return response metadata

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument resource to update.
        :param contact_phone_number: The [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number of the person who will sign the new authorization document.
        :param contact_title: The formal title of the person who will sign the authorization document. For example, `Mr`, `Product Manager`, or `Marketing Director`.
        :param email: The email address that the new authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the new authorization document will authorize for hosting phone number capabilities on our platform.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = self._create(
            address_sid=address_sid,
            contact_phone_number=contact_phone_number,
            contact_title=contact_title,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            cc_emails=cc_emails,
        )
        instance = AuthorizationDocumentInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    async def _create_async(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> tuple:
        """
        Internal async helper for create operation

        Returns:
            tuple: (payload, status_code, headers)
        """

        data = values.of(
            {
                "AddressSid": address_sid,
                "ContactPhoneNumber": contact_phone_number,
                "ContactTitle": contact_title,
                "Email": email,
                "HostedNumberOrderSids": serialize.map(
                    hosted_number_order_sids, lambda e: e
                ),
                "CcEmails": serialize.map(cc_emails, lambda e: e),
            }
        )
        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Content-Type"] = "application/x-www-form-urlencoded"

        headers["Accept"] = "application/json"

        return await self._version.create_with_response_info_async(
            method="POST", uri=self._uri, data=data, headers=headers
        )

    async def create_async(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> AuthorizationDocumentInstance:
        """
        Asynchronously create the AuthorizationDocumentInstance

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument resource to update.
        :param contact_phone_number: The [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number of the person who will sign the new authorization document.
        :param contact_title: The formal title of the person who will sign the authorization document. For example, `Mr`, `Product Manager`, or `Marketing Director`.
        :param email: The email address that the new authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the new authorization document will authorize for hosting phone number capabilities on our platform.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.

        :returns: The created AuthorizationDocumentInstance
        """
        payload, _, _ = await self._create_async(
            address_sid=address_sid,
            contact_phone_number=contact_phone_number,
            contact_title=contact_title,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            cc_emails=cc_emails,
        )
        return AuthorizationDocumentInstance(self._version, payload)

    async def create_with_http_info_async(
        self,
        address_sid: str,
        contact_phone_number: str,
        contact_title: str,
        email: str,
        hosted_number_order_sids: List[str],
        cc_emails: Union[List[str], object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously create the AuthorizationDocumentInstance and return response metadata

        :param address_sid: The SID of the [Address](https://www.twilio.com/docs/usage/api/address) resource that is associated with the AuthorizationDocument resource to update.
        :param contact_phone_number: The [E.164](https://www.twilio.com/docs/glossary/what-e164) formatted phone number of the person who will sign the new authorization document.
        :param contact_title: The formal title of the person who will sign the authorization document. For example, `Mr`, `Product Manager`, or `Marketing Director`.
        :param email: The email address that the new authorization document should be sent to for signing.
        :param hosted_number_order_sids: The HostedNumberOrder SIDs that the new authorization document will authorize for hosting phone number capabilities on our platform.
        :param cc_emails: The email addresses that the new authorization document should be copied to. This parameter takes only one email address. To provide more than one email address, repeat this parameter for each address.

        :returns: ApiResponse with instance, status code, and headers
        """
        payload, status_code, headers = await self._create_async(
            address_sid=address_sid,
            contact_phone_number=contact_phone_number,
            contact_title=contact_title,
            email=email,
            hosted_number_order_sids=hosted_number_order_sids,
            cc_emails=cc_emails,
        )
        instance = AuthorizationDocumentInstance(self._version, payload)
        return ApiResponse(data=instance, status_code=status_code, headers=headers)

    def stream(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> Iterator[AuthorizationDocumentInstance]:
        """
        Streams AuthorizationDocumentInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = self.page(email=email, status=status, page_size=limits["page_size"])

        return self._version.stream(page, limits["limit"])

    async def stream_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> AsyncIterator[AuthorizationDocumentInstance]:
        """
        Asynchronously streams AuthorizationDocumentInstance records from the API as a generator stream.
        This operation lazily loads records as efficiently as possible until the limit
        is reached.
        The results are returned as a generator, so this operation is memory efficient.

        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: Generator that will yield up to limit results
        """
        limits = self._version.read_limits(limit, page_size)
        page = await self.page_async(
            email=email, status=status, page_size=limits["page_size"]
        )

        return self._version.stream_async(page, limits["limit"])

    def stream_with_http_info(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Streams AuthorizationDocumentInstance and returns headers from first page


        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = self.page_with_http_info(
            email=email, status=status, page_size=limits["page_size"]
        )

        generator = self._version.stream(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    async def stream_with_http_info_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> tuple:
        """
        Asynchronously streams AuthorizationDocumentInstance and returns headers from first page


        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. stream()
                      guarantees to never return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, stream() will attempt to read the
                          limit with the most efficient page size, i.e. min(limit, 1000)

        :returns: tuple of (generator, status_code, headers) where generator yields instances
        """
        limits = self._version.read_limits(limit, page_size)
        page_response = await self.page_with_http_info_async(
            email=email, status=status, page_size=limits["page_size"]
        )

        generator = self._version.stream_async(page_response.data, limits["limit"])
        return (generator, page_response.status_code, page_response.headers)

    def list(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AuthorizationDocumentInstance]:
        """
        Lists AuthorizationDocumentInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return list(
            self.stream(
                email=email,
                status=status,
                limit=limit,
                page_size=page_size,
            )
        )

    async def list_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> List[AuthorizationDocumentInstance]:
        """
        Asynchronously lists AuthorizationDocumentInstance records from the API as a list.
        Unlike stream(), this operation is eager and will load `limit` records into
        memory before returning.

        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: list that will contain up to limit results
        """
        return [
            record
            async for record in await self.stream_async(
                email=email,
                status=status,
                limit=limit,
                page_size=page_size,
            )
        ]

    def list_with_http_info(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Lists AuthorizationDocumentInstance and returns headers from first page


        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = self.stream_with_http_info(
            email=email,
            status=status,
            limit=limit,
            page_size=page_size,
        )
        items = list(generator)
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    async def list_with_http_info_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        limit: Optional[int] = None,
        page_size: Optional[int] = None,
    ) -> ApiResponse:
        """
        Asynchronously lists AuthorizationDocumentInstance and returns headers from first page


        :param str email: The email address of the AuthorizationDocument resources to read.
        :param &quot;AuthorizationDocumentInstance.Status&quot; status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param limit: Upper limit for the number of records to return. list() guarantees
                      never to return more than limit.  Default is no limit
        :param page_size: Number of records to fetch per request, when not set will use
                          the default value of 50 records.  If no page_size is defined
                          but a limit is defined, list() will attempt to read the limit
                          with the most efficient page size, i.e. min(limit, 1000)

        :returns: ApiResponse with list of instances, status code, and headers
        """
        generator, status_code, headers = await self.stream_with_http_info_async(
            email=email,
            status=status,
            limit=limit,
            page_size=page_size,
        )
        items = [record async for record in generator]
        return ApiResponse(data=items, status_code=status_code, headers=headers)

    def page(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AuthorizationDocumentPage:
        """
        Retrieve a single page of AuthorizationDocumentInstance records from the API.
        Request is executed immediately

        :param email: The email address of the AuthorizationDocument resources to read.
        :param status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AuthorizationDocumentInstance
        """
        data = values.of(
            {
                "Email": email,
                "Status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = self._version.page(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return AuthorizationDocumentPage(self._version, response)

    async def page_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> AuthorizationDocumentPage:
        """
        Asynchronously retrieve a single page of AuthorizationDocumentInstance records from the API.
        Request is executed immediately

        :param email: The email address of the AuthorizationDocument resources to read.
        :param status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: Page of AuthorizationDocumentInstance
        """
        data = values.of(
            {
                "Email": email,
                "Status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response = await self._version.page_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        return AuthorizationDocumentPage(self._version, response)

    def page_with_http_info(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Retrieve a single page with response metadata


        :param email: The email address of the AuthorizationDocument resources to read.
        :param status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationDocumentPage, status code, and headers
        """
        data = values.of(
            {
                "Email": email,
                "Status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        response, status_code, response_headers = self._version.page_with_response_info(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = AuthorizationDocumentPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    async def page_with_http_info_async(
        self,
        email: Union[str, object] = values.unset,
        status: Union["AuthorizationDocumentInstance.Status", object] = values.unset,
        page_token: Union[str, object] = values.unset,
        page_number: Union[int, object] = values.unset,
        page_size: Union[int, object] = values.unset,
    ) -> ApiResponse:
        """
        Asynchronously retrieve a single page with response metadata


        :param email: The email address of the AuthorizationDocument resources to read.
        :param status: The status of the AuthorizationDocument resources to read. Can be: `opened`, `signing`, `signed`, `canceled`, or `failed`.
        :param page_token: PageToken provided by the API
        :param page_number: Page Number, this value is simply for client state
        :param page_size: Number of records to return, defaults to 50

        :returns: ApiResponse with AuthorizationDocumentPage, status code, and headers
        """
        data = values.of(
            {
                "Email": email,
                "Status": status,
                "PageToken": page_token,
                "Page": page_number,
                "PageSize": page_size,
            }
        )

        headers = values.of({"Content-Type": "application/x-www-form-urlencoded"})

        headers["Accept"] = "application/json"

        (
            response,
            status_code,
            response_headers,
        ) = await self._version.page_with_response_info_async(
            method="GET", uri=self._uri, params=data, headers=headers
        )
        page = AuthorizationDocumentPage(self._version, response)
        return ApiResponse(data=page, status_code=status_code, headers=response_headers)

    def get_page(self, target_url: str) -> AuthorizationDocumentPage:
        """
        Retrieve a specific page of AuthorizationDocumentInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationDocumentInstance
        """
        response = self._version.domain.twilio.request("GET", target_url)
        return AuthorizationDocumentPage(self._version, response)

    async def get_page_async(self, target_url: str) -> AuthorizationDocumentPage:
        """
        Asynchronously retrieve a specific page of AuthorizationDocumentInstance records from the API.
        Request is executed immediately

        :param target_url: API-generated URL for the requested results page

        :returns: Page of AuthorizationDocumentInstance
        """
        response = await self._version.domain.twilio.request_async("GET", target_url)
        return AuthorizationDocumentPage(self._version, response)

    def get(self, sid: str) -> AuthorizationDocumentContext:
        """
        Constructs a AuthorizationDocumentContext

        :param sid: The SID of the AuthorizationDocument resource to update.
        """
        return AuthorizationDocumentContext(self._version, sid=sid)

    def __call__(self, sid: str) -> AuthorizationDocumentContext:
        """
        Constructs a AuthorizationDocumentContext

        :param sid: The SID of the AuthorizationDocument resource to update.
        """
        return AuthorizationDocumentContext(self._version, sid=sid)

    def __repr__(self) -> str:
        """
        Provide a friendly representation

        :returns: Machine friendly representation
        """
        return "<Twilio.Numbers.V1.AuthorizationDocumentList>"
